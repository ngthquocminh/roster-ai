---
scope: ShiftMind governed scheduling agent portfolio MVP
purpose: build substrate for independently implemented epics
altitude: feature
updated: 2026-07-22T16:25
---

- (event) Fast/autonomous create path confirmed; the architecture spine is the only requested deliverable.
- (constraint) The final PRD and technical addendum are authoritative product inputs; the UX memlog plus EXPERIENCE.md and DESIGN.md bind the scenario workspace and evidence-navigation behavior.
- (constraint) Brownfield reality: React/Vite SPA, generated OpenAPI TypeScript client, FastAPI service/use-case/repository split, pure scheduling domain, SchedulerEngine protocol, CP-SAT implementation, SQLite repository, in-process single-thread worker, and unauthenticated globally visible resources.
- (constraint) The later PRD technical research supersedes the brief SQLite/local-worker assumption: PostgreSQL durability, cancellation/recovery evidence, AWS deployment, tested restore, and rollback are required.
- (decision) Adopt a hexagonal modular monolith with durable workflow state machines: domain and application ports remain inward; HTTP, persistence, model, solver, identity, telemetry, and object storage are replaceable adapters. Binds all implementation units and prevents framework/provider types from becoming product contracts.
- (decision) Keep one deployable backend image but run separate stateless API and lease-based worker processes. Binds HTTP, agent, solver, and job units and prevents CPU-heavy or resumable work from depending on an API process, background thread, or browser connection.
- (decision) Application code owns identity, site scope, authorization, policy, risk, approvals, versions, budgets, idempotency, state transitions, persistence, and audit; the model only interprets and orchestrates, and CP-SAT alone constructs or validates accepted schedules. Prevents stochastic output or the UI from becoming authority.
- (decision) All inbound adapters call application use cases; agent tools never call repositories or solver adapters directly. Domain and application modules may depend on ports but never on FastAPI, PydanticAI, SQLAlchemy, Cognito, S3, Logfire, or concrete model providers. Prevents incompatible business paths and adapter lock-in.
- (decision) Resolve actor and site from the authenticated server session and current PostgreSQL membership on every request, tool call, job lease, approval, audit lookup, and evidence read. Repositories require trusted site context and PostgreSQL RLS is defense in depth. Prevents browser/model-supplied tenant authority and cross-site access.
- (decision) Enforce the portfolio one-user/one-active-membership rule in provisioning and database invariants, not only the UI; retain organization/site/membership ownership for later SaaS. Prevents MVP shortcuts from making tenant ownership untraceable.
- (decision) Scenario fixtures are immutable scenario_version records. The read-only Scenario Data endpoint and agent inspection tool share one application-owned normalized projection and evidence identifiers; the MVP exposes no scenario mutation command, route, or UI control. Prevents viewer-agent drift and accidental source-data writes.
- (decision) Represent schedule changes as immutable proposals and schedule versions. Drafts are reversible and isolated; baseline promotion is the only operational schedule mutation and uses optimistic expected-version checks. Prevents silent rebasing and in-place schedule mutation.
- (decision) Compose agent tools from versioned capability modules in an application-owned registry. Each module declares typed contracts, permissions, risk, resource scope, approval policy, budgets, idempotency, safe audit/evidence mapping, and evaluation cases; read and write capabilities remain distinct. Prevents core-loop branching and capability names from granting authority.
- (decision) Persist conversations, messages, agent runs, tool calls, jobs, approval requests, events, schedule runs, and terminal outcomes before acknowledging accepted work. Worker leases and checkpoints drive recovery; persisted monotonically sequenced events feed SSE replay via Last-Event-ID. Prevents transient streams or process memory from being workflow state.
- (decision) Use an explicit run state machine with queued, running, approval_required, completed, infeasible, timed_out, cancellation_requested, cancelled, and failed states; transitions are compare-and-set and terminal states are immutable. Prevents contradictory retry, cancellation, and UI interpretations.
- (decision) Every mutating HTTP command requires a caller idempotency key scoped to actor, site, operation, and canonical body hash plus expected resource version; tool/worker effects use stable agent_run_id/tool_call_id or job effect keys backed by unique constraints. Replays return the original semantic outcome. Prevents duplicate jobs and baseline effects.
- (decision) Approval is a persisted one-time state machine bound to actor, site, action type, normalized parameter hash, candidate and baseline versions, consequence-summary hash, policy version, and expiry. Promotion revalidates all bindings and commits the new baseline pointer, schedule version, approval decision, and successful audit atomically. Prevents stale, replayed, or client-boolean authorization.
- (decision) Version-bound evidence references identify scenario version, evidence group, record ID, and optional field/time range. Numerical claims and comparison deltas are produced or verified by application-owned calculators against immutable evidence; missing, unauthorized, and mismatched evidence fail distinctly. Prevents fabricated numbers and silent evidence fallback.
- (decision) Keep product state, business audit, operational logs, AI traces, and evaluation results as distinct records of truth. Audit is unsampled append-only PostgreSQL evidence with checksummed immutable S3 references; CloudWatch diagnoses AWS; sanitized OpenTelemetry/Logfire is optional for correctness; version-controlled evals gate releases. Prevents telemetry loss or schema churn from corrupting authority.
- (decision) Expose versioned REST/JSON commands and queries from FastAPI, generate the TypeScript client from OpenAPI, and maintain one frontend API client. Use RFC 7807-style error codes plus resource versions and persisted SSE only for updates. Prevents hand-authored contract drift and transport-specific workflow semantics.
- (decision) The frontend keeps server state in TanStack Query and route/UI state locally; it never treats cached state as authorization or current version. Chat, Scenario Data, Runs, and Results are peer scenario views with evidence deep links that preserve return context. Prevents multiple client state owners and approval-by-rendering.
- (decision) Untrusted prompts, messages, fixture content, tool output, and future uploads cannot modify tool policy or trusted dependencies. No arbitrary SQL, shell, credentials, unrestricted network, identity administration, or model-generated capability loading exists. Secrets stay server-side and prompt/tool content export is disabled by default. Prevents prompt injection from expanding authority.
- (decision) Use deterministic model doubles and versioned golden datasets in normal CI; live-provider tests are explicit and budgeted. Authorization, approval, tenant isolation, grounding, hard constraints, idempotency, audit continuity, viewer-agent parity, worker recovery, accessibility, backup/restore, and rollback failures block release. Prevents stochastic demos from substituting for architecture proof.
- (decision) Deploy the SPA through CloudFront and private S3; route API/SSE through ALB to ECS Fargate API; run a separate ECS Fargate worker against RDS PostgreSQL and immutable S3 evidence. Use Cognito through a BFF session boundary, ECR immutable images, Secrets Manager, CloudWatch, and sanitized hosted Logfire. Prevents public storage, mixed process authority, and hidden operational dependencies.
- (decision) Provision AWS with Terraform and GitHub Actions OIDC using reviewed plans, environment-specific state, least-privilege API/worker task roles, backward-compatible schema migrations, automated RDS backups, restore drills, health-gated deployment, and rollback to the prior image. Prevents console drift, long-lived deploy keys, and unrecoverable releases.
- (decision) Use PostgreSQL job leasing first; add a transactional outbox/SQS only for measured broker or independent-consumer need, and reconsider a durable workflow engine only for genuinely branching multi-day or cross-system workflows. Prevents premature distributed coordination.
- (assumption) The low-cost portfolio environment may run one API task, one worker task, and small single-AZ RDS capacity, with limitations disclosed; Multi-AZ, formal SLO/RTO/RPO, and autoscaling require measured pre-pilot decisions.
- (assumption) The seeded planner may self-approve in the one-user MVP, but approval remains a distinct exact-action step; separation of duties is revisited before a second user is activated.
- (version) Brownfield lock verified: Python support is >=3.10,<3.13; select Python 3.12 for containers. OR-Tools is pinned 9.11.4210 because 9.15 segfaults on the development machine; FastAPI 0.138.1, Pydantic 2.13.4, Uvicorn 0.49.0, React 19.2.7, React Router 8.2.0, Vite 8.1.5, TypeScript 5.9.3, TanStack Query 5.101.2, and openapi-fetch 0.17.0 are current repository locks.
- (version) Current planned backend pins verified 2026-07-22: PydanticAI 2.14.1 (PyPI latest 2026-07-21; stable V2 policy), PostgreSQL 18.4 current docs, SQLAlchemy 2.0.51 current stable docs, Psycopg 3.3.4 current release, Alembic 1.18.5 current release, Logfire SDK 4.38.0, and Terraform 1.15.5 latest stable.
- (direction) Alternatives weighed and rejected for the MVP spine: microservices versus modular monolith; SQS/Celery/Temporal versus PostgreSQL leases; direct SPA bearer tokens versus Cognito-backed BFF session; WebSockets versus replayable SSE; and duplicated viewer/tool projections versus one normalized read contract. The selected options minimize coordination while preserving the required recovery and authority boundaries.
- (question) Customer-specific retention, residency, compliance, public SLOs, concurrency, recovery objectives, role separation, and first external integrations remain open until pre-pilot discovery or measured AWS traffic; they must not be claimed by the portfolio MVP.
- (decision) Use server-generated opaque UUID strings for internal aggregate IDs, preserve source fixture identifiers as version-bound domain keys, store timestamps as UTC PostgreSQL timestamptz and expose RFC 3339, suffix measured fields with units, use monotonic bigint resource versions and event sequences, and name events noun.past_tense.v1. Prevents cross-module format drift.
- (decision) Application command handlers own transaction boundaries and return stable domain/application errors; HTTP maps them to RFC 7807 problem details with a stable code, correlation ID, resource ID, and current version when relevant. Prevents repositories and adapters from committing independently or inventing incompatible error shapes.
- (decision) Operate exactly three MVP environment classes: local developer, ephemeral CI, and one AWS portfolio environment. The AWS environment is production-shaped but not represented as customer production; a separate staging/customer-production topology is added only before an external pilot. Prevents environment sprawl and unsupported production claims.
- (version) AWS fit verified 2026-07-22: Amazon RDS supports PostgreSQL 18.4, and RDS PostgreSQL 18 has been generally supported since 2025-11-14.
- (decision) Cognito authentication uses OIDC authorization-code flow with PKCE at the FastAPI BFF. Tokens remain server-side; the browser holds only a Secure HttpOnly SameSite cookie for an opaque application session. Unsafe methods require same-origin validation plus a CSRF token, and each request re-resolves current membership. Prevents token exposure, login CSRF, and stale session authorization.
- (decision) Each leased job carries owner, lease expiry, heartbeat, and monotonically increasing fencing epoch. A worker may checkpoint or commit an effect only with the current epoch; expired work may recompute but unique effect keys prevent duplicate commits. Cancellation is persisted and cooperative. Prevents a paused former lease holder from committing after recovery.
- (decision) Capability risk classes are exactly inspect, draft, compute, consequential, and prohibited. Compute requires a current explicit planner run request; consequential requires exact-action approval; prohibited capabilities are absent. Prevents modules from inventing weaker autonomy tiers.
- (decision) Model outage bypasses AgentRuntime only: authenticated Scenario Data, saved results, and manual deterministic solver commands remain available through the same site, version, idempotency, and audit controls. Prevents conversational availability from becoming product availability.
- (decision) RDS remains non-public in private subnets; API is reachable only through ALB/CloudFront and worker has no inbound listener. Portfolio egress implementation may optimize cost but must be documented and must not expose the database or evidence bucket. Prevents low-cost networking from weakening data authority.
- (decision) Evidence locators bind scenario version plus the producing or baseline schedule/run version, evidence group, record ID, and optional field/time range. Scenario Data list APIs use deterministic stable ordering, bounded cursor windows with total/remaining counts, and exact-target lookup so deep links can reveal a record outside the current window. Prevents non-deterministic table navigation and retargeted evidence.
- (decision) The conversation API returns a discriminated application-owned activity stream for planner_message, agent_response, clarification, draft, run_progress, comparison, approval_request, and terminal_outcome. The composer can create a message or explicit command but never encode approval as ordinary text submission. Prevents UI surfaces from collapsing authority stages.
- (decision) Cross-site or unauthorized resource lookup uses the same public not-found shape and timing budget as an absent resource where practical; browser errors never reveal resource existence, membership details, or policy internals. Prevents authorization side channels.
- (decision) Gate A is a dependency rule: PostgreSQL/site membership, immutable fixtures, normalized scenario-read service, authenticated read-only viewer, viewer-service parity tests, and negative mutation-path tests must pass before AgentRuntime or agent inspection tools are introduced. Prevents the agent from becoming a second source-data interpretation.
- (decision) Application configuration owns maximum agent iterations, model calls, tool calls, tokens, retries, wall time, per-site concurrent work, and solver duration. The model cannot raise limits; exhaustion persists a terminal status with a stable budget_exhausted or timed_out reason, emits an event, and performs no implicit retry. Prevents unbounded cost and incompatible failure semantics.
- (decision) Every audit/provenance envelope covers successful, denied, stale, failed, and cancelled consequential attempts and includes actor/site, request/conversation/agent-run/tool/approval/job/solver-run IDs, action and policy outcome, safe input/result hashes or summaries, before/after resource versions, model/prompt/tool/policy/application versions, and immutable evidence references; it excludes hidden reasoning. Prevents incomplete reconstruction.
- (decision) EvidenceSnapshot is a site-owned immutable aggregate referenced by scenario versions, solver runs, schedule versions, approvals, and audit records; no one workflow row owns its lifecycle. Prevents snapshots from disappearing or being overwritten when a schedule pointer changes.
- (decision) Cross-epic contracts are versioned ShiftMind-owned schemas in application/contracts: ScenarioProjectionV1, EvidenceRefV1, ProposalV1, RunSnapshotV1, ComparisonV1, ApprovalBindingV1, AuditEnvelopeV1, ActivityItemV1, PersistedEventV1, and ProblemDetailsV1. Adapters translate to them; schema fixtures and compatibility tests gate change. Prevents DTO reinvention.
- (decision) Canonical hashes use RFC 8785 JSON Canonicalization Scheme over UTF-8 contract payloads followed by SHA-256 with algorithm/version stored beside the digest. Secrets and display-only fields are excluded by the owning schema. Prevents incompatible approval, idempotency, and evidence hashes.
- (decision) Scheduling time uses a versioned horizon_start UTC instant, IANA site_timezone, and integer minute offsets in half-open intervals [start_minute,end_minute); adapters alone convert offsets to current engine float hours. Other instants are UTC timestamptz/RFC3339 and elapsed durations use explicit units. Prevents DST, rounding, and boundary disagreement.
- (decision) Persisted SSE uses PersistedEventV1 with stream_id, decimal sequence, event_type, occurred_at, resource_version, correlation IDs, and typed payload. Event ID is stream_uuid:sequence; Last-Event-ID must match the requested stream and replay returns sequence greater than cursor. Fifteen-second heartbeat comments carry no ID and are not persisted. Prevents cursor/envelope incompatibility.
- (decision) Aggregate owners are fixed: identity owns organization/site/user/membership/session; scenario owns fixtures and scenario versions; conversation owns messages and agent runs; scheduling owns proposals/runs/schedule versions/baseline pointer; workflow owns jobs/events; governance owns approvals/policy decisions; evidence owns snapshots/audit/provenance. Cross-owner changes occur only through an application orchestrator with named atomic bundles. Prevents two modules mutating one entity.
- (decision) Named atomic bundles are: accept-turn commits message+agent-run+event; enqueue-compute commits run snapshot+job+event; complete-compute commits terminal run+schedule version+evidence refs+event; request-approval commits binding+paused state+event; promote-baseline commits approval decision+baseline pointer+audit+event. No adapter or repository expands these bundles. Prevents partial choreography.
- (decision) PostgreSQL uses a migration owner separate from restricted API and worker runtime roles. Tenant tables carry site_id, enable and force RLS, and policies read transaction-local trusted actor/site context; pooled connections clear context and every transaction sets it. Worker leasing uses FOR UPDATE SKIP LOCKED plus fencing epoch. Prevents table-owner or pooled-connection RLS bypass.
- (decision) Successful audit idempotency is unique by site/effect_key/outcome inside the mutation transaction; denied/failed/cancelled attempt audit is unique by site/request_id/outcome in a reliable separate transaction. Prevents both missing and duplicate audit evidence.
- (decision) Evidence S3 keys are content-addressed/version-addressed. Bucket versioning is enabled; PutObject requires If-None-Match:* through bucket policy, and application roles have no DeleteObject. This is application-enforced create-only behavior, not regulatory WORM. Prevents overwrite while preserving honest claims.
- (decision) CloudFront disables caching for auth/API/SSE paths and forwards cookies, Origin, CSRF header, Last-Event-ID, and required methods. SSE emits 15-second heartbeats; CloudFront origin response and ALB idle timeouts exceed the heartbeat interval, compression/proxy buffering is disabled for event streams, and reconnect always replays persisted events. Prevents edge defaults from breaking BFF sessions or recovery.
- (decision) Deployments use expand-migrate-contract compatibility: additive schema first, API and worker versions N and N-1 remain compatible during rolling replacement, resumable backfills run before reads require new fields, and destructive contraction waits until old tasks are gone. App rollback targets the prior image only while schema compatible; otherwise restore procedure applies. Prevents mixed-version corruption.
- (decision) Brownfield cutover is one-way and non-ambiguous: existing SQLite scenarios/runs are legacy demo data and are not migrated into governed history. Gate A imports predefined fixture files into new checksummed PostgreSQL scenario versions, regenerates the OpenAPI client, and moves all governed UI routes to lowercase V1 states in one coordinated cutover. No adapter exposes both legacy and governed histories. Prevents mutually exclusive migration implementations.
- (decision) The MVP lifecycle has no in-product delete: authoritative PostgreSQL, audit, and S3 evidence remain until explicit environment teardown; RDS automated backups retain seven days and teardown creates a final snapshot; CloudWatch application logs retain thirty days; hosted Logfire follows its plan retention and is non-authoritative. Revisit before external data. Prevents undefined current retention while avoiding customer-policy claims.
- (decision) CloudWatch alarms cover API health/latency, queue age, worker lease expiry, solver failures/duration, agent budget exhaustion, tool/guardrail denials, approval age/outcomes, audit write failure, model failure, and telemetry export health. AWS Budgets and cost-allocation tags cover the portfolio environment. Prevents operational requirements from remaining prose only.
- (decision) Every evaluation report binds dataset, evaluator, model, prompt, tool, policy, application, scenario, and solver versions plus code/image revision. Prevents incomparable release evidence.
- (assumption) PydanticAI 2.14.1 is a planned seed, not installed reality; before the agent slice, a compatibility spike must lock it with Python 3.12/Pydantic 2.13.4 and prove provider adapters, deferred calls, deterministic models, durable message translation, and content-disabled instrumentation. A different V2 patch may replace it only with the same evidence.
- (decision) RunSnapshotV1 contains scenario checksum/version, baseline and proposal versions, locks/constraints/objectives, solver configuration/seed/time limit, component versions, accepted timestamp, and input evidence references. ComparisonV1 contains affected worker/shift/task, interval coverage, overtime, cost/objective components, constraint status, and unresolved infeasibility. Prevents FR-14/FR-15 implementations from preserving or comparing different facts.
- (decision) AgentRun, ScheduleRun, Job, and ApprovalRequest have distinct state machines. Approval-required exists only on AgentRun; cancellation-requested exists on ScheduleRun/Job; ApprovalRequest uses pending, approved, rejected, expired, stale, consumed. Prevents one run enum from producing impossible transitions.
- (decision) A feasible completed solver result creates an immutable candidate ScheduleVersion; infeasible, timed-out, cancelled, and failed results create evidence but no ScheduleVersion. Baseline promotion consumes an approval and moves the baseline pointer to the existing candidate; it never creates that candidate. Prevents approval/solver transaction contradiction.
- (decision) API session bootstrap uses a hardened SECURITY DEFINER auth.resolve_session function with fixed search_path and EXECUTE-only runtime permission; global worker leasing uses a separate EXECUTE-only workflow.lease_next_job function returning site/actor/job/fencing context. Domain access then occurs in a new RLS-scoped transaction. Prevents bootstrap deadlock without bypass roles.
- (decision) Runtime database roles are NOSUPERUSER NOBYPASSRLS and do not own tenant tables; the migration owner cannot be used by application tasks. Prevents FORCE RLS from being bypassed by role attributes or ownership.
- (decision) S3 and PostgreSQL do not share an atomic transaction: evidence bytes are conditionally written and checksum/version verified first; only then may one PostgreSQL transaction commit EvidenceSnapshot metadata, business mutation, and audit reference. A database failure leaves an unreferenced non-authoritative object retained until environment teardown; an S3 failure leaves no business mutation. Prevents impossible cross-store atomicity claims.
- (decision) Gate A uses a declared maintenance-window cutover before any governed durable work exists: disable legacy writes, drain or cancel the in-process worker, snapshot SQLite, import fixtures to PostgreSQL, deploy/test V1 API and generated client, then switch the no-cache SPA index. Post-Gate-A rolling N/N-1 rules do not apply retroactively. Prevents data loss and split UI/API history.
- (decision) After Gate A every durable job records contract_version and required capability version. N and N-1 API/worker/client compatibility is release-tested; incompatible capability activation waits until all tasks are N. CloudFront keeps content-hashed prior assets, serves index.html no-cache, and API metadata publishes supported client versions. Prevents rolling deploy from stranding work or browsers.
- (decision) Audit attempts use a server-generated attempt_id. An idempotent replay returns the same attempt/audit; a deliberate new attempt receives a new ID. Non-success audit uniqueness is site_id plus attempt_id, not request_id plus outcome. Prevents distinct attempts from collapsing.
- (decision) Evidence navigation persists target EvidenceRefV1 plus source activity ID in the URL/history state; exact-target loading never retargets, and returning restores route, scroll, and focus to the triggering control. Unauthorized targets use non-disclosing not-found behavior. Prevents broken UX inheritance.
- (decision) Agent adapters discard provider hidden-reasoning/thinking parts and persist only planner-visible messages, application-owned concise decision summaries, typed calls/results required for recovery, and evidence links. Neither provenance nor durable history stores private chain-of-thought. Prevents reasoning privacy drift.
- (decision) All public hops use HTTPS with TLS 1.2 or newer; CloudFront-to-ALB is HTTPS, RDS requires TLS, S3/RDS/logs/secrets use encryption at rest, S3 Block Public Access and CloudFront OAC protect buckets, and secret values never enter Terraform state inputs where avoidable. Prevents epics choosing incompatible transport/storage security.
- (decision) CloudFront forwards API query strings as well as required headers/cookies and disables cache for dynamic routes. Because CloudFront has no documented generic buffering toggle, SSE acceptance is an end-to-end streamed-heartbeat/reconnect/replay test through CloudFront and ALB. Prevents unsupported configuration claims.
- (decision) Node 22.22.0 is a planned observed-local build target and must be committed in a toolchain version file/CI image before frontend implementation; Terraform 1.15.5 is the verified planned CLI seed and must be constrained in required_version. Prevents planned toolchains being mistaken for existing locks.
- (decision) Approval has no durable approved-but-unconsumed state: an approve decision revalidates and atomically transitions pending to consumed while moving the baseline pointer; failure rolls back to pending, while rejected, expired, and stale are terminal. Prevents orphan approval authority.
- (decision) Database ownership uses a NOLOGIN shiftmind_owner role; a deployment-only migrator may SET ROLE to it, but runtime credentials cannot. Internal auth session-index and global job-queue control tables are not direct tenant query surfaces and are accessible only through owner-held hardened security-definer functions with PUBLIC revoked and specific EXECUTE grants. All domain tenant tables FORCE RLS. Prevents incomplete privileged bootstrap.
- (decision) Normal deployment rollback never restores PostgreSQL. Before contraction, rollback is prior compatible images; after an irreversible contraction, remediation is roll-forward. Point-in-time restore is disaster recovery only, requires workflow pause and reconciliation, and may not be presented as no-loss release rollback. Prevents authoritative history rewind.
- (decision) Queued AgentRun and ScheduleRun may become timed_out when total elapsed budget expires or failed on pre-lease validation/infrastructure failure; queued cancellation becomes cancelled. Prevents accepted work from being stuck outside the closed graphs.
- (version) Version correction from live official registries on 2026-07-22: Node 24.18.0 is latest LTS and replaces the earlier planned 22.22.0 target; Terraform 1.15.8 replaces the earlier 1.15.5 seed. Both remain planned until toolchain/required_version pins and compatibility checks land.
- (event) Reviewer Gate converged: deterministic lint passed with zero findings; rubric, current-technology, and adversarial divergence reviewers passed after fixes.
- (event) spine finalized
