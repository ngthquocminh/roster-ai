---
scope: Epic 4: Exact Baseline Decision and Decision Record (Stories 4.1-4.3 focus)
purpose: Prevent Stories 4.1-4.3 from re-deciding consequential approval, promotion, audit, and recovery semantics at implementation/review
altitude: epic
updated: 2026-08-27T18:14
---

- (constraint) Parent spine ARCHITECTURE-SPINE.md (2026-07-22) is binding read-only; inherit AD-2, AD-6..AD-12, AD-16, AD-20, AD-22 verbatim; never renumber or override parent AD ids; conflicts are surfaced, not resolved locally.
- (constraint) Non-goals: no production worker runtime composition/hosting (Epic 5/6); no redesign of global spine; no silent resolution of unrelated deferred-work items; no code or Epic 4 story-file edits in this run.
- (decision) [ADOPTED] No-baseline is an explicit valid state: first approval establishes the initial operational baseline; later approvals replace one exact named baseline. Binding must encode expected-absence (null baseline version means 'expects no baseline'), distinct from 'unknown'.
- (decision) [ADOPTED] actor_id = server-derived authenticated human principal for a user-initiated command; never a worker identity; never supplied by browser or model input.
- (decision) [ADOPTED] Audit/provenance distinguishes three roles: initiating planner, deciding planner, automated executor (worker/job attempt/fencing info). Executor is never an app_user actor_id.
- (decision) [ADOPTED] Approval is a persisted workflow: request -> persist exact immutable binding + approval_required -> reconnect/replay restores it -> explicit approve/reject/expiry/stale outcome -> literal persisted terminal/result state.
- (decision) [ADOPTED] Rejection or expiry of an approval produces agent_cancelled with explicit reasons (approval_rejected, approval_expired); 'such as' wording permits approval_stale in the same family.
- (decision) [ADOPTED] No general user-initiated AgentRun cancellation is added by Epic 4.
- (decision) [ADOPTED] Approval consumption, baseline movement, authoritative audit, and persisted event are one atomic, idempotent transaction (parent AD-10/AD-22).
- (decision) [ADOPTED] Every new contract or guard names a real production supplier, or explicitly defines a seeded proof plus the remaining production gap (Epic 3 retro closing commitment).
- (constraint) Brownfield facts verified live: agent_run status CHECK already contains approval_required and agent_cancelled but agent_run has NO reason column; persisted_event.actor_id is NOT NULL FK app_user; scenario_catalogue serves baseline_schedule_version as literal(None) - no pointer storage exists; no approval/audit table, no ApprovalBindingV1/AuditEnvelopeV1 module in backend/application/contracts/.
- (constraint) deferred-work.md items explicitly owned by Epic 4 and therefore in scope to decide: worker/human actor mixing on run event streams (story-3.5 review, 'Owner: Epic 4'); baseline-version start-validation supply (story-3.6, 'Owner: Epic 4 (AD-10; Story 4.3)'); comparison staleness vacuously false (story-3.8, 'Owner: Story 4.3'); audit:owned_by_epic_4 (story-3.9).
- (constraint) deferred-work.md 3.8/3.10 items NOT silently absorbed: baseline-side wage/selected-shift placeholders and empty get_baseline_assignments become WRONG the moment a non-empty baseline assignment supply exists; Epic 4 must guard, not wire, that supply.
- (decision) EAD-1 ownership: Governance owns ApprovalRequest rows (approval_request table, ApprovalBindingV1 states pending/consumed/rejected/expired/stale, at most one pending per agent_run); Scheduling owns the baseline pointer as a dedicated one-row-per-site site_baseline record (site_id unique, schedule_version_id FK, monotonic resource_version) - NOT a column on identity-owned site; Evidence owns audit_event append-only per AD-12 uniqueness. Prevents two owners of one entity and a pointer landing on the identity aggregate.
- (decision) EAD-2 identity/attribution: every consequential audit envelope carries initiated_by_actor_id and decided_by_actor_id (both server-derived human principals; equal in the one-user MVP but structurally distinct and proven distinct with seeded actors) plus executor attribution as worker facts (lease_owner, attempt_id, fencing_epoch) in the envelope/payload - no synthetic system app_user row. Worker-driven persisted_event.actor_id = the initiating human principal of the command that enqueued the work (the run requester per Story 3.6), executor detail in payload. Decides the Epic-4-owned story-3.5 ledger item.
- (decision) EAD-3 lifecycle coupling: a terminal non-consumed ApprovalRequest outcome (rejected/expired/stale) terminalizes the paused AgentRun as agent_cancelled with matching literal reason (approval_rejected/approval_expired/approval_stale) in the same transaction; approve records the decision and resumes agent_running within the promotion transaction. agent_run gains a nullable status_reason column (additive migration) with a closed reason vocabulary so reconnect renders the literal outcome without event replay.
- (decision) EAD-4 expiry is lazily materialized: expires_at is compared in-transaction at any decision/validation touch; an overdue pending row is semantically expired and the touch persists the terminal expired state + cancellation bundle. No background sweeper in Epic 4 (no production worker composition exists).
- (decision) EAD-5 idempotency/atomicity boundaries: request-approval, decide-approve (promotion), decide-reject each one transaction; HTTP commands idempotent per AD-8 (actor/site/operation/body-hash + expected versions); promotion effect key = approval_id; audit uniqueness (site,effect_key,outcome) makes double promotion impossible; replay returns original semantic result; a decision against an already-terminal binding fails with literal expected/current context.
- (decision) EAD-6 baseline-supply consistency guard: Epic 4 moves the pointer (metadata) but does NOT wire get_baseline_assignments to it; any comparison whose frozen baseline version is non-null while the baseline assignment supply for that version is not authoritatively readable must fail closed with a distinct outcome - never render an empty read as 'baseline is empty / all assignments net-new'. Guards the 3.8/3.10 wage/selected-shift/empty-supply deferrals from silently going wrong at first promotion.
- (question) Surfaced to parent spine owner: AD-22's fixed promote-baseline bundle needs the agent-run decision-recorded transition inside the same transaction, and Epic 4 needs a decide-rejection bundle AD-22 does not enumerate; recommend parent amendment, epic proceeds with extended sets as the epic contract.
- (question) persisted_event stream CHECK ties stream_id to conversation or schedule_run; approval lifecycle events are decided to ride the conversation stream anchored to the paused agent_run - the exact CHECK arms must be verified at Story 4.3 creation before the promotion event shape is frozen.
- (question) Approval expiry duration value and whether a config change bumps policy_version: supplier is Settings (env-validated at start); value to be fixed at Story 4.1 creation.
- (assumption) policy_version supplier remains the server-owned constant one-user-mvp-v1 (Story 2.5) with the documented production gap (database-backed policy supplier at second user / security review).
- (direction) Deliverables: epic spine + consequential-workflow ADR note in the same workspace; audience product/architecture/implementation/test owners; Fast path with [ASSUMPTION] tags.
- (event) Reconcile pass: brief requires explicit ownership/transition statements for ScheduleRun (no new transitions; promotion reads feasibility, never re-creates a candidate) and persisted event (workflow-owned, conversation stream) - to be added with reviewer fixes.
- (event) Reviewer (versions) verified all 8 brownfield claims true. One finding: persisted_event's stream-owner CHECK only constrains conversation_id/schedule_run_id, not agent_run_id - the conversation-stream convention IS compatible today; close open-question #2 at Finalize instead of deferring to Story 4.3 creation.
- (event) Reviewer (adversary) found real gaps: (1) site_baseline table-creation ownership contradicts itself - EAD-1/2 need 4.1 to read baseline_version at request time but EAD-9 names 4.3 as table creator; (2) no story owns TX3 decide-reject/expire/stale bundle - 4.2's map row omits EAD-6, 4.3's ACs are approve-only; (3) pending->stale vs pending->pending fork has no single named owner; (4) EAD-7 render-time materialization is self-contradictory (side-effecting read vs pure read); (5) AD-8 dual idempotency shapes (HTTP body-hash vs tool/worker agent_run_id/tool_call_id) collide with 4.1's dual planner-or-agent initiator; (6) minor: EAD-8 doesn't name 4.2 as consumer; nobody named to finalize resumed AgentRun after approve; post-resume worker-event actor attribution unstated.
- (event) Reviewer (rubric) verdict: sound spine, approve after revision. F1 HIGH: AD-22 bundle widening (TX1 adds audit vs parent's binding+pause+event) not surfaced as conflict alongside the other two. F2 HIGH: consequence-summary text has no decided home across 4.1/4.2/4.4 (ApprovalBindingV1 minimum is hash-only). F3 MEDIUM confirms adversary #5 (request-approval effect_key unnamed; reject/expire/stale reuse of approval_id is actually safe via outcome disambiguation). F4 MEDIUM confirms adversary #4 (EAD-7 unenforceable, no write-on-read pattern in codebase, overdue-untouched limbo has no place in closed vocabulary). F5 LOW: AD-3/14/15 cited in Story map but absent from Inherited table/frontmatter binds.
- (constraint) Brownfield fact found during reviewer-fix pass (load-bearing, missed in draft): execute_turn.py:107-128 maps adapter 'suspended' -> agent_cancelled with reason approval_unsupported as an explicit STOPGAP naming Epic 4 (AD-10, Stories 4.1-4.3) as the owner that must restore the approval_required mapping together with a persisted pending-call payload; claim_queued_run only claims agent_queued; test_request_path_grants_no_approval_capability_in_this_milestone fails the moment an approval-policy capability becomes reachable, which reopens the decision.
- (constraint) Brownfield fact: the resume seam ALREADY EXISTS in owned contracts - AgentApprovalPendingV1 (pending_calls + resumable owned turn history) is returned to the application rather than resolved in-process, and AgentTurnRequestV1.approvals carries server-owned AgentApprovalDecisionV1 supplied from a persisted approval record, never model output. Epic 4 persists the pending payload and feeds the decision back through a new turn; no new resume mechanism is invented.
- (decision) EAD-10 (fix, adversary F2/F3): Story 4.2 owns the single decide endpoint and the whole TX3 decide-reject/expire/stale bundle; Story 4.3 owns TX2 promotion only, invoked as the approve branch of that same endpoint. Revalidation fork is fixed: any BUSINESS mismatch (candidate/baseline/hash/membership/policy/expiry) terminalizes to stale; only a transactional/infra write fault rolls back to pending.
- (decision) EAD-1 fix (adversary F1): Story 4.1 owns the migration creating approval_request AND site_baseline, because 4.1 must read the pointer (or its absence) to populate the binding before 4.3 ever writes it. EAD-9 supplier row corrected.
- (decision) EAD-7 rewrite (adversary F4, rubric F4): reads are PURE - no GET, render, or replay may write. An overdue pending binding is presented as expired by comparison; the terminal expired state materializes only inside a decision-attempt transaction via TX3. Removes the write-on-read pattern the codebase does not have.
- (decision) EAD-6 fix (adversary F5, rubric F3): effect_key is the server-generated approval_id for ALL THREE bundles (generated at TX1 start), disambiguated by a closed outcome vocabulary approval_requested|approval_consumed|approval_rejected|approval_expired|approval_stale, satisfying AD-12's (site_id,effect_key,outcome) uniqueness.
- (decision) EAD-3 fix (adversary F6): dual initiator resolved - planner-initiated request-approval is an HTTP command keyed by AD-8 body-hash; agent-initiated is a typed capability tool call keyed by (agent_run_id, tool_call_id). Both write the same approval_request row through one use case; the table carries a single opaque request_effect_key column accommodating either shape. Epic 4's inherited AD-8 row no longer narrows the parent.
- (decision) EAD-11 (rubric F2): the consequence-summary TEXT is an application-owned literal snapshot persisted on the approval_request row beside its canonical hash - not recomputed at render, not read from ProposalV1, never model prose. 4.2 renders it, 4.4 replays it, both from the same immutable row.
- (constraint) Surfaced conflict #3 to parent owner (rubric F1): AD-22's request-approval bundle is 'binding + agent pause + event' with no audit; EAD-6's TX1 adds an audit write, which AD-12 requires for a consequential attempt. Widening surfaced, not silently taken.
- (event) All reviewer findings applied: EAD-1 migration ownership to 4.1; EAD-3 dual key shapes + post-resume attribution; EAD-5 resume via existing AgentApprovalDecisionV1 seam; EAD-6 effect_key/outcome vocabulary; EAD-7 rewritten pure-read; EAD-8 binds 4.2/4.4; new EAD-10 (endpoint/bundle ownership + revalidation fork) and EAD-11 (consequence-summary home); AD-3/14/15 added to Inherited table and binds; three AD-22 widenings surfaced; persisted_event CHECK question closed. Spine status set final.
- (event) spine finalized
- (decision) EAD-12 [ADOPTED by Minh]: snapshot-time vs decide-time rule. Settings consulted once at TX1 and snapshotted into the binding (expiry duration -> absolute expires_at, hashes, versions) are immune and never bump policy_version, following the existing session_ttl_s precedent in api/routers/auth.py:148. Only decide-time revalidation inputs version the policy, enumerated in one frozen PolicyInputsV1 (today the capability feature flags); policy_version is DERIVED as POLICY_GENERATION + truncated SHA-256 over RFC 8785 canonical JSON via application/contracts/canonical.py, never hand-typed. Rejected hashing all of Settings (a CORS edit would invalidate every pending approval) and rejected a hand-bumped constant (the retro's 'guard that cannot go red' pattern).
- (event) Parent AD-22 amendment APPROVED by Minh and applied to architecture-ShiftMind-2026-07-22/ARCHITECTURE-SPINE.md: three bundles widened, AD id unchanged, prior 2026-07-22 wording preserved in an Amendment note, parent updated: bumped to 2026-08-27, parent memlog appended. Epic 4 Open Questions reduced to one product number (expiry duration value).
