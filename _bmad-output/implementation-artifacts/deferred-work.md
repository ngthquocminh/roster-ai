# Deferred Work

## Deferred from: correct-course of story-2-7 grounding mechanism (2026-08-13)

Surfaced while amending Story 2.7's Decision 2 (`sprint-change-proposal-2026-08-13.md`). None of these is caused by that amendment, and none is fixed by it. They are recorded together because they are one gap seen from three sides: **the harness evaluates the plumbing, not the model.** Story 2.7's grounding gate is a structural invariant — every rendered number is application-computed, from an immutable version, with a locator — and it is enforced per request. Whether the model chose the *right* metric for the question asked, and whether its prose fairly characterises the number beside it, are statistical properties that only an evaluation layer can measure. That layer has targets but no instrument.

- **The live-model evaluation path is declared and entirely unimplemented.** `RunSource = Literal["double", "live"]` [backend/evals/evaluators.py:12] and `EvalVerdict.authoritative` already encodes NFR26's "never the sole release evidence" by admitting only `"double"`. But the string `"live"` appears **nowhere else in `backend/evals/`** — there is no live runner, no gating, no budget. The seam was cut and never used, so no evidence in this repository has ever been produced by a real model. **Owner/revisit trigger:** the first story that needs a claim about real-model behaviour rather than harness behaviour — realistically Gate B, where NFR28's routing floor is meant to be reported.

- **NFR28's "≥90% overall tool routing" currently describes the dataset, not the model.** Every golden case drives a deterministic double whose turns are *authored* (`ScriptedModelTurn`, [backend/evals/cases.py:33-43]), so the routing a case exercises is the routing its writer wrote down. `ToolRoutingEvaluator` then asserts exact equality against that same authored expectation [backend/evals/evaluators.py:38-93]. This is sound regression coverage and is exactly what NFR26 asks normal CI to be — but a ≥90% pass rate computed this way is a property of the case files, and reporting it as a model-quality measure would overclaim. **Owner/revisit trigger:** shares an owner with the live-run entry above; whoever implements the live path must also state which of NFR28's two numbers each run source is entitled to report.

- ~~**No evaluator assesses whether visible prose is faithful to the computed claims.**~~ **CLOSED 2026-08-15 (Story 2.9):** `PolicyOutcomeEvaluator` now judges allow/refuse/clarify against the owned outcome variant, and the report combines that verdict with exact `expected_visible_state` and `expected_visible_text` comparisons derived through the same `outcome_visible_text()` function used by the product. A visible-state or visible-copy drift now makes the generated report fail rather than leaving either required `GoldenCase` field unread.

## Deferred from: code review of story-2-6-add-and-remove-a-governed-capability-module (2026-08-12)

- ~~**A capability timeout is reported to the model as a correctable argument error and re-driven.**~~ **CLOSED 2026-08-13 (Story 2.7):** inspection overruns now raise the manifest-declared, non-retryable `inspection_timeout`; both capability base classes carry generic non-retryable codes, and invalid arguments use dedicated retryable subclasses. Focused conformance proves the vocabularies and retry sets remain aligned.
- **`remaining_budget` is a per-run snapshot that is never decremented.** Both governed handlers precheck `deps.remaining_budget.tool_calls_limit` [backend/application/capabilities/deps.py:29], which is the configured ceiling from `AgentBudgetV1`, not a live counter. The declared `budget_exhausted` error is therefore unreachable through actual consumption — it can only fire if an operator configures a non-positive limit — and repeated tool calls in one run each observe the initial budget. Pre-existing from Story 2.5. **Owner/revisit trigger:** the first story that persists per-run tool-call accounting (Epic 3's durable job/lease work).
- **Grant predicates are self-declared by the governed module with no registry-side allow-list.** `required_role` and `required_feature_policy` are plain `str` fields the module writes about itself [backend/application/capabilities/module.py:17-18]; `validate_manifest` never sees them, and `PLANNER_ROLE` is now exported but absent from control flow. A module shipping `required_role="viewer"`, or reusing another module's policy string, is granted accordingly with nothing able to notice. This is Decision 7 as specified, not a deviation — recorded because the reduction is invisible in the code. **Owner/revisit trigger:** shares an owner with the constant role/feature-policy supplier entry below; add a recognized-role/policy allow-list when a real supplier lands.
- **Reserved and framework tool-name collisions are unchecked at registration.** `_register_module` adds a tool under `manifest.capability_name` with no check against names already on the toolset or reserved by the framework (e.g. `final_result`) [backend/agent/capability_tools.py:61-68]. A colliding governed capability would shadow a framework tool silently. **Owner/revisit trigger:** the first story adding a third capability, or any story introducing output tools.

## Deferred from: code review of story-2-5-inspect-scenario-facts-through-a-governed-capability (2026-08-11)

- **`frontend/playwright.config.ts` breaks the story's mandated zero-line `frontend/` fence.** Decision 1 required "no frontend change whatsoever" and Task 10 ticked the fence as verified, but the branch adds `workers: 4` [frontend/playwright.config.ts:6-11]. The change is test-infrastructure only — `frontend/openapi.json` and `frontend/src/api/schema.d.ts` are both genuinely clean, and all seven backend fences verified empty — and the Completion Notes disclose it. Two residual concerns: the worker cap is hardcoded with no CI branch (unlike the adjacent `retries: process.env.CI ? 2 : 0`), justified in-comment by one developer's 16-thread Windows host and now pinned for every future runner regardless of core count; and it alters the conditions under which the Gate A `playwright.xml` (46 cases) was measured. **Owner/revisit trigger:** the first story that creates `.github/` and a CI workflow — at that point make the worker count CI-aware rather than fixed.

## Deferred from: story-2.5 implementation (2026-08-11)

- **Trusted role and feature policy are server-owned constants in the one-user MVP.** Story 2.5 introduced the typed `CapabilityGrantContextV1` seam and branches on role, site, feature policy, and conversation scope, but `planner`, `scheduling_inspect_enabled`, and policy version `one-user-mvp-v1` have no database-backed supplier. This is deliberate: the membership model has one active member and no role column. **Owner/revisit trigger:** the first story activating a second user, or a customer security review. Replace the supplier without changing the registry shape.
- ~~**Story 2.5 is the first intentional break of the `backend/agent/**` zero-line-diff fence.** It modifies `backend/agent/runtime.py` and adds `backend/agent/capability_tools.py` to render application-granted capabilities with trusted `AgentDepsV1`. `backend/agent/translate.py` remains untouched. **Owner/revisit trigger:** Story 2.6's add/remove conformance work should treat the new runtime rendering seam as the baseline rather than assuming the old fence still stands.~~ **CLOSED 2026-08-12 (Story 2.6):** the generic `CapabilityModuleV1` renderer and add/remove conformance suite now define and mechanically protect that baseline.

## Deferred from: code review of story-1-1-establish-governed-fixture-history (2026-07-24)

- `canonicalize_json`'s recursive `_serialize` has no depth guard; a pathologically deep payload raises an uncaught `RecursionError` [backend/adapters/postgres/canonical_json.py:63-83]. Only reachable today via trusted, shallow fixture files — worth bounding if this function is ever fed untrusted input.
- `PostgresFixtureHistoryAdapter` is built on a synchronous SQLAlchemy engine with no async/thread-offload [backend/adapters/postgres/fixture_history.py:44-53]. Not a problem while only called from a standalone script; the next story that calls it from a live FastAPI request handler must route it through a worker thread to avoid blocking the event loop.

  **Still open, annotated 2026-08-11 (story-2-4 implementation).** Story 2.4 adopted this rule for its own long-lived path — every SSE poll runs its synchronous SQLAlchemy work through `fastapi.concurrency.run_in_threadpool` (`api/routers/conversations.py`), matching `api/deps.py:get_session` and `api/main.py`'s session middleware. **That does not close this item**: the adapter named here is still reached only from `scripts/` and tests, so the condition the item is waiting on — a live request handler calling *`PostgresFixtureHistoryAdapter`* — has not occurred. Left open deliberately so a future story does not read "the principle was applied somewhere" as "this adapter was fixed".
- `site` table's RLS policy (`site.id = app.site_id`) is confirmed correct as designed, not a defect — but Story 1.2's real runtime session flow must resolve `site_id` via `auth.resolve_session()` before any RLS-scoped domain transaction, never by querying organization+name (that lookup-by-name path is a privileged, Gate-A-only migrator bootstrap operation, exempt from RLS by nature). **Story 1.2 action item: add a test proving a Site A runtime session cannot enumerate Site B, even within the same organization.**

## Deferred from: code review of story-1-2-sign-in-to-the-seeded-site-safely (2026-07-26)

- No cleanup/retention job for expired `auth.login_handshake` / `auth.session_index` rows — both tables grow unbounded over time [backend/migrations/versions/5e2a4c9d1f70_add_seeded_site_identity.py]. Operational housekeeping reasonably arrives with Epic 3's job-queue infrastructure; no acceptance criterion requires it now.
- No `X-Frame-Options` / CSP `frame-ancestors` header on the new sign-in surface [backend/api/main.py]. Clickjacking hardening is an app-wide/infra concern better addressed holistically (e.g. at a reverse proxy or in a dedicated hardening pass) rather than piecemeal in this identity story.

## Deferred from: story-1-3-choose-an-immutable-fixture (2026-07-27)

- The legacy route components were retired, leaving the intentionally orphaned `frontend/src/components/{editor,runs,results,scenarios}/**` trees and their hooks: `useApplyConstraint`, `useCreateScenario`, `useFixtures`, `useOverrides`, `useRun`, `useRunInsights`, `useRunResult`, `useRuns`, `useScenario`, `useScenarios`, and `useTriggerRun`. Keep them unreachable; Story 1.9's mutation-path audit and a later cleanup should verify and remove this inventory without re-mounting it.
- **Three shared modules are transitively orphaned by the same cutover but live OUTSIDE those four directories**, so an audit scoped to the list above will miss them. Every remaining non-test importer of each is itself inside the orphaned tree: `frontend/src/components/layout/ErrorBanner.tsx` (OverridesList, ProviderDownBanner, ScenarioHeader, RunHistoryTable, ScenarioTable, InsightPanel, WarningsBanner), `frontend/src/lib/runStatus.ts` (RunInFlightPanel, RunStatusLabel, `useRuns`), and `frontend/src/lib/formatShiftWindow.ts` (CoverageByDayTable, ScheduleTable). Note `ErrorBanner.tsx` sits in `components/layout/` beside components that are very much live (`AppBar`, `RootErrorBoundary`), so it must not be swept by directory alone — check importers before removing.

## Deferred from: code review of story-1-3-choose-an-immutable-fixture (2026-07-27) — backend chunk

> **Two entries below were closed on 2026-07-29** rather than carried forward — the fixture version ordering and the missing generic `Exception` handler. Both are recorded as resolved in the story's Review Findings section; the remaining entries are still open.

- **Remaining caveat from the version-ordering fix (open):** ordering now keys on the digits inside the version string read as a number, which is correct for the `v<N>` format in use but concatenates multi-part versions — `"1.2"` vs `"11"` compares 12 vs 11 and is wrong. Introducing dotted or semver-style versions means revisiting `_VERSION_ORDINAL` in `backend/adapters/postgres/scenario_catalogue.py`, or constraining the column format (which needs a migration).
- `TestClient(app)`'s lifespan calls `get_settings()` directly instead of consulting `app.dependency_overrides` [backend/api/main.py:42]. Test fixtures that override `db_path` and `maintenance_flag_path` are therefore inert at startup: `db.init_db` initializes the developer's real SQLite file and `_gate_a_flag_is_set()` reads the ambient flag path. The overrides read as isolation but provide none.
- `imported_at` is never normalized to UTC before serialization [backend/adapters/postgres/scenario_catalogue.py:42]. `TIMESTAMPTZ` comes back in the Postgres session's timezone, so the published contract can shift from `2026-07-24T00:00:00Z` to `2026-07-24T02:00:00+02:00` on server configuration alone. The contract test cannot catch it — it feeds a hardcoded UTC value through a stub reader.
- The catalogue ordering assertion compares Postgres collation order against Python codepoint order [backend/tests/test_postgres_integration.py:198-208]. It agrees today only because the seeded fixture ids differ on their first letter; ids differing in punctuation or case would diverge, and the SQL pins no collation.
- Two undiscussed response shapes on `GET /api/v1/scenarios/{scenario_id}` [backend/adapters/postgres/scenario_catalogue.py:19-26]: the inner join makes a `scenario` row with zero `scenario_version` rows return 404 despite existing and being visible to the caller's site, and a malformed non-UUID id returns 422 `invalid_request` — a third class alongside the specified 401/404. Neither discloses site membership, so AD-3 non-disclosure still holds.

## Deferred from: code review of story-1.4 (2026-08-05)

- Pagination cursor isn't pinned to a `scenario_version`. Every paginated request (`get_tasks`/`get_workers`/`get_demand`/`get_constraints`) independently re-resolves "latest version"; the cursor is a bare integer offset carrying no version identity. If a new `scenario_version` is imported between two paginated requests for the same `scenario_id`, the underlying list being sliced changes silently — pages can skip, duplicate, or reorder records with no error surfaced. AD-4 exists specifically to prevent this class of "fact drift." [backend/adapters/postgres/scenario_projection.py:329-350,433-451] — resolved as a decision-needed finding on 2026-08-05: deferred, accepted as a known limitation. Fixture imports are a manual, governed operation (Story 1.1) with low real-world odds of racing a user's paginated browsing session; revisit (cursor pinned to `scenario_version_id`, rejecting a later page that no longer matches) if/when import cadence or concurrent-editing use cases change.
- No pre-normalization type/shape validation on raw fixture rows in a few spots — `_horizon` indexes `row["PeriodStartDate"]`/`row["PeriodEndDate"]` with no guard, and the `Inbound Workload` nested `Tasks[]` loop assumes dict-shaped entries. A malformed row today raises, but `api/main.py`'s global `Exception` handler already converts that into a proper RFC 7807 500 with no data leakage, and the raw fixture is governed/validated at import (Story 1.1). [backend/adapters/postgres/scenario_projection.py:88-95,237-243] — pre-existing safe failure mode, not reachable via governed fixture import.
- `_horizon` doesn't validate `end >= start` or that exactly one `Scenario Range` row exists — a malformed row wouldn't raise, it would silently produce a wrong/negative `horizon_minutes`. [backend/adapters/postgres/scenario_projection.py:88-95] — not reachable via governed fixture import.
- `_as_utc` doesn't special-case a DST fold/gap instant, and `_minute_offset` truncates toward zero rather than flooring. Fixture timestamps are minute-granular and the current Gate A fixture date ranges (early June, Australia/Sydney winter) never cross a DST boundary, so neither path is exercised today. [backend/adapters/postgres/scenario_projection.py:61-66] — unreachable with current fixtures, revisit if a DST-crossing scenario is ever imported.
- A zero-duration `Indirect Workforce Requirement` window (`start == end`) falls into the `+24h` wrap branch instead of staying zero-length, since the wrap condition is `end_hours <= start_hours`. No such row exists in either committed Gate A fixture. [backend/adapters/postgres/scenario_projection.py:79-81] — unreachable with current fixtures.
- `area_id` defaults to `""` while the paired `area_name` lookup defaults to `"Unknown"` via a separate dict miss — an inconsistent placeholder pair if a `Task` row is ever missing `AreaID`. Not reachable in either Gate A fixture. [backend/adapters/postgres/scenario_projection.py:124,134] — unreachable with current fixtures.
- `TaskID`/`ContactID` values are compared against `set[str]` membership (`valid_task_ids`) without an explicit `str()` coercion at four call sites; a `TaskID` typed as a JSON number in one table but a string in another would silently fail the membership check. `TaskID` is a GUID string in every relevant table across both Gate A fixtures, and the exact same non-coercing pattern already exists in `ingest/input_adapter.py`. [backend/adapters/postgres/scenario_projection.py:150-160,209-264] — pre-existing convention, no reachable trigger given the GUID id format.
- `TeamMemberTaskRateOverride`/`DefaultTaskRate` values that fail `float()` coercion silently become `0.0` via `_to_float`, indistinguishable from a legitimate explicit zero-rate disqualification. Same pattern already exists in `input_adapter.py`; neither Gate A fixture has a non-numeric rate field. [backend/adapters/postgres/scenario_projection.py:156-159] — pre-existing pattern, unreachable with current fixtures.
- Broad `except (KeyError, TypeError, ValueError): continue` in the worker-window and demand-datetime parsing loops drops a malformed row with no logging or counter — a data-quality signal is lost silently. [backend/adapters/postgres/scenario_projection.py:174-178,213-217,232-236,265-273] — safe-but-silent failure mode, revisit if fixture provenance ever becomes untrusted.

## Deferred from: code review of story-1.5 (2026-08-05)

- Unauthorized-case non-disclosure test coverage exists for only one of the six evidence groups (`work-areas-and-tasks`), and only via the fixture-backed `_ProjectionReader`, not a real cross-site Postgres scenario for any of the five new resolve endpoints beyond it. [backend/tests/test_scenario_projection.py:779] — deferred, optional hardening; the story's own Task 5 wording explicitly permits either a postgres-marked or fixture-backed test for this check, so this isn't a violation, just unexercised breadth.
- `_resolve_items` returns the first matching candidate via `next(...)` with no detection of duplicate `record_id`s among normalized items for a given group. [backend/adapters/postgres/scenario_projection.py:331] — pre-existing assumption inherited from the same `_normalize_tasks`/`_normalize_workers`/`_normalize_demand`/`_normalize_constraints` functions Story 1.4's paginated endpoints already rely on for uniqueness; not newly introduced by this story's resolve path.

## Deferred from: code review of story-1-6-establish-shiftmind-design-tokens-and-shared-primitives (2026-08-06)

- **Premise corrected by Story 2.8 (2026-08-15); remains open under the design-system owner.** No contrast test for `EvidenceLink` (`evidence-link`) rendered on `EvidenceHighlight`'s `evidence-surface` background [frontend/src/index.test.ts] — only the white-background case is verified. Story 2.8 does not compose those primitives: the link remains in Chat's reading column, while the highlight contains the resolved record and a Return to claim button. Revisit only if a future design actually places an EvidenceLink on `evidence-surface`.
- `EmptyState`'s `action` prop has no touch-target enforcement [frontend/src/components/primitives/EmptyState.tsx] — nothing in the component guarantees a caller's action carries `min-h-11`; every current call site (fixtures, `FixtureCatalogueView`) supplies it manually.
- **Closed by Story 2.8 (2026-08-15).** `EvidenceLink` activation props are now a discriminated union: a link requires `href`, and a button requires `onActivate`, so the neither-prop state is a TypeScript error rather than an inert runtime control. [frontend/src/components/primitives/EvidenceLink.tsx]
- `InlineAlert`'s title-only / action-without-description render path is untested [frontend/src/components/primitives/InlineAlert.tsx:31-36] — every current fixture and call site supplies both `title` and `description`.
- `FixtureCatalogueView.test.tsx`'s empty-state assertion reaches into `EmptyState`'s private DOM structure via `explanation.parentElement` classes [frontend/src/features/fixture-catalogue/FixtureCatalogueView.test.tsx] — couples an unrelated consumer test to an implementation detail rather than `EmptyState`'s public contract.
- **Premise corrected by Story 2.8 (2026-08-15); remains open under the Chat/reading-surface owner.** `EvidenceLink`'s label has no truncation handling for long `group`/`record`/`fieldOrRange` values [frontend/src/components/primitives/EvidenceLink.tsx]. Story 2.8 places the link in Chat's wrapping reading column and renders the target as a separate record region, never inside the dense Scenario Data grid. Revisit if real locator lengths overflow the Chat reading surface; no speculative truncation was added here.
- `ReconnectBanner`'s `COPY[state]` lookup has no runtime guard for a `state` outside its 3-member union [frontend/src/components/primitives/ReconnectBanner.tsx:20-21] — type-safe today with no runtime caller; SSE integration (the first real caller) arrives Epic 2/3 per this story's own Dev Notes.

## Deferred from: code review of story-1-8-control-scenario-data-tables (2026-08-06)

- All six panels bypass the type system with `(controls?.queryParams ?? {}) as WorkerQuery`-style casts, and correctness depends on Radix `TabsContent` unmounting inactive children (undocumented assumption, no runtime guard against a future `forceMount`) [frontend/src/features/scenario-data/groups/*.tsx]. Not a functional bug today — verified the lazy-mount behavior and the sort/filter vocabularies line up 1:1 across all six groups — but a fragile pattern worth a typed-controls refactor.
- Demand group's backend `sort` enum includes `end_minute`, but `columns.ts` only maps the `window` column to `sortKey: "start_minute"` — `end_minute` is unreachable from the UI even via a hand-edited URL [frontend/src/features/scenario-data/columns.ts:42]. Not a spec violation (Task 6 only requires column sortKeys ⊆ backend sort table, not the reverse); candidate UX affordance for a later story.
- An explicit empty-string filter query param (e.g. a hand-typed `?task_id=`) is treated as an exact match on `""` (zero results) rather than being ignored, because `_present_filters` only strips `None` [backend/api/routers/scenario_projection.py:71-74]. Only reachable via manual URL editing — the app's own `applyFilters` never sends empty strings — and there's no spec requirement to special-case it.
- `start_minute_gte`/`end_minute_lte` accept negative integers with no `ge=0` bound, unlike `cursor`/`limit`'s explicit `Query(...)` bounds [backend/api/routers/scenario_projection.py:313-314]. Well-defined (if unusual) semantics; spec only requires the *type* to 422, not a range floor.

## Deferred from: code review of story-1.9 (2026-08-06)

- `evidence/story-1.9/gate-a-viewer-parity-and-mutation-denial.json`'s `code.git_commit` records the pre-commit parent hash (`e2b2038`), not the actual commit (`366bd4e`) containing the story's own changes [evidence/story-1.9/gate-a-viewer-parity-and-mutation-denial.json:55]. Pre-existing, repo-wide evidence convention — `evidence/story-1.4/nfr35-scenario-data-load.json` and `evidence/story-1.5/nfr35-evidence-target-resolution.json` show the identical off-by-one pattern (evidence generated before the commit it describes exists), not a defect newly introduced by this story.
- `legacyReachability.test.ts`'s regex-based import walker (`IMPORT_PATTERN`) cannot trace dynamic `import(...)` expressions, so a future legacy/mutation module reachable only via dynamic import would be silently reported unreachable [frontend/src/test/legacyReachability.test.ts:29]. No dynamic import exists anywhere in the frontend today, so nothing is exploitable now — but this is the sole gate for an irreversible bulk deletion and worth hardening before a future story leans on it again.

## Deferred from: story-1.11 (2026-08-07)

- **ARIA-snapshot regression lock → Story 4.9.** The Gate A accessibility tree is currently protected by axe sweeps and Playwright browser a11y checks only — the manual NVDA pass (formerly Story 1.11 Task 6) was descoped 2026-08-09 for this portfolio MVP (see `docs/GATE-A-RUNBOOK.md` and `EXPERIENCE.md`'s Accessibility Floor). None of these lock the tree against silent regression. Playwright 1.62.1 (already pinned) supports `expect(locator).toMatchAriaSnapshot()`, which would turn the accessibility tree into a permanent regression guard rather than a point-in-time observation. Assigned to **Story 4.9**, which owns the completed-journey WCAG proof — deliberately not added here, because Story 1.11's scope is evaluating the substrate, not extending its accessibility coverage.
- `evidence/story-1.11/`'s `git_commit` off-by-one entry from the story-1.9 review (above) is **closed** by this story: `backend/scripts/evidence_binding.py` refuses to resolve bindings against a dirty tree, and all four prior evidence files were re-measured and rebound on a clean tree. The rule is documented in `docs/EVIDENCE-CONVENTION.md` and enforced repo-wide by `backend/tests/test_evidence_convention.py`.

## Deferred from: code review of story-1.11 (2026-08-08)

- **Evidence files have no expiry.** `audit_evidence_file()` checks only that `code.git_commit` is a real object, an ancestor of HEAD, and touched at least one non-docs file [backend/scripts/evidence_binding.py:415-433]. None of those age. Reproduced: taking the real story-1.9 evidence, setting `git_commit` to `203db1bf` (*"Phase 2: FastAPI backend skeleton"*, 2026-06-26 — from before PostgreSQL, before Gate A, before anything story 1.9 measures) and `measurement_date` to `1999-01-01` returns `()` — zero violations, fully bound. Also note `_is_code_path()` accepts `uv.lock` and `.gitignore` as "code", so a lockfile-only commit satisfies the touches-code rule. Nothing is exploitable today (all five files bind `b11fe9d`, three commits from HEAD); the exposure is Epic 2–5, when these files sit still while the code beneath them moves. **Deferred deliberately to be decided with the audit-rule redesign** — and note that redesign *increases* this risk: the `schema_version == head` rule was acting as an accidental expiry, forcing regeneration on every migration, and making it monotone removes that. Candidate rule: require the commit to touch a path the check actually depends on (needs a `subject_paths` field on `GateACheck`).
  - **RE-FRAMED 2026-08-17 — the "no expiry" headline above states the problem wrongly, and it nearly sent Epic 3 down the wrong road.** Evidence is a *historical record*: "at commit X, measurement Y produced result Z". That proposition is true forever, and reproducing it means checking out X and re-running — nothing about it can expire. The monotone rule set in `audit_evidence_file()` is **correct by design**, not a hole; its own docstring [backend/scripts/evidence_binding.py:568-585] already separates *"was this evidence generated correctly?"* (verdict, monotone) from *"is this evidence still valid now?"* (drift, non-monotone). Story 1.11 collapsed those two questions into one rule and that was the defect it had to fix — demanding expiry here is a request to recreate it. Note also that the reproduce recorded above demonstrates something narrower than the headline claims: a **hand-edited** file passes the audit, but forgery was never in `audit_evidence_file()`'s threat model. The defence against forgery is "never hand-type an evidence file, always generate through `evidence_binding.py`" (`docs/EVIDENCE-CONVENTION.md`, swept repo-wide by `test_evidence_convention.py`).
  - **Where the real defect lives: the consumption site, not the storage site.** `gate_a_readiness._evidence_result()` [backend/scripts/gate_a_readiness.py:106-135] opens a stored evidence file, reads its `passed` field, and returns it as the verdict for a *present-tense* question ("does ShiftMind satisfy this invariant right now?"). That is a category error. Scope was **4 of 20 checks** — `normalized_projection_nfr35_latency` (1.4), `evidence_target_nfr35_latency` (1.5), `viewer_parity_evidence` (1.9), `accessibility_and_responsiveness` (1.10); the other 16 consume fresh JUnit XML and re-measure on every CI push. **As of 2026-08-18 it is 3 of 20**: `viewer_parity_evidence` was retired (see the CLOSED note below) and 17 checks now re-measure.
  - **Risk is not uniform across those four.** 1.4/1.5 are **low**: the 2-second threshold is asserted inside the measurement itself [backend/tests/test_postgres_integration.py:593], the test carries `@pytest.mark.postgres`, and the `backend-postgres` CI job runs it on every push — so a latency regression turns CI red regardless of what the evidence file says. Those files record *observed numbers on a reference machine*, which a shared CI runner cannot meaningfully produce; re-measurement is fully scripted (`regenerate_evidence.py --measurements`). **1.9 is the one that matters**: wholly static, no automated re-measurement path, and its guard is circular (see the next entry).
  - **CORRECTED 2026-08-18 (Story 3.1):** the former claim that this invariant "gates the mutation-denial invariant that Story 3.1 writes into" was false. Story 1.9 protects fixture source data under FR22/FR24. Story 3.1 touches neither `scenario` nor `scenario_version`; Story 2.3 had already introduced a governed write path without disturbing fixture parity. Story 3.1 instead adds the first mutating route outside `/api/v1/conversations`, protected by structural assertions over the proposal adapter and router. The P3 replacement of the circular check remains work **alongside** Epic 3, not a prerequisite or gate for Story 3.1.
  - **Direction:** turn 1.9's parity/mutation-denial into live CI tests and drop `evidence_path` from that check — do **not** build `subject_paths` machinery across all 20 checks. This remains work that runs *alongside* Epic 3.
  - **CLOSED 2026-08-18 (`chore/gate-a-p3-live-parity`).** All three parts done. (1) The invariant is now stated positively and dated in `docs/GATE-A-RUNBOOK.md` — "What 'read-only' means once drafts are writable" — with an approved-write-path table and the reason each does not touch governed data. (2) The circular third test in `test_gate_a_mutation_audit.py` is replaced by real denied write requests, parametrized over every mutating route the live OpenAPI document exposes: 401 no session, 403 no CSRF, 403 cross-origin. There is deliberately NO wrong-site request test — proving cross-site invisibility needs real RLS, and with the repository stubbed the assertion would only read its own stub, which is the circularity this item exists to remove; site scoping is asserted structurally instead, and behaviourally by `test_proposal_persistence.py` on PostgreSQL. (3) `viewer_parity_evidence` is retired; its slot is filled by `api_parity`, a live check pointing at `test_gate_a_projection_api_matches_every_contract_record_for_both_fixtures` [backend/tests/test_postgres_integration.py:840] — the one thing the evidence file claimed with no live check behind it. Evidence-backed checks: **4 → 3**, and the riskiest of the four is gone. The 1.9 evidence file is untouched and still on disk; it remains a true record of commit `e2ecdb6`, no longer consulted as a present-tense verdict. Deliberately NOT filed under `parity_tests`: the negative tests live in `backend_mutation_denial` / `negative_mutation_tests`, where `test_gate_a_mutation_audit.py` was already registered — putting mutation-denial proof in a parity bucket would have recreated the mis-filing defect recorded two entries below.
- `test_gate_a_mutation_audit.py`'s third test is circular: it asserts that `evidence/story-1.9/gate-a-viewer-parity-and-mutation-denial.json` contains the literal string `"passed"` — the same field the Gate A registry's `viewer_parity_evidence` check reads to decide that invariant. Its second test `ast.walk`s exactly two hardcoded adapter files for the substrings `INSERT`/`UPDATE`/`DELETE`, so it misses a third adapter, misses runtime-composed SQL, and would false-positive on a benign `SELECT updated_at` [backend/tests/test_gate_a_mutation_audit.py]. Pre-existing (Story 1.9); neither artifact issues a mutating HTTP request. Worth replacing with a real negative request before Epic 2 leans on the mutation-denial invariant.
  - **PARTIALLY CLOSED 2026-08-18 (`chore/gate-a-p3-live-parity`):** the third (circular) test is gone, replaced by real denied write requests — see the CLOSED note above. The **second** test (the `ast.walk` substring scan over two hardcoded adapters) is untouched and remains open; it is weak independently of Epic 3 and was deliberately left out of scope so that session stayed focused.
- The `authenticated_readonly_scenario_data` invariant's largest contributors are design-token and table-widget tests (`StatusBadge`, `EmptyState`, `ColumnChooser`, `FilterBar`, `columns.ts`, `filters.ts`), while `backend/tests/test_scenario_projection.py` — which holds the actual 401/403 assertions — is filed under `normalized_scenario_reads` [backend/scripts/gate_a_checks.py:220-280]. `validate_registry()` requires at least one check per invariant but never that a check is topically relevant, so this invariant would still roll up `passed` with every authorization test removed from its bucket. Not a defect in Story 1.11 — the spec's own coverage table (story lines 60-68) dictated the mapping — but the registry is the thing Epic 2+ will trust, and it should prove what it claims.
- `dataset` and `scenario` are documented as "derived live, none is a literal", but resolve through `default_fixtures()` [backend/scripts/gate_a_cutover.py:76-89], which is a hardcoded tuple of `FixtureSpec`s with literal `"v1"` version strings and no checksum of the underlying `data/sample_tiny_input*.json` bytes. Editing a fixture's contents without bumping the literal leaves the binding byte-identical. Importing rather than re-declaring was the spec's explicit instruction, so this is inherited, not introduced — but the fixture binding is weaker than the contract-digest binding beside it.
- The readiness gate cannot be run twice in a row: `gate_a_readiness.main()` writes into `evidence/`, which dirties the tree, so the next `resolve_bindings()` raises `DirtyTreeError` before doing any work. The documented escape (`--allow-dirty`) marks every test-backed check unbound and blocks the gate, leaving the unvalidated `bindings=` parameter as the only ergonomic path — which is also the parameter that bypasses the dirty-tree refusal entirely. Worth a `--force`/stash-aware flow before the Epic 5 release gate reuses this module.
- `requires_git` [backend/tests/test_evidence_convention.py:31-42] silently skips `test_every_recorded_commit_is_a_real_ancestor_that_touched_code` and `test_evidence_file_is_fully_bound` when git is unavailable, so a git-less CI image would run the repo-wide sweep green having proved nothing about commits — the same "a skip looks like a pass" failure `junit_ingest.py` was deliberately built to close for the gate itself. No git-less CI exists today (`.github/` is still absent), so nothing is exploitable now.

## Deferred from: code review of story-2-1-establish-the-owned-agent-runtime-boundary (2026-08-10)

- ~~`create_agent_runtime()` never wires `config.model`/`config.api_key` into a real PydanticAI model.~~ **CLOSED 2026-08-13 (Story 2.7, commit `a3bbebf`):** the adapter now resolves explicit `test`, `openrouter:<model>`, and `google:<model>` configurations while preserving an injected model as the winning test seam; normal CI's `test` default constructs no network-capable client.
- `timed_out`/`budget_exhausted` outcomes never populate `turn` — the cancellation/budget-exceeded exception branches return an outcome with `turn` defaulting to empty, discarding any tool-call progress made before the failure; the adapter never wraps `run_sync` in PydanticAI's `capture_run_messages()`. [backend/agent/runtime.py:151-170] — enhancement, not required by any AC in this story; revisit when a story needs to debug/display a timed-out run.
- `_merge_budget` overloads `None` as both "field not specified" and "explicitly unlimited" — a caller cannot request "no limit" and have it win over a configured default, unlike `settings.py`'s `_optional_int`/`_optional_float` tri-state parsing built to avoid exactly this ambiguity. [backend/agent/runtime.py:213-238] — no current caller depends on the distinction.
- `_to_deferred_results` has no duplicate-`tool_call_id` detection — two approval decisions sharing an id in one request silently collapse to the last one with no error or log. [backend/agent/runtime.py:257-272] — requires malformed/duplicate caller input not produced by any current path.
- The `except AgentRunError` catch-all assumes every real provider failure surfaces as a PydanticAI exception type; whether a raw transport exception (e.g. from the underlying `httpx`/`openai`/`google-genai` SDK) could cross the seam unwrapped during a live call is unverified. **Re-annotated 2026-08-13 (Story 2.7):** configured provider construction and the execute-turn request path make this exposure live rather than hypothetical, but NFR26 still keeps live-provider calls out of normal CI. **Owner/revisit trigger:** the first story adding budgeted live-provider coverage must prove raw transport exceptions remain wrapped.
- ~~`to_framework_messages` silently drops an unrecognized `AgentMessageV1.role` (no `else`), and its counterpart silently skips an unrecognized `AgentPartV1.kind`.~~ **CLOSED 2026-08-13 (Story 2.7):** execute-turn now rehydrates bounded owned history from persisted visible activities, and reverse translation raises with explicit tests for both unknown discriminants. Raw `AgentTurnV1` persistence remains intentionally out of scope for Epic 4.
- `test_the_guarded_layers_are_not_empty` checks the *combined* `domain`+`application` file count (`>5`), not each layer individually — `domain` could theoretically be emptied while `application` alone satisfies the count, silently disabling all domain-layer checks. [backend/tests/architecture/test_agent_runtime_boundaries.py:227-233] — meta-test hardening, not exploitable without an adversarial deletion that would break many other tests first.
- `find_framework_typed_contract_fields` inspects annotation/default AST nodes but never string-literal (quoted forward-reference) annotations, so a hypothetical `raw: "ModelResponse | None"` field would bypass the AC3 contract-field guard. [backend/tests/architecture/test_agent_runtime_boundaries.py:165-186] — no current contract field uses this style.
- `test_wall_time_exhaustion_is_timed_out_not_budget_exhausted` asserts a `timed_out` outcome using a real `time.sleep(0.3)` against `deadline_seconds=0.05` — wall-clock-timing-dependent and a plausible (unconfirmed) source of future CI flakiness under load, despite the ~6x margin. [backend/tests/test_agent_runtime_adapter.py:209] — passed in the recorded Gate A run.
- `AgentBudgetV1.total_tokens_limit` has no corresponding `agent_runtime_*` setting/env var, unlike `request_limit`, `tool_calls_limit`, and `deadline_seconds` — it can only be set programmatically per-request, never operationally configured. [backend/settings.py:67-74, 173-208] — minor config-surface asymmetry.

## Deferred from: code review of story-2-2-establish-the-deterministic-evaluation-harness (2026-08-10)

- ~~The evaluation report's pass signal is narrower than the case schema it reports against. `GoldenCase` carries `expected_evidence_refs`, `expected_visible_state` and `expected_visible_text`, but `ToolRoutingEvaluator` judges tool name and arguments only.~~ **CLOSED 2026-08-15 (Story 2.9):** the report now composes routing, grounding where applicable, policy outcome, exact visible state, and exact application-derived visible text; any component can turn the case verdict red.

  **PARTIALLY CLOSED 2026-08-14 (Story 2.7 code review).** Two claims above were true when written and are now false, so they are corrected here rather than left to mislead a future reader:

  - *"`expected_evidence_refs` is evaluated nowhere in the repo"* — **no longer true.** `GroundingEvaluator` compares it on every branch, supported and failed alike (`backend/evals/evaluators.py`). Comparing it on the failure branches is what proves AR11's non-retargeting rule: a failed claim must emit no locator naming another record or version. The locators now come from a real calculator run over a real fixture projection (`backend/evals/fixture_projection.py`); the earlier implementation fabricated the capability result, so the field was read but the oracle could not fail.
  - *"Task 3 explicitly forbids shipping a second evaluator"* — **spent, as designed.** Story 2.7 shipped exactly one second evaluator (`GroundingEvaluator`), which is what that fence reserved. The remaining fence is Story 2.9's, for refusal/injection.

  ~~**Still open, and this is the surviving half:** `expected_visible_state` and `expected_visible_text` are asserted by the pytest suite but not by the report generator.~~ **CLOSED 2026-08-15 (Story 2.9):** both comparisons execute inside `generate_demonstration_report`; the report reason records policy and visible-state/text verdicts alongside routing and grounding.
- ~~`alembic check` cannot be reproduced by another engineer or by CI: the repository has no checked-in `alembic.ini`, so Story 2.2's Task 10 gate was run against a temporary config that was then deleted (recorded in Completion Notes). The "zero diff" claim is therefore unverifiable after the fact. Pre-existing repo gap, not introduced here; a checked-in `alembic.ini` (or a `scripts/` wrapper that synthesizes one deterministically) would make the migration gate a real, re-runnable check.~~
  **CORRECTED 2026-08-10 (story-2-3 creation) — the central claim is false, and the correction was verified by running it.** `alembic.ini` **is** checked in at the repository root. It was committed in Story 1.1 (`3e9540c feat(1-1)`), is not gitignored, is present on `main` and on the story-2-2 branch head, sets `script_location = %(here)s/backend/migrations`, and leaves `sqlalchemy.url` empty because `backend/migrations/env.py:20-22` fills it from `default_settings().provisioning_database_url`.

  Measured at `8c21de0` with Docker PostgreSQL up:

  | Working directory | Command | Result |
  |---|---|---|
  | repository root | `uv run --project backend alembic check` | `No new upgrade operations detected.` — **the gate passes** |
  | `backend/` | `uv run alembic check` | `FAILED: No 'script_location' key found in configuration.` |

  **Root cause confirmed:** alembic was invoked from `backend/` — pytest's working directory, and the natural place to stand — where it finds no root-level `alembic.ini`. The resulting error reads exactly like a missing/incomplete config, which is how the original finding was reached. It is a working-directory mistake, not a repository gap. **No remediation work is owed and this item needs no owner.** Left visible rather than deleted because Story 2.2's Completion Notes record a temporary config being synthesized on this basis, and the next story to touch migrations (Story 2.3) must not repeat it.
- `.github/workflows` is still absent, so every "normal CI" guarantee in this story — AC1's zero-network default suite, Task 8's "fails CI if any file under `backend/evals/golden/` does not validate" — is enforced only by whoever remembers to run pytest locally. Pre-existing (noted in the story-1.11 review above, in the `requires_git` entry); the exposure grows as Epic 2+ adds gates that assume CI executes them.
## Deferred from: story-2-3 creation (2026-08-10)

- **`ScenarioCatalogueReader` leaks SQLAlchemy into the application layer — the one open AD-1 violation in `backend/application/**`.** `from sqlalchemy import Connection` at [backend/application/ports/scenario_catalogue.py:9] is a top-level runtime import, and `Connection` then appears in the Protocol's own signatures (`list_fixture_versions(self, connection: Connection)`, `get_scenario_context(self, connection: Connection, ...)` — lines 38-48). AD-1/AR1 name SQLAlchemy explicitly: *"Domain and application code must not import FastAPI, PydanticAI, SQLAlchemy, Cognito, S3, Logfire, or concrete model providers."* Nothing is broken at runtime; the cost is that the seam does not swap (a non-SQLAlchemy implementation cannot satisfy the contract honestly, and stubs drag the driver in through the type), and — more practically — this single line is what prevents `sqlalchemy` being added to `FORBIDDEN_ROOT_MODULES` and turning AD-1 from prose into a test that can go red for the whole layer.

  The newer port written for Stories 1.4/1.5 already made the opposite choice: [backend/application/ports/scenario_projection.py:104] types the same runtime object as `connection: Any`, keeping the vendor name out of the layer.

  **Deliberately not fixed in Story 2.3** — it is Story 1.3 code inside the Gate A read surface, and Story 2.3 was already the first story to introduce a governed write path. Story 2.3 instead (a) directs its new `ports/conversation.py` to copy `scenario_projection.py` rather than this file, and (b) ships an architecture guard scoped to the new application modules carrying a **documented allow-list entry** for this known leak. **Closing this item means deleting that allow-list entry** — that is the definition of done, and it is the thing to grep for.

  Three fix tiers, in ascending order of correctness — do not stop at the first:
  1. `if TYPE_CHECKING: from sqlalchemy import Connection` — removes the runtime import, keeps type checking. Strictly better than `Any`, but the vendor type still appears in the contract.
  2. `connection: Any` — matches `scenario_projection.py`, removes the vendor name, loses type safety on the parameter. Consistent with the repo, not actually good.
  3. **The real fix:** the port takes no connection at all; the adapter owns its own unit of work, and the application calls a method rather than threading a driver handle through the boundary. This is a refactor of the port, its Postgres adapter, `api/deps.py:get_catalogue_reader`, and the fixture-catalogue router — not a one-liner.

  ~~**No story owns this yet.** Earliest natural candidate is **Story 2.4**, which adds the next `application/ports/` module (the SSE/event-stream port) and will already be touching that guard; otherwise, the next story that modifies `ScenarioCatalogueReader` or `adapters/postgres/scenario_catalogue.py` for any reason.~~

  **CORRECTED 2026-08-11 (story-2-4 implementation) — the premise for naming Story 2.4 is false, and the item stays open.** Story 2.4 adds **no** `application/ports/` module. Its Decision 3 puts the SSE replay read on the **existing** `ConversationRepository` as a single `events_after` method, because AD-22 gives the conversation aggregate ownership of its own events and the database pins the stream's identity to the conversation UUID (`ck_persisted_event_stream_is_conversation`) — a second port for "the event stream" would split one aggregate across two seams. So Story 2.4 never went near `scenario_catalogue.py`, and nothing about it made this leak cheaper to fix.

  It was also **not** taken opportunistically. The real fix is tier 3, which refactors a Gate A read port, its Postgres adapter, `api/deps.py:get_catalogue_reader` and the fixture-catalogue router; Story 2.4 already carried an NFR35 threshold that blocks its own acceptance (AD-26), and tiers 1 and 2 would have been *worse than leaving it* — someone would then delete the `ALLOWED_LEAKS` entry while the vendor type still sat in the contract. `ALLOWED_LEAKS` is therefore untouched, and `test_every_allowed_leak_still_exists_and_still_leaks` still goes red on a half-fix, which is the behaviour we want.

  **Owner restated: the next story that modifies `ScenarioCatalogueReader` or `adapters/postgres/scenario_catalogue.py` for any reason.** Whoever takes it: tier 3 touches Gate A code, so re-run the Gate A readiness report per `docs/EVIDENCE-CONVENTION.md` and confirm `gate_a_passed` still reads `true`.

## Deferred from: code review of story-2-3-create-and-revisit-durable-conversations (2026-08-10)

- **UX-DR35's "Send must be visually discontinuous from Run optimization and Approve" is neither expressed in code nor asserted by a test.** `Composer.tsx:6` renders `<button type="button" disabled={…}>Send</button>` with no styling at all, and nothing anywhere in `frontend/src/features/chat/` establishes a visual authority hierarchy. **Deferred reason: not actionable in this slice** — the Chat surface ships no Run or Approve control for Send to be discontinuous *from*, and Story 2.3's Task 9 explicitly forbids adding a disabled Run or Approve "for later". The requirement is therefore vacuously satisfied today and becomes real the moment a second, higher-authority action lands beside Send.

  **PARTIALLY DISCHARGED 2026-08-18 (Story 3.1); remains open.** Story 3.1 ships revise/reject beside the conversation surface and makes those draft commands visually discontinuous from Send, with an automated assertion. It explicitly ships neither Run optimization nor Approve, so it cannot satisfy the entry's original owner condition and does not close it.

  Separately resolved in the same review, so **not** part of this item: Chat's missing pending/error states and its non-use of `EmptyState` / `InlineAlert` / `StatusBadge` / `Skeleton` / `ReconnectBanner` were triaged as patches against Task 8, not deferred.

  **Owner: Story 3.6**, the first story to ship a Run optimization control on the Chat surface. It must introduce and automatically assert Send-versus-Run discontinuity; Approve remains outside Story 3.1. `EXPERIENCE.md`'s Accessibility Floor makes automated coverage the only accepted proof in this repo.

## Deferred from: code review of story-2-7-ground-schedule-claims-in-exact-evidence (2026-08-13)

- **UPDATED 2026-08-19 (Story 3.2): the schedule-version aggregate now exists, but `baseline_schedule_version` still has no value supply.** Story 3.2 introduced immutable `schedule_version` rows and candidate persistence, while both scenario producers continue to return `None`: `adapters/postgres/scenario_projection.py` returns the literal `None`, and `adapters/postgres/scenario_catalogue.py` selects `literal(None, type_=String)`. The aggregate prerequisite is therefore closed; moving and supplying the scenario baseline pointer remains owned by Epic 4 (AD-10).

- **CORRECTED 2026-08-18 (Story 3.1): the production projection has no lock supply at all.** `ScenarioProjectionReader.get_locks` applies the query to a hardcoded empty tuple; this is stronger than both committed fixtures merely containing zero locks. Story 3.1 preserves the real field and proves the preservation mechanism with a seeded reader that returns locks, without editing Gate A fixtures or claiming that a zero-item production read proves preservation.

- **No recovery for a run stranded in `agent_running`.** `claim_queued_run` commits `status='agent_running'` in its own short transaction and the model call runs outside any transaction, so a client disconnect, worker death, or process restart between claim and finish leaves the row unrecoverable: there is no reaper, no timeout sweep, and no `claimed_at` column to build one from. [backend/adapters/postgres/conversation.py:310-314] — **Re-annotated 2026-08-20 (Story 3.3):** Story 3.3 delivered durable leasing and fencing for Workflow-owned `ScheduleRun` compute jobs only. `AgentRun` belongs to Conversation, has no backing job row, and `workflow.job_queue.schedule_run_id` is deliberately required; generalizing that table would create an unrequested payload-reference union. `AgentRun` claiming therefore remains a separate, unfenced mechanism. **Owner/revisit trigger: the first story that specifically requires `AgentRun` recovery, or a future generalized job abstraction justified by measured need.** Note the separable half that was *not* deferred: unhandled exceptions on the normal path (`UncitedNumericProseError`, `_finish` failures) are a patch against Task 11, since they strand runs during ordinary operation rather than on infrastructure failure.

- **A revoked membership turns a queued run into a permanent non-disclosing 404.** The claim query inner-joins `membership` on `(app_user_id, site_id, revoked_at IS NULL)`, so revoking a membership after `accept_turn` has queued a run yields no row; `claim_queued_run` returns `None` and the route raises a bare 404 that is indistinguishable from "no such run". Nothing ever drains the orphaned queued row. [backend/adapters/postgres/conversation.py:293-307] — **Deferred reason: membership lifecycle is not this story's**, and the non-disclosure behaviour is correct as a *response*; what is missing is the queue-drain path. **Owner/revisit trigger: the first story that revokes a membership while work is in flight**, or Epic 3's recovery sweep, whichever lands first.

- **Duplicate live memberships raise `MultipleResultsFound` → 500.** The claim query ends in `.one_or_none()`, so two non-revoked memberships for the same user and site crash the request rather than resolving to one. [backend/adapters/postgres/conversation.py:305] — **Deferred reason: no current path creates the duplicate** and no database constraint currently forbids it either, so this is a latent schema gap rather than a reachable defect. **Owner/revisit trigger: the story that introduces a second membership role** (the registry's `CapabilityGrantContextV1` docstring already anticipates "until a second user is activated") — it should add the partial unique index at the same time.

- **Per-request LLM provider construction and per-item `TypeAdapter` rebuilds.** `get_agent_runtime_factory` is a per-request FastAPI dependency and `PydanticAIAgentRuntime.__init__` now eagerly builds a real provider client when no model is injected, so every `/execute` call constructs a fresh `OpenAIChatModel`/`GoogleModel` plus HTTP client with no pooling and no shutdown hook. Separately, `_activity` constructs a `TypeAdapter(ActivityItemOut)` per timeline item (up to 200 per request) and per SSE frame, and the postgres adapter builds a fresh adapter on every payload serialize/deserialize. [backend/agent/runtime.py:399-421, backend/api/routers/conversations.py:116-119, backend/adapters/postgres/conversation.py:211-216] — **Deferred reason: correctness is unaffected and no AC or NFR in this story carries a latency threshold** (NFR35's four rows belong to Stories 1.4, 1.5, 2.4 and 3.5). Pydantic schema construction is the expensive part and is trivially cacheable at module scope. **Owner/revisit trigger: the first story with a measured latency budget on the conversation surface, or the first live-provider load test.**

- **Any site member can execute another member's queued run.** `claim_queued_run` filters on `conversation_id` and `agent_run_id` only; site isolation comes from RLS, so within a site any authenticated member can trigger execution — and the associated LLM spend — of a run queued by someone else's message. `deps.actor_id` is taken from the originating message, so the audit trail attributes the run to a user who did not start it. [backend/adapters/postgres/conversation.py:299-303] — **Deferred reason: unreachable in this milestone.** The membership table has no role column and the registry composes `role` from the single active membership, so there is exactly one actor per site today. **Owner/revisit trigger: the first story that activates a second user or membership role** — it must decide whether execution authority follows the conversation's author or the site, and assert it.

- **Metric calculators cannot push a time window into the projection, so every demand metric drains far more rows than it needs.** `DEMAND_FILTERS` publishes only `start_minute_gte` and `end_minute_lte` (`backend/adapters/postgres/scenario_projection.py:355-361`), which express *containment* within a range. The grounding calculators need *overlap* (`row.start < window_end AND row.end > window_start`), which those two keys cannot express — using them would silently drop partially-overlapping rows that `interval_overlap_minutes` correctly counts, converting a fail-closed bound error into a wrong number wearing a valid evidence locator (trap #1). Story 2.7's code review therefore pushed down `task_id`/`family` only and raised the compute row bound instead. Measured on `sample_tiny_input`: demand is 60-minute buckets, so an 8-hour window is ~8 rows per task/family versus the 53–197 that task+family pushdown alone leaves. — **Deferred reason: adding `start_minute_lt`/`end_minute_gt` widens `GroupQueryKeysV1`, the published filter-key contract that `scheduling_inspect` (Story 2.5) also reads**, and AD-4 makes source interpretation the adapter's own decision. Out of scope for a story that declared `scheduling_inspect` changed only for Task 8. **Owner/revisit trigger: the first story that needs a demand metric to scale beyond one fixture week, or any story already amending `GROUP_QUERY_TABLES`** — it should add the overlap keys, push the window down, and restore the compute row bound to the inspect limit.

- **`revoked_conversation_ids` is a grant dimension with no data source.** `CapabilityGrantContextV1.revoked_conversation_ids` is branched on by `compose_granted_capabilities` (`backend/application/capabilities/registry.py:52-53`), but the only production construction site — the execute-turn route — leaves it at its empty default, so per-conversation capability revocation has never taken effect. [backend/api/routers/conversations.py:190-196] — **Deferred reason: nothing in the schema records a revoked conversation**, so wiring it now would be a mechanism with no user and no test that could distinguish it from the default. Story 2.7's code review fixed the sibling defect (`feature_policy` derived tautologically from `installed_modules()`) because that one *did* have a real supplier available in `Settings`; this one does not. **Owner/revisit trigger: the first story that can revoke a conversation or a capability grant at runtime** — Epic 4's audit/authority work is the natural candidate. It must add the persisted source and an assertion that a revoked conversation composes an empty grant.

## Deferred from: code review of story-2-7-ground-schedule-claims-in-exact-evidence (2026-08-14, remediation re-review)

- ~~**`argument_mismatch` is not an observably distinct failure, so two of the four NFR28 cases assert an identical pair.**~~ **CLOSED 2026-08-15 (Story 2.9):** `GroundingEvaluator` retains AR11's closed persisted failure vocabulary, but now separately compares each failed claim's metric and arguments with the originating `scheduling_compute` call. The argument-mismatch and unknown-result cases therefore remain visibly `missing_evidence` while their evaluation oracles are pairwise distinguishable; a cross-swapped outcome fails in both directions.

- **Removing `required_demand_minutes` from `MetricV1` breaks deserialization of any already-persisted claim.** The D4 unit split replaced `required_demand_minutes` with `required_headcount_minutes` + `required_demand_volume`, but `_activity_from_payload` reads persisted rows through `TypeAdapter(GroundedResponseV1).validate_python(value["response"])`, which raises `ValidationError` for any `persisted_event` row whose `response.segments[].metric` carries the old name. That propagates through `_event_from_row` to `timeline()`, permanently 500-ing the affected conversation with no compat alias and no data migration in the change. [backend/application/contracts/grounding.py:19-35, backend/adapters/postgres/conversation.py:483] — **Deferred reason: pre-production, and reachable only in developer databases.** No deployed environment holds `agent_response` rows — the variant itself shipped in this story — so the blast radius today is a local dev DB that executed a turn between the Phase B commit and the remediation. (`GroundingUnitV1` gaining `"units"` is additive and safe; only the metric rename is breaking.) **Owner/revisit trigger: the first change to `MetricV1`, `GroundingFailureV1`, or any persisted contract `Literal` after a real deployment exists** — at that point a closed vocabulary in a durable payload needs either a compat alias, a migration, or a documented forward-only read path. Worth deciding the general rule then rather than per-field.

- **No metric answers "how many people are working".** `staffed_minutes` returns worker-MINUTES, which is the right dimension for comparing against demand but not the answer to the plainest staffing question a planner asks. Assignments carry worker identity, so a distinct headcount metric is computable from the projection today. [backend/application/contracts/grounding.py — `MetricV1`] — **Deferred reason: Task 3 deliberately closed the metric vocabulary** ("four is enough — do not invent a fifth to look complete"), and the code review that surfaced this also removed `shortfall_minutes`, so adding one back in the same pass would churn the closed set twice for a question no AC names. The gap is real and was confirmed against the domain model during review: asking about headcount on an outbound or inbound task is a VALID planner question, answered from assignments rather than demand — only *required demand in minutes* is unanswerable for those families. **Owner/revisit trigger: the first story whose ACs include a staffing or coverage question, or Epic 3 when the solver makes staffing central.**

- **UPDATED 2026-08-19 (Story 3.2): `shortfall_minutes` remains deferred, with condition 1 now satisfied.** Governed solver assignments now carry the per-worker, per-task `QualificationRefV1.rate` actually used for coverage, satisfying `docs/DOMAIN-MODEL.md` §4 condition 1. Conditions 2–4 remain open: preserve demand family/unit through the candidate read model, define the mixed-family aggregation rule, and expose the metric through the grounded-query contract with dimensional tests. The prior read-model problem is unchanged: headcount minutes and volume cannot be subtracted before those semantics exist. **Owner/revisit trigger: Story 3.8 at the earliest**, when candidate comparison/read models can satisfy all remaining conditions together.

## Deferred from: code review of story-2-8-jump-to-evidence-and-return-to-the-claim (2026-08-15)

- **AC3's accessibility proof runs in Vitest but is not bound to Gate A's NFR29 registry.** Story 2.8 Task 8 offered two options — extend `frontend/src/test/accessibility-contract.test.tsx`, or add a sibling file — and they are not equivalent: a `GateACheck` only ingests the files it declares, and the `accessibility_component_layer` check declares a hand-written list naming `accessibility.test.tsx`, `accessibility-contract.test.tsx` and `usePhoneViewport.test.tsx`. The sibling that was created, `frontend/src/test/evidence-accessibility.test.tsx` (5 cases), is outside it, so Gate A would report `accessibility_and_responsiveness` green even if every evidence focus assertion went red. [backend/scripts/gate_a_checks.py:367-377, frontend/src/test/evidence-accessibility.test.tsx] — **Deferred reason: portfolio MVP scope.** The behaviour is still protected by CI (`npm test` goes red), so this is an attribution gap, not a protection gap; and registering the file means editing a Python file, breaching the zero-line `backend/` fence that Decision 1 established as this story's hard constraint. **Owner/revisit trigger: the next story that already has a legitimate reason to touch `gate_a_checks.py`** — it should add the file to `accessibility_component_layer` at that point, or fold the cases into `accessibility-contract.test.tsx`, whichever is cheaper then. Note the separable half that was *not* deferred: the missing return-focus case in that file is a patch, since it is product behaviour rather than registry plumbing.

- **The streaming JUnit reporter cannot distinguish a truncated Playwright run from a complete one.** `StreamingJUnitReporter` rewrites the whole XML on every `onTestEnd` and implements no `onEnd`, so a run that dies midway leaves a file that is accurate about the cases that finished and silent about the ones that never started — `tests="31" failures="0"` reads identically whether 31 was the total or the point of death. It also hard-codes `errors="0"` at both suite and root level, so Playwright's own error channel (worker crashes, global-setup failures) is never recorded. Per-case `failures` accounting is correct (`test.outcome() === "unexpected"`), so the reporter does not misreport red tests. [frontend/e2e/support/streaming-junit-reporter.mjs] — **Deferred reason: the specific measurement behind Story 2.8's Gate A refresh is corroborated** (the story's Debug Log records an independent `list`-reporter run completing all 48 cases) **and becomes reproducible once the reporter is committed**, which is a patch in this same review. `junit_ingest.py`'s zero-case rule already catches a wholly missing spec file; only partial truncation is invisible. Truncation detection protects *future* measurements and is not owed by a story with no measured threshold of its own. **Owner/revisit trigger: the first story that adds a new Playwright spec to a Gate A check, or any Gate A run that produces an unexpected case count** — at that point add `onEnd` finalisation writing a completion marker, and surface real `errors`.

## Deferred from: code review of story-2-9-clarify-refuse-and-fail-safely (2026-08-16)

- **A suspension is finalised as `agent_cancelled` rather than `approval_required`, so the approval it was waiting on is discarded.** `terminal_status` maps `suspended -> agent_cancelled` and `outcome.approval.pending_calls` is not persisted anywhere. AD-7 models `approval_required` as a WAITING state with outgoing edges (`--> agent_running: decision recorded`, `--> agent_cancelled: rejected or expired`), and writing it from the finalisation path parked the row forever: `claim_queued_run` only claims `agent_queued`, nothing else touches the row, and no approval decision can be recorded in this milestone. Landing on AD-7's own "rejected or expired" edge is terminal and truthful, and the distinct cause survives as terminal reason `approval_unsupported` with its own planner-visible label. [backend/application/use_cases/execute_turn.py:terminal_status] — **Deferred reason: approval execution is Epic 4's by AD-10/FR17-19**, and the three missing pieces (persisting `AgentApprovalPendingV1.pending_calls`, an approval-decision endpoint, `DeferredToolResults` on the request path) are Stories 4.1-4.3, not a review patch. The path is unreachable today: `demonstration` is the only `approval_policy="exact_action"` module and `demonstration_enabled` defaults `False`. **Owner/revisit trigger: Stories 4.1-4.3.** This is enforced mechanically, not by this note: `test_request_path_grants_no_approval_capability_in_this_milestone` composes the real request-path grant and fails the moment any capability with a non-`none` approval policy becomes reachable, with the remediation spelled out in its assertion message. Verified to fail under `DEMONSTRATION_ENABLED=true` during this review.

- **AgentRun cancellation is declared by `AgentFailureReasonV1` but has no producer and no owner.** `cancelled` is a member of the pre-existing `AgentFailureReasonV1`, yet no branch in `backend/agent/` or the request path ever sets it. It was removed from `TerminalReasonV1` and from `terminal_outcome`'s mapping in this review, because its only reachable path would have been a manifest error code colliding with it — the wrong path — and the dead `failure_reason == "cancelled"` branch in `terminal_status` gave a false impression that turn cancellation existed. AD-7 does model `agent_queued --> agent_cancelled` and `agent_running --> agent_cancelled`, so the capability is architecturally intended. [backend/application/contracts/agent_runtime.py:AgentFailureReasonV1] — **Deferred reason: no story currently owns AgentRun cancellation.** Story 3.4 ("Provide the Safe Cancellation Command") is a `ScheduleRun` technical enabler feeding Story 3.7's Runs workspace; it does not cancel agent turns. Removing the value from the pre-existing adapter vocabulary would be a contract change outside this story's scope. **Owner/revisit trigger: the first story that makes the adapter emit `failure_reason="cancelled"`** — `test_every_emittable_failure_reason_has_a_terminal_mapping` fails at that moment and requires the `TerminalReasonV1` value and its named branch to land in the same change, so the reason can never silently render as `capability_error`.

- **The clarification `question` is unbounded untrusted model prose and may name entities the application never verified.** `resolve_clarification` passes `clarification.question` through verbatim; only its length is now bounded. A model may write "Did you mean Taylor Smith or Jordan Lee?" with no such workers existing. [backend/application/clarification/resolve.py] — **Deferred reason: enforcement would cost the feature.** The structural fix matching Decision 5 is a closed `question_kind` vocabulary with application-composed prose, but Decision 5 explicitly protects clarifications that cite no entity at all ("ambiguity about time, scope, or intended consequence has no entity to cite, and requiring one would force the model to invent one"), and a closed vocabulary forces every ambiguity into a preset slot. Mitigated instead by comparability: the candidate list is a separately named region ("Records in Scenario Data") and each resolved label now carries the record's real name alongside its identifier, so a fabricated name has verified rows to be checked against in the same view — where previously the list showed identifiers only and could not falsify anything. Blast radius is bounded by AC1: a clarification creates no draft, run, or side effect. **Owner/revisit trigger: the first story where a clarification can precede a consequential action**, or any evidence of planners acting on unverified names.

## Deferred from: code review of story-3-1-create-and-revise-a-reversible-repair-draft (2026-08-18)

- **No reaper exists for an agent run stranded in `agent_running` by an aborted finalisation.** `finalize_agent_run` composes the create-draft bundle inside the caller's one transaction, so any failure after `claim_queued_run` committed `agent_running` leaves the row terminal-less. Story 3.1's content-addressed `proposal_id` makes this reachable on the ordinary happy path rather than only on infrastructure failure — that half is a patch in this same review. [backend/application/use_cases/finalize_agent_run.py] — **Deferred reason: pre-existing and already tracked** (`deferred-work.md:180`); the stranding mechanism predates this story and the fix is a background reaper or a claim-expiry sweep, neither of which any story owns yet. **Owner/revisit trigger: the first story that runs a scheduled backend job, or any observed stranded run in a deployed environment.**

- ~~**`ProposalV1` gives every field a default, so `ProposalV1()` constructs an empty proposal.**~~ **CLOSED 2026-08-19 (Story 3.2):** the progressive proposal contract remains backward compatible, but its immutable acceptance boundary is now `RunSnapshotV1`, whose `__post_init__` rejects every absent AD-20-required field. `create_run_snapshot` builds that contract only from an accepted persisted proposal and current scenario data, then hashes and inserts it atomically with `schedule_run`; an incomplete proposal therefore cannot be frozen or executed.

## Deferred from: implementation of story-3-2-produce-a-deterministic-candidate-from-an-immutable-snapshot (2026-08-19)

- **The governed solver does not wire the fixture's `MinimumHoursBetweenShifts = 10`; legacy `DEFAULT_MIN_GAP_HOURS = 2.0` remains in effect.** This is a measured, behavior-changing divergence rather than a documentation-only mismatch: applying the fixture value changes every solve and would invalidate calibrated legacy behavior. Story 3.2 deliberately wires only the independently testable per-employment-type weekly caps through its governed adapter and leaves `backend/engine/cpsat/**` untouched. **Owner/revisit trigger: the first Epic 3 story that explicitly owns minimum-rest semantics and can recalibrate and regression-test the resulting solve behavior.**

- **More than 200 preserved locks makes a scenario permanently undraftable through a non-retryable error.** `_preserved_locks` raises `DraftFailedError` once `remaining <= 0`, and `retryable_error_codes` covers only `invalid_query`, so the model cannot correct it, the planner cannot see why, and `MAX_PRESERVED_LOCKS = 200` is a module constant with no setting to raise it. The loop also never checks `len(page.items) <= limit`, so an over-long page drives `remaining` negative into the same dead end. [backend/application/capabilities/scheduling_draft.py:33, :308-333] — **Deferred reason: unreachable today by construction.** Gap 1 measured that `get_locks` calls `_apply_query((), ...)` with a hardcoded empty tuple — there is no code path in the repository that produces a lock, so the ceiling cannot be hit and any fix would ship untested against real data. **Owner/revisit trigger: the first story that supplies real locks** — at that point the bound becomes a setting, the failure becomes retryable or paginating, and the page-length check lands with a test that drives more than 200 locks.

## Deferred from: review of spec-gate-a-p3-live-parity (2026-08-18)

Surfaced by the adversarial review of the Gate A P3 change. All five were
verified against the code; none is caused by that change, so each was recorded
rather than folded in.

- source_spec: `spec-gate-a-p3-live-parity.md`
  summary: The three legacy SQLite write routes (`POST /scenarios`, `POST /scenarios/{id}/runs`, `POST /constraints`) are unauthenticated and site-unaware, held offline only by a filesystem-flag check.
  evidence: `refuse_legacy_routes_during_gate_a` [backend/api/main.py:143-158] gates them on `Path(maintenance_flag_path).exists()`. It fails closed on `OSError`, but a host where the flag is simply absent serves all three. Their dependants are `{get_db, get_settings}` — no session, no CSRF, no site. Pre-existing since Gate A cutover; the retired 1.9 evidence file recorded the live flag state as explicitly "not verified". Now named in `test_gate_a_mutation_audit._LEGACY_WRITE_ROUTES` and in `docs/GATE-A-RUNBOOK.md` so they are at least visible. **Revisit trigger: deleting the legacy SQLite surface, which Story 1.9's legacy sweep began and did not finish.**

- source_spec: `spec-gate-a-p3-live-parity.md`
  summary: `api_parity` binds a whole file (~50 cases) rather than the one test that proves parity, because `GateACheck` has no node-id granularity.
  evidence: `file_outcomes()` [backend/scripts/junit_ingest.py] rolls up per file, and `declared_pytest_cases()` recovers expectations from that same source — so deleting `test_gate_a_projection_api_matches_every_contract_record_for_both_fixtures` removes the expectation along with the proof. Mitigated by `test_api_parity_binds_a_test_that_still_exists` [backend/tests/test_gate_a_readiness.py], which pins the name. The structural fix is a `test_cases` field on `GateACheck`, which affects all 17 JUnit-backed checks. **Revisit trigger: the next story that adds a check to a shared test file.**

- source_spec: `spec-gate-a-p3-live-parity.md`
  summary: The CSRF middleware returns one indistinguishable 403 for both a bad Origin and an invalid token, so no test can assert which check fired.
  evidence: `api/main.py:231-236` returns `csrf_validation_failed` for `supplied_origin not in allowed_origins` **or** `not csrf_valid`. A regression making every CSRF token fail validation would leave both `test_every_versioned_write_route_denies_a_request_without_a_csrf_token` and `..._denies_a_cross_origin_request` green. Pre-existing; splitting the codes is an API-contract change affecting the generated client. **Revisit trigger: any story touching the CSRF contract.**

- source_spec: `spec-gate-a-p3-live-parity.md`
  summary: `test_proposal_persistence.py` — the behavioural PostgreSQL proof of proposal site scoping — is in no Gate A check.
  evidence: `grep -c test_proposal_persistence backend/scripts/gate_a_checks.py` returns 0. `test_gate_a_mutation_audit.py` defers the behavioural half of site scoping to that file, and the runbook's site-scoping claim rests on it, but nothing in the registry consumes it. Story 3.1 is an Epic 3 story and Gate A's registry covers Epic 1, so this is a registry-scope question, not an omission by this change. **Revisit trigger: whoever defines the Epic 3 equivalent of the Gate A registry.**

- source_spec: `spec-gate-a-p3-live-parity.md`
  summary: `assert repository.calls == []` covers only the proposal repository, so it is inert for four of the six versioned write routes.
  evidence: The denial tests override `get_proposal_repository` alone; `auth/logout` and the three conversation routes use other collaborators, and the middleware short-circuits before any dependency resolves regardless. `test_an_authorized_proposal_write_does_reach_the_repository` makes the assertion falsifiable for the proposal routes specifically. Extending it means recording stubs for `get_conversation_repository` and `get_identity_store`. **Revisit trigger: the first story that adds a write route with a new collaborator.**

## Deferred from: code review of story-3.2 (2026-08-19)

- **The AD-9 "no silent rebasing" scenario-staleness check locks the proposal row but not the scenario side of the same comparison.** `create_run_snapshot` (`application/use_cases/create_run_snapshot.py:70-95`) takes `proposal_repository.get_current(..., for_update=True)` but reads `scenario_catalogue.get_scenario_context(...)` with no lock. Under READ COMMITTED, a concurrent scenario-version change landing between that read and the snapshot INSERT in the same transaction is not guarded against — the staleness check can pass against a version that stops being current a moment later. [backend/application/use_cases/create_run_snapshot.py:70-95] — **Deferred reason: out of scope per the zero-line-diff mandate.** Closing it means extending `ScenarioCatalogueReader` with a locking read, which touches `backend/adapters/postgres/scenario_catalogue.py` — a file this story explicitly fences as a zero-line diff. **Owner/revisit trigger: the first story that next touches `ScenarioCatalogueReader`, or any observed stale-scenario candidate in a deployed environment.**

- **AD-20-required contract fields are structurally dead.** `RunSnapshotV1.objectives` is hardcoded to `()` in `create_run_snapshot.py:115` because `ProposalV1` has no "objectives" distinct from constraints to source it from, and `ConstraintResultV1.contributing_evidence_refs` / `SolverOutcomeV1.evidence_refs` are never populated by either `hard_constraints.py` or `candidate_metrics.py`. They satisfy `RunSnapshotV1.__post_init__`'s field-presence validation but never carry real data via any path reachable today. [backend/application/use_cases/create_run_snapshot.py:115, backend/application/scheduling/hard_constraints.py, backend/application/scheduling/candidate_metrics.py] — **Deferred reason: pre-existing scope gap, not a regression.** Populating `objectives` needs `ProposalV1` to gain an objectives concept distinct from constraints (Story 3.1's contract, out of this diff's scope); populating `contributing_evidence_refs`/`evidence_refs` needs the constraint/metric calculators to thread evidence-ref data they don't currently receive. **Owner/revisit trigger: the first story that needs a non-empty `objectives` list or that traces a `ConstraintResultV1` back to specific evidence.**

## Deferred from: code review of story-3.3 (2026-08-20)

- **`actor_id` and the idempotency scope come from the proposal's author, not the requester.** `enqueue_compute` accepts no requesting-actor parameter; it sets `actor_id = record.created_by_actor_id` and uses that same value as the actor component of the `(site_id, actor_id, operation, idempotency_key)` idempotency scope. Two different planners enqueueing compute for the same proposal therefore share one key namespace: one either receives the other's `(schedule_run_id, job_id)` as a "replay" or gets an `IdempotencyKeyConflictError` for a key it never used, and the job's audit trail names the proposal's author rather than the requester. [backend/application/use_cases/enqueue_compute.py:68] — **Deferred reason: decided by Decision 3**, which states there is no live HTTP actor context at this call boundary because Story 3.6 owns the route, making the proposal's creator the only available identity. **Owner/revisit trigger: Story 3.6**, when the run command supplies a real HTTP actor and idempotency header — that story must thread the requesting actor through `enqueue_compute` rather than inheriting this default.

- **`api/deps.py`'s "single place that establishes trusted site context" invariant is now false and was not corrected.** `backend/worker/lease_worker.py`'s `runtime_context` reproduces the `set_config('app.site_id', ..., true)` + `SET LOCAL ROLE shiftmind_runtime` pattern that `api/deps.py:180-181` documents as "The single place in this repository ... Both callers go through it." The duplication is defensible — importing `api` from `worker` would be worse — but the stale comment now misleads. [backend/api/deps.py:180] — **Deferred reason: `backend/api/**` is a mandated zero-line diff for Story 3.3**, so the comment could not be corrected in this change. **Owner/revisit trigger: the first story permitted to touch `backend/api/deps.py`.**

- **`GRANT shiftmind_lease TO shiftmind_login` gives the API process the ability to lease jobs.** Task 7 asked for the lease role to be "scoped to the worker process only"; that scoping is achieved structurally (only `backend/worker/` calls it) but not at the grant level — `shiftmind_login` is the API's connection role, so any API session can `SET ROLE shiftmind_lease` and call `workflow.lease_next_job`. [backend/migrations/versions/a2b3c4d5e6f7_add_job_queue_and_lease_functions.py:181] — **Deferred reason: unavoidable today**, because the database has exactly one LOGIN role and the worker must reach the lease role through it. **Owner/revisit trigger: the first story that introduces a second LOGIN role, or a security review of role separation (the Deferred table's "Multiple roles and separation of duties" entry, `ARCHITECTURE-SPINE.md:455`).**

- **AD-23 no longer describes the implemented grant set for `workflow.job_queue`.** AD-23 (`ARCHITECTURE-SPINE.md:222-226`) states the internal job queue "grants runtime roles no table access". Story 3.3's Task 2 instead mandates `GRANT SELECT, INSERT` plus a narrow `GRANT UPDATE (status, heartbeat_at)` to `shiftmind_runtime`, which the migration implements. AC2's narrower clause — that the *lease* role cannot directly query tenant/control tables — is genuinely satisfied (`shiftmind_lease` holds zero table grants, proven live). [backend/migrations/versions/a2b3c4d5e6f7_add_job_queue_and_lease_functions.py:120] — **Deferred reason: the implementation correctly follows the story; it is the spine text that is now imprecise**, and amending an architecture decision is not a code review's call. **Owner/revisit trigger: the next amendment pass over `ARCHITECTURE-SPINE.md`, or any story that adds a second Workflow-owned table and must decide the same grant question.**

- **The migration has no backfill, so any `schedule_run` row already in a non-terminal state at upgrade time strands permanently.** `workflow.job_queue` is created empty, so a pre-existing `solver_queued`/`solver_running` run has no job row; `_has_current_epoch` is false for every epoch, and both `mark_running` and `finalize_run` raise `StaleLeaseError` with the misleading text "current is None". [backend/migrations/versions/a2b3c4d5e6f7_add_job_queue_and_lease_functions.py:23] — **Deferred reason: structurally unreachable today** — no route calls `create_run_snapshot` or `execute_schedule_run`, so no deployed database can hold such a row. **Owner/revisit trigger: Story 3.6**, which builds the first route that creates `schedule_run` rows — before that ships to any environment carrying data, the upgrade path needs either a backfill or a documented drain.

- **The queue hot-path index cannot serve the lease query, and `completed` jobs are never pruned.** `ix_job_queue_leaseable` is a plain btree on `(status, lease_expires_at, created_at)`, while `lease_next_job` filters `status = 'queued' OR (status = 'leased' AND lease_expires_at < now())` and orders by `created_at, id` — a disjunction on the leading column with ordering on the third will not produce an index-ordered scan. Nothing archives or deletes `completed` rows, so the scanned relation grows monotonically with lifetime run volume. A partial index on `(created_at, id) WHERE status <> 'completed'` would match the query and stay bounded. [backend/migrations/versions/a2b3c4d5e6f7_add_job_queue_and_lease_functions.py:89] — **Deferred reason: performance only, and invisible at current volumes** (single site, no route enqueueing yet). **Owner/revisit trigger: the first measured lease latency regression, or Story 3.7's monitoring work if it surfaces queue depth.**

- **`assert` guards load-bearing invariants that would let `None` reach the database layer under `python -O`.** `enqueue_compute.py:101,116` and `lease_and_execute_schedule_run.py:49-52` assert non-`None` on `job_id`, `attempt_id`, `schedule_run_id`, and `site_id`; with assertions stripped, those `None`s flow into inserts and lease calls instead of failing fast. [backend/application/use_cases/lease_and_execute_schedule_run.py:49] — **Deferred reason: pre-existing codebase-wide pattern**, established by `create_run_snapshot.py` and the other Story 3.2 use cases; changing it here would fix one instance of a convention while leaving the rest, and the convention itself was never decided against. **Owner/revisit trigger: a deliberate decision to run the backend under `-O`, or a convention pass that replaces contract asserts with raised errors repository-wide.**

- **The AD-6 heartbeat remains inert, but Story 3.4 removed the transaction blocker.** Story 3.4 split worker execution into a short transition transaction and a solve/finalization transaction, so `solver_running` now commits before the solve and is observable to cancellation and future event readers. The heartbeat half remains open: `renew_job_lease` still has no caller, and a fixed `lease_seconds` still spans the solve without renewal. [backend/application/use_cases/lease_and_execute_schedule_run.py:59, backend/adapters/postgres/schedule_run.py:429] — **Deferred reason (Minh, 2026-08-20, D1 option c): Story 3.5 is forced to touch this by its own acceptance criteria, so the remaining debt has a named owner rather than a hope.** Its state machine AC requires monotonic persisted run events (`epics.md:959-962`) and its ceiling AC decides wall-time exhaustion (`epics.md:969-972`), the exact lease-expiry decision the renewal caller must inform. `renew_job_lease` therefore stays as declared dead code until that caller and event path land. **Owner: Story 3.5.**

- **`lease_seconds` is unbound to the solver budget.** It is a bare argument of `run_once` with no setting, no default, and no validation against `solver_wall_time_limit_seconds` (30.0, `settings.py:94`). A deployment choosing a value below the solver budget guarantees every solve is fenced out, rolled back, and repeated. [backend/worker/lease_worker.py:52] — **Deferred reason (Minh, 2026-08-20, D1 option c): Story 3.6's second AC requires positive application-owned ceilings for solver time and total elapsed time, with no ceiling originating from model output (`epics.md:1000-1003`).** 3.6 is also the first story to create `schedule_run` rows through a real route, so it cannot avoid choosing this number. A safe default bound to `solver_wall_time_limit_seconds` lands in Story 3.3 as a review patch, so the window is not left open should 3.6 ship before 3.5. **Owner: Story 3.6.**

- **`workflow.job_queue` has no terminal-failure state, so a job failing between lease and `complete_job` is re-leased forever at the head of the queue.** `JobStatusV1` is `queued|leased|completed` with a matching `ck_job_queue_status`; there is no failure status, no attempt counter, no dead-letter. Reachable paths: missing snapshot (`lease_and_execute_schedule_run.py:59`), `RunSnapshotV1` validation drift (`adapters/postgres/schedule_run.py:238-279`), any `mark_running` status mismatch. Each rolls back, the job stays `leased`, expires, and — because `lease_next_job` orders `ORDER BY q.created_at, q.id` — is re-selected ahead of newer work every cycle. Story 3.4 adds one benign consequence of the same vocabulary gap: cancelling a queued run sets the job's carrier flag but cannot move the job terminal, so one worker must lease it, observe the run is already `solver_cancelled`, and complete the job without solving; if no worker runs, the queued row remains indefinitely. [backend/application/contracts/job_lease.py:12] — **Deferred reason (Minh, 2026-08-20, D2 option 1): Story 3.5's state machine AC names `failed` and `timed-out` as required literal states (`epics.md:959-962`), so 3.5 must reopen the closed `JobStatusV1` vocabulary Decision 1 froze.** Adding a fourth member now would be redesigned there within one or two stories. Story 3.11 is the backstop — it tests worker kill and lease expiry and blocks release on regression (`epics.md:1106-1113`). **Owner: Story 3.5.**

## Deferred from: code review of story-3.4 (2026-08-20)

- **`finished_at` is written from two different clocks on the same column.** `cancel_queued_run` sets `finished_at = func.now()` (database clock, `adapters/postgres/schedule_run.py:87`); `finalize_run` writes `candidate.created_at if candidate else (finished_at or datetime.now(timezone.utc))` (Python clock, `:577`). `_claim_epoch`'s own docstring states the rule the new path follows — "writes the clock from the database so the column has one time source" — so the deviating side is the older one. Under clock skew, ordering `finished_at` across cancelled-while-queued and cancelled-while-running rows can show a run finishing before it started. [backend/adapters/postgres/schedule_run.py:577] — deferred, pre-existing.

- **`getattr(result, "rowcount", 1) != 1` defaults a missing rowcount to success.** Every compare-and-set guard in the repository fails *open* against a result object that does not report `rowcount` — a stubbed connection or a driver that omits it. Occurrences: `:99` (new, both cancellation edges), `:207`, `:261`, `:441`, `:583`. The guards that make the cancellation edges safe are the ones that silently report success on a zero-row UPDATE. [backend/adapters/postgres/schedule_run.py:99] — deferred, pre-existing pattern; the new code follows the established convention rather than introducing it.

- **`ScheduleRunOut.created_at` and `finished_at` are published but never populated.** Task 4 specified both fields; `_out()` omits them, so `frontend/openapi.json` and `frontend/src/api/schema.d.ts` publish two fields that are always `null`. An in-file comment scopes them to Story 3.7, which owns the Runs workspace that renders them. [backend/api/routers/schedule_runs.py:43] — deferred, deliberate scope-down. **Owner: Story 3.7.**

- **A `solver_running` cancellation with no `job_queue` row parks the run non-terminal with no observer.** `cancel_schedule_run` writes the non-terminal `cancellation_requested` edge without verifying a job row exists, and `set_job_cancellation_requested` tolerates zero rows by design (Decision 4: "no job row is not an error"). Only a worker at Checkpoint 1 or 2 resolves `cancellation_requested` to a terminal, so such a run never reaches a terminal and `finished_at` stays NULL forever — after the planner was told `200`. Safe today only incidentally: `mark_running`'s `_has_current_epoch` requires a leased job row, so `solver_running` implies a job exists. That safety is a property of `mark_running`, not an assertion of the command. [backend/application/use_cases/cancel_schedule_run.py:130] — deferred; belongs with Story 3.5's orphan and lease-expiry handling. **Owner: Story 3.5.**

- **`execute_schedule_run`'s "must already be `solver_running`" precondition now lives only in a docstring.** Decision 6 correctly removed its `mark_running` call, but no replacement precondition was added at the use case's own boundary — the only remaining status guard is `finalize_run`'s predicate, which fires *after* the solve. Any caller that forgets Transaction A burns a full CP-SAT solve before the failure surfaces. [backend/application/use_cases/execute_schedule_run.py:38] — deferred, no live consequence (the sole caller is `lease_and_execute_schedule_run`).

- **AD-13's correlation ID and resource ID are absent from RFC 7807 problem responses.** `api/problems.py` accepts only `status`, `code`, `title`, `detail`. The new `_command_problem` carries the stable code and the current version in prose, but neither a correlation ID nor a resource ID. AD-13 requires all four. This is the repo-wide shape and Story 3.4 was explicitly instructed to copy `api/routers/proposals.py`'s structure, which is identical. [backend/api/problems.py:7] — deferred, pre-existing and currently unowned.
