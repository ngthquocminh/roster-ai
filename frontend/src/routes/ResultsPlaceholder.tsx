import { PlaceholderView } from "@/components/layout/PlaceholderView";

/** Mounts at `/scenarios/:scenarioId/runs/:runId`. */
export function ResultsPlaceholder() {
  return <PlaceholderView viewName="Results" />;
}
