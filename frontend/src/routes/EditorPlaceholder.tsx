import { PlaceholderView } from "@/components/layout/PlaceholderView";

/** Mounts at the ScenarioLayout index route (`/scenarios/:scenarioId`). */
export function EditorPlaceholder() {
  return <PlaceholderView viewName="Editor" />;
}
