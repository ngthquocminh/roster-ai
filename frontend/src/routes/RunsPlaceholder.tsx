import { PlaceholderView } from "@/components/layout/PlaceholderView";

/** Mounts at `/scenarios/:scenarioId/runs`. */
export function RunsPlaceholder() {
  return <PlaceholderView viewName="Runs" />;
}
