---
phase: 01-first-nl-constraint-end-to-end
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - backend/services/run_service.py
  - backend/tests/test_constraints_api.py
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "run_service._execute reads the scenario's overrides JSON into SolverConfig.overrides before solving."
    - "The full NL -> override -> re-solve round trip passes in CI with zero network calls (stub-driven)."
    - "A re-solved scenario carrying the override yields a schedule that visibly honors it; an unchanged scenario re-solves to the baseline."
  artifacts:
    - "backend/services/run_service.py — overrides threaded from scenario JSON into SolverConfig."
    - "backend/tests/test_constraints_api.py — end-to-end stub-driven round-trip test."
  key_links:
    - "scenario['overrides'] JSON (dict keyed by id) -> list[OverrideCall] -> SolverConfig.overrides -> engine."
    - "app.dependency_overrides[get_llm_provider] = StubLLMProvider (mirrors get_engine override) for zero-network CI."
---

<objective>
Close the loop: thread each scenario's stored overrides into `SolverConfig` at solve time,
then prove the complete vertical slice end to end — create scenario, POST one plain-English
constraint, trigger a run, and observe the resulting schedule honoring the override — all
stub-driven with no network calls (TEST-02, ROADMAP success criterion 4 round trip).

Purpose: This is the true integration that makes the phase goal observably TRUE from the
user's vantage point. It joins the engine half (plan 01-01) and the parse/store half
(plan 01-02) at the existing async run lifecycle.

Output: the `run_service._execute` overrides wiring (Claude's-discretion item per CONTEXT
lines 96-98) and the end-to-end API test that exercises NL -> persisted override -> re-solve.

User story (phase goal): **As a** workforce scheduler, **I want to** state a staffing
floor in plain English and re-solve, **so that** I get a schedule that honors it without
editing solver code or JSON. This plan proves that end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-nl-constraint-end-to-end/01-CONTEXT.md
@.planning/phases/01-first-nl-constraint-end-to-end/01-PATTERNS.md

# Live seams this plan modifies / mirrors (read before editing):
@backend/services/run_service.py
@backend/tests/test_api.py
@backend/domain/overrides.py
@backend/engine/base.py
@backend/llm/stub.py
</context>

<artifacts>
This plan produces (depends on plans 01-01 and 01-02):
- overrides-threading in `_execute` — `backend/services/run_service.py`
- `backend/tests/test_constraints_api.py` — full stub-driven round-trip test
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Thread stored overrides into SolverConfig in run_service._execute</name>
  <files>backend/services/run_service.py</files>
  <read_first>
    - 01-PATTERNS.md "backend/services/run_service.py (MODIFIED)" section — the exact dict-keyed JSON -> list[OverrideCall] -> SolverConfig wiring.
    - backend/services/run_service.py — `_execute` (lines 74-94), the `SolverConfig(time_limit_s=...)` construction (line 84), and the existing `import json` (line 11).
    - backend/domain/overrides.py — `OverrideCall` (from plan 01-01).
    - backend/engine/base.py — `SolverConfig.overrides` (from plan 01-01).
  </read_first>
  <action>
    In `backend/services/run_service.py`, add `from domain.overrides import OverrideCall` and, inside
    `_execute`, before constructing `SolverConfig`, parse the scenario's overrides:
    `raw = json.loads(scenario["overrides"] or "{}")` (the dict-keyed store, D-04), then build
    `overrides = [OverrideCall(id=k, tool=v["tool"], args=v["args"]) for k, v in raw.items()]`. Pass it into
    the config: `config = SolverConfig(time_limit_s=float(scenario["time_limit_s"]), overrides=overrides)`.
    `scenario` is the row dict from `ScenarioRepo.get` (includes the `overrides` column); an empty store
    yields `[]`, preserving the identical-to-baseline path (ENG-06). `json` is already imported. Keep this
    defensive: a malformed override entry surfaces as an exception caught by the existing
    `except Exception` in `_execute` (run marked FAILED) rather than crashing the worker.
  </action>
  <verify>
    <automated>cd backend && python -c "import inspect, services.run_service as r; src=inspect.getsource(r._execute); assert 'overrides' in src and 'OverrideCall' in src and 'SolverConfig(' in src; print('ok')"</automated>
    <automated>cd backend && python -m pytest tests/test_api.py -x -q</automated>
  </verify>
  <done>
    `_execute` reads `scenario["overrides"]` into `SolverConfig.overrides`; existing API tests
    (no-override path) still pass unchanged.
  </done>
  <acceptance_criteria>
    - Overrides JSON is parsed into `list[OverrideCall]` and attached to `SolverConfig` (no solve-signature change, ENG-02).
    - An empty/absent overrides store yields `[]` -> baseline solve (ENG-06).
    - Malformed override data is handled via the existing `_execute` failure path, not an unhandled crash.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end stub-driven NL -> override -> re-solve round-trip test</name>
  <files>backend/tests/test_constraints_api.py</files>
  <read_first>
    - 01-PATTERNS.md "backend/tests/test_constraints_api.py (NEW)" section and the "Dependency-injection seam swapped in tests" shared-pattern entry.
    - backend/tests/test_api.py — the whole fixture: `_DATA_DIR` constant (line 24), env set BEFORE importing `api.deps`/`api.main`, `StubEngine` (lines 27-43), `app.dependency_overrides[get_engine]` (line 55), and the `_wait_terminal` poll helper (lines 61-68) for the async run.
    - backend/llm/stub.py — `StubLLMProvider` (the production stub is deterministic; reuse it via `dependency_overrides[get_llm_provider]`).
    - data/sample_tiny_input.json — the committed fixture; its tasks carry GUID ids and human names (e.g. a task named like "C Pick | Picking chill 080"). The stub emits token "Pick"; constraint_service resolves it to the real GUID id (plan 01-02). Pick a task NAME substring present in this fixture that uniquely resolves, and phrase the test text accordingly (e.g. "at least 2 on Pick" if "Pick" is unique; otherwise choose a unique token from the fixture).
  </read_first>
  <behavior>
    - create scenario (fixture `sample_tiny_input.json`) -> `POST /constraints` with {scenario_id, text} -> 200, response has `tool=="set_min_workers_per_task"`, `args["task_id"]` is the resolved real id, `args["n"]==2`, `id` startswith "ov_", and a non-empty `parsed_constraint`.
    - re-fetching the scenario (or a second identical POST) shows the override stored once under its content-hash id (idempotent).
    - trigger a run for the scenario -> poll to terminal (COMPLETED) -> the solve runs through the REAL engine (so the penalty actually applies; do NOT stub the engine for the honoring assertion) and the result schedule reflects the floor at demanded hours, OR (if asserting on metrics is more robust for the full fixture) the run completes COMPLETED with the override present in config. At minimum assert: 200 + persisted override + run reaches COMPLETED with zero network calls.
    - a control scenario with NO constraint posted re-solves to the same baseline result (no regression at the API level).
  </behavior>
  <action>
    Create `backend/tests/test_constraints_api.py` by copying the fixture idiom from `tests/test_api.py`:
    set `ROSTERAI_DB`/`ROSTERAI_DATA_DIR` env BEFORE importing `api.deps`/`api.main`, reuse `_DATA_DIR`,
    and register `app.dependency_overrides[get_llm_provider] = lambda: StubLLMProvider()` (the NEW seam,
    TEST-01/TEST-02 — no live API in CI). For the honoring assertion, solve through the REAL engine (do NOT
    override `get_engine`) so the penalty is genuinely exercised; reuse the `_wait_terminal` poll helper for
    the async run and give the run a small `time_limit_s`.

    Write the end-to-end test: POST `/scenarios` (fixture `sample_tiny_input.json`) -> POST `/constraints`
    with `{"scenario_id": <id>, "text": <demo sentence resolving to a unique fixture task>}` -> assert 200 and
    the echoed override (tool, resolved `args`, `id` prefix, `parsed_constraint`). Then trigger a run
    (`POST /scenarios/{id}/runs` or the existing run-trigger path) and poll to a terminal state; assert
    COMPLETED. Where feasible from the run result, assert the schedule places >= 2 bodies on the overridden
    task at demanded hours (the "visibly honored" criterion); if the full fixture makes an exact body-count
    assertion brittle, fall back to asserting the override was carried into the solve (run COMPLETED with the
    override present) and rely on plan 01-01's engine test for the strict honoring proof. Add a second test
    that runs a scenario WITHOUT any posted constraint and asserts it reaches COMPLETED (baseline path
    unaffected). Confirm no network egress occurs (the stub is pure-Python; assert by construction — no
    Anthropic client is imported in the stub path).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_constraints_api.py -x -q</automated>
    <automated>cd backend && python -m pytest -q</automated>
  </verify>
  <done>
    The round-trip test passes: scenario created, constraint POSTed (200 + echoed override), run reaches
    COMPLETED with the override applied, and the no-constraint control also completes — all with zero network
    calls. The full backend test suite passes.
  </done>
  <acceptance_criteria>
    - The test drives the real `StubLLMProvider` via `dependency_overrides[get_llm_provider]` — no live LLM API (TEST-01/TEST-02).
    - The honoring path solves through the real CP-SAT engine (engine not stubbed for that assertion).
    - 200 response echoes the stored override (resolved args, content-hash id, parsed_constraint).
    - A no-constraint control scenario completes normally (baseline preserved, ENG-06 at the API level).
    - Whole suite (`python -m pytest -q`) is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored overrides JSON -> run_service -> SolverConfig | Persisted override data is deserialized and fed to the solver in a worker thread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-R1 | Tampering | `_execute` deserializing `scenario["overrides"]` | medium | mitigate | `json.loads(... or "{}")` tolerates empty/missing values; a malformed entry raises inside the existing `try/except` in `_execute`, marking the run FAILED rather than crashing the worker pool. Only validated overrides reach the DB (plan 01-02), so well-formed data is the norm. |
| T-01-R2 | Denial of Service | override -> SolverConfig -> engine | high | mitigate | Overrides apply strictly as the bounded soft round-2 penalty built in plan 01-01; they cannot make the solve infeasible or unbounded regardless of stored content. |
| T-01-SC | Tampering | dependency installs | low | accept | No new package installs in this plan (stdlib + existing test deps `pytest`/`httpx`); no `[ASSUMED]`/`[SUS]` packages. Real Claude SDK deferred to Phase 4. |
</threat_model>

<verification>
- `cd backend && python -m pytest -q` passes (whole suite, including the new round-trip test).
- The new test imports `StubLLMProvider` and overrides `get_llm_provider`; no Anthropic/network client is imported on the tested path.
</verification>

<success_criteria>
- ROADMAP success criterion 4 (round trip): the full NL -> override -> re-solve path passes in CI with zero network calls.
- ROADMAP success criterion 2 (end-to-end): a re-solved scenario carrying the override yields a schedule that honors it.
- TEST-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-first-nl-constraint-end-to-end/01-03-SUMMARY.md` when done.
</output>
