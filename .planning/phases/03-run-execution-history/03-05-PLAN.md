---
phase: 03-run-execution-history
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - frontend/src/routes/RunHistory.tsx
  - frontend/src/routes/RunHistory.test.tsx
  - frontend/src/App.tsx
  - frontend/src/routes/router.test.tsx
  - frontend/src/routes/RunsPlaceholder.tsx
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04, RUN-05]
must_haves:
  truths:
    - "The Run History view replaces RunsPlaceholder at /scenarios/:scenarioId/runs and owns a single useRuns + useTriggerRun pair, deriving the table rows, the in-flight panel's newest-active run, and the button's disabled state from ONE list response (RUN-01..RUN-04)."
    - "Triggering a run makes it appear immediately as PENDING at the top of the list via the useTriggerRun to ['runs', id] invalidation, before any poll tick (RUN-01)."
    - "A user watches a run advance PENDING to RUNNING to COMPLETED/FAILED without manually refreshing, driven by the useRuns poll that stops when terminal (RUN-02)."
    - "While in flight the user sees the honest multi-minute / cannot-be-cancelled panel and no cancel or progress-to-completion affordance (RUN-03)."
    - "Prior runs for the scenario are visible with status and created/started/finished timing (RUN-04)."
    - "A FAILED run shows its recorded error text rather than appearing merely absent or permanently stuck (RUN-05)."
    - "The view header shows 'Run History' as a Heading-role (20px/600) title with the 'Run Scenario' CTA anchored top-right, mirroring Home's header-button placement."
    - statement: "A deep link to a nonexistent :scenarioId's Runs tab renders the ordinary empty state (GET /scenarios/{id}/runs does not 404), not a 'Scenario not found' gate"
      verification: backstop
  prohibitions:
    - "A cancel affordance must not exist anywhere in the Run History view."
    - "The ScenarioLayout 'Results' tab must not be enabled by this phase, and no ResultsView content is built."
  artifacts:
    - frontend/src/routes/RunHistory.tsx
    - frontend/src/routes/RunHistory.test.tsx
  key_links:
    - "App.tsx routes: the 'runs' child Component swaps RunsPlaceholder to RunHistory; the pre-existing 'runs/:runId' child stays ResultsPlaceholder (Phase-4 target) — no new route is added and no route is removed."
    - "RunHistory passes the single runsQuery into RunHistoryTable, the derived newestActiveRun into RunInFlightPanel, and the derived runInProgress + isLoadingList + isPending + error into TriggerRunButton — one useRuns response drives all three children."
---

<objective>
Compose the Run History view: one useRuns + one useTriggerRun, wired to TriggerRunButton (header CTA), RunInFlightPanel (honest wait), and RunHistoryTable (prior runs). Swap it in for RunsPlaceholder at /scenarios/:scenarioId/runs and retire the placeholder — the point where all of RUN-01..RUN-05 become true end to end in the browser.

Purpose: Make the phase goal ("trigger a solve and follow it to a terminal state without leaving the browser") literally true by driving the panel, table, and button from a single polling list response, while holding the scope guards: no Results-tab activation, no ResultsView content, no cancel path, no backend change.
Output: frontend/src/routes/RunHistory.tsx (+ test); App.tsx route swap; router.test.tsx update; RunsPlaceholder.tsx retired.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-run-execution-history/03-UI-SPEC.md
@frontend/src/routes/Editor.tsx
@frontend/src/routes/Home.tsx
@frontend/src/routes/ScenarioLayout.tsx
@frontend/src/routes/RunsPlaceholder.tsx
@frontend/src/routes/ResultsPlaceholder.tsx
@frontend/src/App.tsx
@frontend/src/routes/router.test.tsx
@frontend/src/hooks/useRuns.ts
@frontend/src/hooks/useTriggerRun.ts
@frontend/src/lib/runStatus.ts
@frontend/src/components/runs/RunHistoryTable.tsx
@frontend/src/components/runs/RunInFlightPanel.tsx
@frontend/src/components/runs/TriggerRunButton.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunHistory view composition + App.tsx route swap</name>
  <files>frontend/src/routes/RunHistory.tsx, frontend/src/routes/RunHistory.test.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/routes/Editor.tsx (the composed-route pattern: call the hook once, derive state, render the fixed vertical region order in a mx-auto max-w-5xl flex flex-col gap-4 px-6 py-8 container — the exact layout shell to mirror)
    - frontend/src/routes/Home.tsx (the header row: a flex items-center justify-between with the title on the left and the primary CTA top-right — the placement precedent for "Run Scenario")
    - frontend/src/hooks/useRuns.ts and useTriggerRun.ts (the two hooks this view owns)
    - frontend/src/lib/runStatus.ts (hasActiveRun / newestActiveRun to derive runInProgress and the panel's run)
    - frontend/src/components/runs/RunHistoryTable.tsx, RunInFlightPanel.tsx, TriggerRunButton.tsx (the three children and their exact prop shapes)
    - frontend/src/App.tsx (the routes array; the 'runs' child currently Component: RunsPlaceholder — the single line to swap; the 'runs/:runId' child stays ResultsPlaceholder)
    - .planning/phases/03-run-execution-history/03-UI-SPEC.md (Application Structure vertical layout: header row, conditional in-flight panel, run history table; Data strategy: one list query drives panel + table; button behavior)
  </read_first>
  <behavior>
    - RunHistory reads scenarioId from the route, calls useRuns(scenarioId) once and useTriggerRun(scenarioId) once.
    - It derives runs = data ?? [], runInProgress = hasActiveRun(runs), activeRun = newestActiveRun(runs).
    - Header: an h2 "Run History" (20px/600) on the left, TriggerRunButton on the right.
    - TriggerRunButton receives onTrigger = () => trigger.mutate(), isLoadingList = runsQuery.isLoading, runInProgress, isPending = trigger.isPending, error = trigger.error.
    - RunInFlightPanel receives run = activeRun (null when no active run → renders nothing).
    - RunHistoryTable receives runsQuery, scenarioId, and onTriggerRun = () => trigger.mutate().
    - App.tsx routes: the 'runs' child renders RunHistory (not RunsPlaceholder); the 'runs/:runId' child still renders ResultsPlaceholder.
  </behavior>
  <action>
    Create frontend/src/routes/RunHistory.tsx exporting `RunHistory()`. Read `scenarioId` via useParams (cast as string like Editor does). Call `const runsQuery = useRuns(scenarioId)` and `const trigger = useTriggerRun(scenarioId)` exactly once each. Derive `const runs = runsQuery.data ?? []`, `const runInProgress = hasActiveRun(runs)`, `const activeRun = newestActiveRun(runs)` (imports from @/lib/runStatus). Render a container `div` mirroring Editor's `mx-auto flex max-w-5xl flex-col gap-4 px-6 py-8`. First child: a header row `div` `flex items-center justify-between` with `<h2 className="text-[20px] leading-[1.2] font-semibold">Run History</h2>` on the left and `<TriggerRunButton onTrigger={() => trigger.mutate()} isLoadingList={runsQuery.isLoading} runInProgress={runInProgress} isPending={trigger.isPending} error={trigger.error} />` on the right. Second child: `<RunInFlightPanel run={activeRun} />` (it self-hides when null). Third child: `<RunHistoryTable runsQuery={runsQuery} scenarioId={scenarioId} onTriggerRun={() => trigger.mutate()} />`. Do NOT add any cancel control, do NOT render solver_status, do NOT add a scenario-existence 404 gate (unlike Editor — the list endpoint does not 404, so a bogus scenarioId must fall through to the table's ordinary empty state; this is the E4 backstop behavior). Then edit frontend/src/App.tsx: replace the `RunsPlaceholder` import with `RunHistory` (from @/routes/RunHistory) and change the `{ path: "runs", Component: RunsPlaceholder }` child to `Component: RunHistory`; leave the `{ path: "runs/:runId", Component: ResultsPlaceholder }` child and every other route unchanged. Create frontend/src/routes/RunHistory.test.tsx (mock @/hooks/useRuns and @/hooks/useTriggerRun at the module boundary; render inside a memory router) covering: the "Run History" heading + a "Run Scenario" button render; an active RUNNING run shows the in-flight panel; an all-terminal list shows no panel; the table renders prior runs; and clicking the enabled button calls the trigger mutation. Keep the copy assertions consistent with the child components' own tests (do not re-assert their internals here).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/routes/RunHistory.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RunHistory calls useRuns and useTriggerRun exactly once each and passes the single runsQuery into RunHistoryTable, activeRun (via newestActiveRun) into RunInFlightPanel, and runInProgress/isLoadingList/isPending/error into TriggerRunButton (test asserts the composition renders and wires the trigger).
    - The header renders an h2 "Run History" and a "Run Scenario" button; clicking it invokes the mocked trigger mutation (test asserts).
    - A RUNNING newest-active run renders the in-flight panel; an all-terminal list renders no panel (tests assert both).
    - App.tsx's 'runs' route Component is RunHistory and the 'runs/:runId' route Component is still ResultsPlaceholder (verified by router.test.tsx in Task 2).
    - RunHistory contains no cancel control, no solver_status reference, and no error.status === 404 scenario-existence gate.
    - `cd frontend && npx vitest run src/routes/RunHistory.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The Run History view drives the panel, table, and button from one useRuns response and is mounted at the /runs route in place of RunsPlaceholder, with no cancel path and no scenario-existence gate.</done>
</task>

<task type="auto">
  <name>Task 2: Update router test + retire RunsPlaceholder (scope guards)</name>
  <files>frontend/src/routes/router.test.tsx, frontend/src/routes/RunsPlaceholder.tsx</files>
  <read_first>
    - frontend/src/routes/router.test.tsx (the SHELL-03 four-route shell tests; the two assertions tied to the Runs placeholder — "Runs — not built yet" heading and "This view ships in a later phase…" — that now describe the real view instead)
    - frontend/src/routes/ScenarioLayout.tsx (confirm the disabled Results tab and the runs/:runId match logic are untouched by this phase)
    - frontend/src/routes/ResultsPlaceholder.tsx (still the /runs/:runId target in Phase 3)
    - frontend/src/routes/RunHistory.tsx (the real view the /runs route now mounts)
  </read_first>
  <action>
    Update frontend/src/routes/router.test.tsx: the test that mounts /scenarios/:scenarioId/runs must now assert the real RunHistory view renders (e.g. the "Run History" heading and/or the "Run Scenario" button) instead of the retired placeholder's "Runs — not built yet" / "This view ships in a later phase…" copy — because router.test builds its router from the shipped `routes` and the /runs route now mounts RunHistory (its useRuns fetch has nowhere to resolve in jsdom, so target the synchronous initial render, matching how the existing Editor deep-link test targets useScenario's isLoading state). Keep unchanged: the /scenarios/:scenarioId/runs/:runId test still asserts the ResultsPlaceholder "Results — not built yet" heading (Phase 4 owns that route); the tab-nav tests still assert the Runs tab active on /runs, the Editor tab active on the index, and the DISABLED Results tab (still a disabled button, aria-current only on the runs/:runId route). Then delete frontend/src/routes/RunsPlaceholder.tsx (retired — nothing imports it after Task 1's App.tsx edit). Do NOT modify ScenarioLayout.tsx (the Results tab stays disabled — enabling it is Phase 4), do NOT modify the runs/:runId route, and do NOT touch any backend file.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/routes/router.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - router.test.tsx's /scenarios/:scenarioId/runs assertion targets the real RunHistory view (no remaining assertion on "Runs — not built yet" or "This view ships in a later phase…").
    - The /scenarios/:scenarioId/runs/:runId test still asserts the ResultsPlaceholder "Results — not built yet" heading (Phase-4 route unchanged).
    - The Results tab remains a disabled button (test still asserts it is disabled / aria-current only on the runs/:runId path); ScenarioLayout.tsx is unmodified in this phase's diff.
    - frontend/src/routes/RunsPlaceholder.tsx no longer exists and nothing imports it (typecheck passes).
    - No backend file appears in this phase's diff.
    - `cd frontend && npx vitest run src/routes/router.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The router test reflects the real /runs view, the placeholder is retired, and the scope guards (disabled Results tab, unchanged runs/:runId route, no backend change) are asserted.</done>
</task>

</tasks>

<flagged_assumptions>
- The `/scenarios/:scenarioId/runs/:runId` route is NOT added by this phase — it already exists in App.tsx (mounting ResultsPlaceholder from Phase 1). RunHistoryTable's row navigation targets that pre-existing route; Phase 3 changes only which component the `runs` (list) route mounts. This corrects the planning-context phrasing "the new /scenarios/:scenarioId/runs/:runId route" — no new route is created, so none is asserted as newly added.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-typed / bookmarked URL → Runs view | a user can deep-link to /scenarios/:anyId/runs with an arbitrary (possibly nonexistent) scenarioId. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-09 | Tampering / injection | deep-link scenarioId → useRuns → listRuns | low | mitigate | scenarioId flows only into a read-only GET as a typed, URL-encoded openapi-fetch path param; no write, no injection surface. A nonexistent scenarioId returns an empty list (the endpoint does not 404), so the view renders the ordinary empty state rather than a gate — the E4 backstop, verified against runs.py. |
| T-3-10 | Elevation / scope | Results tab + runs/:runId route | low | mitigate | This phase must not enable the disabled Results tab or build ResultsView content; router.test asserts the Results tab stays disabled and the runs/:runId route still mounts ResultsPlaceholder, preventing accidental scope creep into Phase 4. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/routes/RunHistory.test.tsx src/routes/router.test.tsx` exits 0.
- `cd frontend && npm run test` (full suite) exits 0 — no regression in the Phase 1/2 route/nav tests.
- `cd frontend && npm run typecheck` passes (RunsPlaceholder fully removed, no dangling import).
- Human check (end-of-phase, human_verify_mode=end-of-phase):
  <human-check>Start the backend and `npm run dev`. Open a scenario's Runs tab. Click "Run Scenario": confirm a run appears immediately as "Queued" without a manual refresh (RUN-01), that the button then disables with "A run is already in progress for this scenario.", and that the in-flight panel shows the honest "Solving…" / multi-minute / cannot-be-cancelled copy with NO cancel button and NO progress bar (RUN-03). Watch the run advance to "Completed" on its own without refreshing (RUN-02). Reload mid-flight and confirm state resumes from polling (durability). Confirm prior runs list with created/started/finished timing (RUN-04). Deep-link to /scenarios/<bogus-id>/runs and confirm the ordinary "No runs yet" empty state renders, not a "Scenario not found" gate (E4 backstop). If a FAILED run is available, confirm its error text shows inline and wraps without breaking the table columns (RUN-05 + long-text backstop).</human-check>
</verification>

<success_criteria>
- All of RUN-01..RUN-05 are true end to end in the browser from the Run History view.
- One useRuns response drives the panel, table, and button.
- Scope guards hold: no Results-tab activation, no ResultsView content, no cancel affordance, no backend change.
</success_criteria>

<artifacts_produced>
New files: frontend/src/routes/RunHistory.tsx (+ test). New export: RunHistory().
Modified: frontend/src/App.tsx (runs route Component RunsPlaceholder → RunHistory), frontend/src/routes/router.test.tsx (runs-route assertions updated).
Removed: frontend/src/routes/RunsPlaceholder.tsx (retired).
Unchanged (scope guard): ScenarioLayout.tsx (Results tab stays disabled), the runs/:runId route (still ResultsPlaceholder), all backend files.
</artifacts_produced>

<output>
Create `.planning/phases/03-run-execution-history/03-05-SUMMARY.md` when done.
</output>
