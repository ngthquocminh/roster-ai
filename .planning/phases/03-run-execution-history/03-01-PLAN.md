---
phase: 03-run-execution-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/runs.ts
  - frontend/src/api/runs.test.ts
  - frontend/src/lib/runStatus.ts
  - frontend/src/lib/runStatus.test.ts
autonomous: true
requirements: [RUN-01, RUN-02, RUN-04]
must_haves:
  truths:
    - "listRuns(scenarioId) issues GET /scenarios/{scenario_id}/runs through the single ./client instance and returns the typed RunOut[]; RunOut is derived from the generated schema, never hand-authored."
    - "triggerRun(scenarioId) issues POST /scenarios/{scenario_id}/runs and returns the created PENDING RunOut; on a non-2xx it throws { status: response.status, ...error } so callers can branch on 404 vs other (T-1-02 convention, same as createScenario)."
    - "runStatusMeta(status) maps PENDING to Clock + 'Queued' + muted-foreground, RUNNING to LoaderCircle + 'Running' + muted-foreground + spin, COMPLETED to Check + 'Completed' + foreground (neutral), FAILED to X + 'Failed' + destructive, and falls back to a neutral Clock + raw-status treatment for any unrecognized status with no fabricated label."
    - "isTerminalStatus(status) returns true only for COMPLETED and FAILED; hasActiveRun(runs) is true iff any run is non-terminal; newestActiveRun(runs) returns the first non-terminal run in the server's newest-first list or null."
    - "Run-status helpers never re-order runs: newestActiveRun relies on the server's newest-first ordering and returns the first non-terminal element, remaining stable when created_at values tie."
  artifacts:
    - frontend/src/api/runs.ts
    - frontend/src/api/runs.test.ts
    - frontend/src/lib/runStatus.ts
    - frontend/src/lib/runStatus.test.ts
  key_links:
    - "runs.ts routes exclusively through the single ./client openapi-fetch instance — no second client is constructed."
    - "runStatus.ts is the single source of run-status vocabulary (label + icon + color AND the terminal/active predicates) that the hooks, table, panel, and button all import."
---

<objective>
Build the two dependency-free foundations every later plan imports: the typed run API wrappers (RUN-01 trigger, RUN-02/RUN-04 list-poll surface) and the run-status vocabulary module (labels, icons, colors, and the terminal/active predicates that drive both polling and rendering).

Purpose: Centralize the run-status truth in one pure, tested module so the visual mapping and the polling-stop logic can never drift apart, and expose the exact two endpoints this phase's UI calls — mirroring the thin-wrapper convention established by api/scenarios.ts and api/constraints.ts.
Output: frontend/src/api/runs.ts (+ test) and frontend/src/lib/runStatus.ts (+ test).
</objective>

<source_coverage_audit>
Sources for this phase (RESEARCH.md and CONTEXT.md were both skipped this run — has_research=false, has_context=false — so those source types are N/A; the authoritative design source is 03-UI-SPEC.md):
- GOAL (ROADMAP Phase 3): "trigger a solve and follow it to a terminal state without leaving the browser" → COVERED across plans 03-01..03-05.
- REQ: RUN-01 (03-01/03-02/03-04/03-05), RUN-02 (03-01/03-02/03-05), RUN-03 (03-04/03-05), RUN-04 (03-01/03-03/03-05), RUN-05 (03-03/03-05). Every ID covered.
- UI-SPEC: Application Structure, Copywriting Contract, status icon/color map, and all 19 ✅-covered + 2 🧪-backstop UI Considerations lifted into must_haves across 03-03/03-04/03-05. No ⛔-dismissed rows lifted (their reason is the audit trail).
No unplanned source items. No PHASE SPLIT needed (frontend feature well within context budget).
</source_coverage_audit>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-run-execution-history/03-UI-SPEC.md
@backend/api/routers/runs.py
@frontend/src/api/scenarios.ts
@frontend/src/api/constraints.ts
@frontend/src/api/client.ts
@frontend/src/api/schema.d.ts
@frontend/src/lib/toolLabels.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typed run API wrappers (runs.ts)</name>
  <files>frontend/src/api/runs.ts, frontend/src/api/runs.test.ts</files>
  <read_first>
    - frontend/src/api/scenarios.ts (the thin-wrapper convention: derive types via indexed access into generated `paths`; throw `{ status: response.status, ...error }` on non-2xx so callers can branch — the exact T-1-02 pattern to replicate)
    - frontend/src/api/constraints.ts (single-endpoint wrapper shape and the boundary-mock test convention it is tested with)
    - frontend/src/api/client.ts (the single `client` instance to import — never construct a second)
    - frontend/src/api/schema.d.ts (the generated RunOut schema at lines ~264-282, and the paths "/scenarios/{scenario_id}/runs" POST/GET entries)
    - backend/api/routers/runs.py (the live contract: POST returns 201 + RunOut or 404; GET returns list[RunOut] and does NOT existence-check scenario_id)
  </read_first>
  <behavior>
    - listRuns("s1") calls client.GET("/scenarios/{scenario_id}/runs", { params: { path: { scenario_id: "s1" } } }) and returns the data array on success.
    - triggerRun("s1") calls client.POST("/scenarios/{scenario_id}/runs", ...) and returns the created RunOut on success (201).
    - When the underlying client returns an error, both functions throw an object carrying response.status (e.g. { status: 404, ... }) so a caller can branch on 404.
    - Neither function is given a getRun/per-run wrapper: this phase polls only the list endpoint (UI-SPEC Data strategy) and the run-detail route lands on the Phase-4 ResultsPlaceholder.
  </behavior>
  <action>
    Create frontend/src/api/runs.ts with exactly two exported async functions, mirroring api/scenarios.ts verbatim in shape: `listRuns(scenarioId: string)` doing `client.GET("/scenarios/{scenario_id}/runs", { params: { path: { scenario_id: scenarioId } } })`, and `triggerRun(scenarioId: string)` doing `client.POST("/scenarios/{scenario_id}/runs", { params: { path: { scenario_id: scenarioId } } })`. Import the shared `client` from ./client. On the destructured `error` from each call, throw `{ status: response.status, ...error }` (T-1-02) so RUN-01's 404-branch is possible downstream; return `data` on success. Do NOT hand-author a RunOut interface — if a payload type is needed, derive it via indexed access into `paths` from ./schema (per the scenarios.ts header note). Do NOT add a getRun wrapper or any other endpoint — keep the module thin (only the two endpoints RUN-01/RUN-02/RUN-04 use). Create frontend/src/api/runs.test.ts mirroring the constraints/scenarios boundary-mock style: `vi.mock("./client")`, assert each function calls the right path with the right params, returns data on success, and throws with the attached status on error.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/api/runs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/api/runs.ts exports listRuns and triggerRun and imports `client` from "./client" (no `createClient` call in this file).
    - runs.test.ts asserts listRuns issues GET on "/scenarios/{scenario_id}/runs" with path param scenario_id, and returns the resolved data array.
    - runs.test.ts asserts triggerRun issues POST on "/scenarios/{scenario_id}/runs" and returns the created RunOut.
    - runs.test.ts asserts an error response causes a throw whose `status` equals the response status (a 404 case is included).
    - No hand-authored `interface RunOut` / `type RunOut = { ... }` field list exists in runs.ts; any type is derived from generated `paths`/`components`.
    - `cd frontend && npx vitest run src/api/runs.test.ts` exits 0.
  </acceptance_criteria>
  <done>listRuns and triggerRun wrap the two run endpoints through the shared client, attach response.status on failure, and are covered by a green boundary-mock test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run-status vocabulary + terminal predicates (runStatus.ts)</name>
  <files>frontend/src/lib/runStatus.ts, frontend/src/lib/runStatus.test.ts</files>
  <read_first>
    - frontend/src/lib/toolLabels.ts (the fixed lookup-map + safe-fallback convention to mirror: `Record<string, ...>` plus an accessor that falls back to the raw value, never a fabricated one)
    - frontend/src/components/editor/TranscriptEntry.tsx (the established Check / X + neutral-vs-destructive icon convention this map extends — no Badge)
    - frontend/src/api/schema.d.ts (RunOut shape: status is a string; started_at/finished_at/error/solver_status are nullable)
    - .planning/phases/03-run-execution-history/03-UI-SPEC.md (Color section status icon/color mapping table — the authoritative source for label + icon + color per status)
  </read_first>
  <behavior>
    - runStatusMeta("PENDING") returns label "Queued", icon Clock, muted-foreground class, no spin.
    - runStatusMeta("RUNNING") returns label "Running", icon LoaderCircle, muted-foreground class, spin true.
    - runStatusMeta("COMPLETED") returns label "Completed", icon Check, foreground (neutral) class, no spin.
    - runStatusMeta("FAILED") returns label "Failed", icon X, destructive class, no spin.
    - runStatusMeta("WEIRD_UNSEEN") returns a neutral fallback (Clock + muted) whose label is the raw string "WEIRD_UNSEEN", never a fabricated humanization.
    - isTerminalStatus: COMPLETED and FAILED true; PENDING and RUNNING false.
    - hasActiveRun([]) is false; hasActiveRun of an all-terminal list is false; hasActiveRun of a list with any PENDING/RUNNING run is true.
    - newestActiveRun returns the FIRST non-terminal run in the input order (server newest-first), or null when none; two runs sharing created_at do not get re-sorted.
  </behavior>
  <action>
    Create frontend/src/lib/runStatus.ts exporting: a `RUN_STATUS_META` object keyed by the four status strings, each value `{ label, Icon, className, spin? }` where Icon is a lucide-react component (Clock, LoaderCircle, Check, X imported from lucide-react) and className is the Tailwind color token from the UI-SPEC Color table ("text-muted-foreground" for PENDING/RUNNING, "text-foreground" for COMPLETED, "text-destructive" for FAILED), with `spin: true` only on RUNNING; a `runStatusMeta(status: string)` accessor that returns the mapped meta or, for an unrecognized status, a neutral fallback `{ label: status, Icon: Clock, className: "text-muted-foreground" }` (raw string as label — mirror toolLabel's fallback discipline); `isTerminalStatus(status: string): boolean` returning `status === "COMPLETED" || status === "FAILED"`; `hasActiveRun(runs)` = `runs.some((r) => !isTerminalStatus(r.status))`; and `newestActiveRun(runs)` returning `runs.find((r) => !isTerminalStatus(r.status)) ?? null`. Type the run parameter via the generated `components["schemas"]["RunOut"]` from @/api/schema (indexed access, not hand-authored). Keep this module pure (no React, no JSX). Create frontend/src/lib/runStatus.test.ts asserting every behavior above, including the unknown-status fallback and the empty/all-terminal/has-one-active cases and the newest-first stability.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/runStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - runStatus.ts exports RUN_STATUS_META, runStatusMeta, isTerminalStatus, hasActiveRun, newestActiveRun.
    - runStatusMeta returns the exact UI-SPEC label/icon/color for each of PENDING/RUNNING/COMPLETED/FAILED, with spin only on RUNNING.
    - runStatusMeta on an unrecognized status returns the raw status as its label (test asserts a nonsense status yields itself as label, not a fabricated string).
    - isTerminalStatus is true only for COMPLETED/FAILED; hasActiveRun([]) === false; newestActiveRun returns the first non-terminal run in input order and null when all terminal.
    - runStatus.ts imports no React/JSX and defines RunOut via generated `components` (no hand-authored field list).
    - `cd frontend && npx vitest run src/lib/runStatus.test.ts` exits 0.
  </acceptance_criteria>
  <done>runStatus.ts is the single, pure, tested source of run-status labels/icons/colors and the terminal/active predicates, with a safe fallback for unknown statuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend (fetch) | scenario_id path param crosses into a read-only GET / a POST; both are same-origin-configured first-party calls to the project's own FastAPI backend (no third-party API). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | listRuns/triggerRun path param | low | mitigate | scenario_id is passed as a typed openapi-fetch path param (URL-encoded by the client), never string-concatenated into the path — no path/query injection surface. |
| T-3-02 | Information disclosure | thrown `{ status, ...error }` | low | accept | The attached error may carry backend detail, but it is thrown for status-branching only; downstream components render fixed copy, not the error body (see 03-04). Internal demo app, no auth (out of scope per REQUIREMENTS.md). |
| T-3-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package installs this phase — UI-SPEC confirms zero new dependencies and zero new shadcn components; all imports (lucide-react, openapi-fetch, @tanstack/react-query) already present. No legitimacy gate required. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/api/runs.test.ts src/lib/runStatus.test.ts` exits 0.
- `cd frontend && npm run typecheck` passes (no hand-authored RunOut drift).
</verification>

<success_criteria>
- runs.ts wraps exactly POST + list GET for runs, attaching response.status on failure.
- runStatus.ts exports the full status vocabulary and terminal/active predicates, pure and tested, with an unknown-status fallback.
- No new package.json dependency or shadcn component added.
</success_criteria>

<artifacts_produced>
New files: frontend/src/api/runs.ts, frontend/src/api/runs.test.ts, frontend/src/lib/runStatus.ts, frontend/src/lib/runStatus.test.ts.
New exports: listRuns, triggerRun (api/runs.ts); RUN_STATUS_META, runStatusMeta, isTerminalStatus, hasActiveRun, newestActiveRun (lib/runStatus.ts).
</artifacts_produced>

<output>
Create `.planning/phases/03-run-execution-history/03-01-SUMMARY.md` when done.
</output>
