---
phase: 04-results-insights
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/results.ts
  - frontend/src/api/results.test.ts
  - frontend/src/api/insights.ts
  - frontend/src/api/insights.test.ts
  - frontend/src/api/runs.ts
  - frontend/src/lib/formatShiftWindow.ts
  - frontend/src/lib/formatShiftWindow.test.ts
  - docs/API.md
autonomous: true
requirements: [RES-03, RES-04, RES-05, RES-06]
must_haves:
  truths:
    - "frontend/src/api/results.ts exports a single hand-written `RunResult` TypeScript interface (with a `warnings: string[]` field) and a `getRunResult(runId)` wrapper that casts the response to `RunResult` exactly once, sourced field-for-field from backend/services/serialize.py — because GET /runs/{run_id}/result has no FastAPI response_model, schema.d.ts resolves it to `{ [key: string]: unknown }` (RESEARCH.md Pitfall 1)."
    - "getRunResult preserves the runtime `warnings: string[]` array end-to-end so it can reach the WarningsBanner — RES-06's warnings are real at runtime (serialize.py) even though schema.d.ts is silent on them."
    - "frontend/src/api/insights.ts exports `getRunInsights(runId)` returning the generated InsightOut ({ ready, run_id, report?, status?, reason? }) and, on error, throws `{ status: response.status, ...error }` so a 502 carries its status for a distinct failure treatment (RES-04/RES-05)."
    - "frontend/src/api/runs.ts gains a `getRun(runId)` wrapper over GET /runs/{run_id} returning RunOut, which always succeeds regardless of run status (D-12's deep-link status probe)."
    - "frontend/src/lib/formatShiftWindow.ts converts hour-offset floats to a 1-indexed 'Day N, HH:MM–HH:MM' string and renders cross-midnight shifts as 'Day N, HH:MM – Day N+1, HH:MM' (RES-03, D-10; RESEARCH.md Pitfall 3), rounding once in total minutes to avoid a 23.999h→24:00 boundary bug (Pitfall 2)."
    - "docs/API.md's RunResult model table and example JSON document the top-level `warnings: string[]` array that GET /runs/{run_id}/result actually returns (closing the doc/code drift flagged in CONTEXT.md canonical_refs; RES-06)."
  prohibitions:
    - "RunResult must not be typed as `unknown` at scattered call sites, and no component may perform its own `as RunResult` / `as any` cast — the single hand-written interface plus one cast in results.ts is the only casting point (RESEARCH.md Anti-Patterns; T-04-02)."
    - "getRunInsights must not pre-interpret the InsightOut body — it returns the raw ready/report shape so the caller branches on the `ready` field; the wrapper must not translate a `ready:false` 200 into a thrown error."
    - "The docs/API.md fix must not be relied on for type safety — it is contract documentation only; it cannot make schema.d.ts type this response (the endpoint has no response_model)."
  artifacts:
    - frontend/src/api/results.ts
    - frontend/src/api/insights.ts
    - frontend/src/lib/formatShiftWindow.ts
  key_links:
    - "results.ts imports the single `client` instance from ./client (no second client) and mirrors runs.ts's `throw { status: response.status, ...error }` (T-1-02) convention."
    - "The RunResult interface's field names and nullability must match backend/services/serialize.py exactly (total_cost/total_unmet_hours nullable, coverage_by_function values nullable, coverage_by_day values nullable, warnings always a list) — drift here silently breaks every downstream results component."
    - "formatShiftWindow's 1-indexed day math (Math.floor(totalMinutes/1440)+1) must equal CoverageByDayTable's `Number(dayKey)+1` convention (plan 04-04) so the same calendar day reads identically in both tables (Pitfall 2)."
---

<objective>
Build the typed read contract this phase renders against: a hand-written `RunResult` interface + `getRunResult` wrapper (the untyped-endpoint deviation), a `getRunInsights` wrapper (InsightOut is already typed), a `getRun` addition to runs.ts (D-12's status probe), the `formatShiftWindow` hour-offset formatter (RES-03/D-10), and the docs/API.md `warnings` contract fix (RES-06).

Purpose: GET /runs/{run_id}/result has NO FastAPI response_model, so openapi-typescript cannot type it — the entire RunResult shape (including `warnings`) is invisible to codegen. This plan makes the contract real in exactly one place (a documented single-point deviation, mirroring formatTimestamp.ts's precedent), so every downstream component consumes one honest type instead of N ad-hoc casts.
Output: results.ts (+ test), insights.ts (+ test), runs.ts getRun, lib/formatShiftWindow.ts (+ test), docs/API.md warnings fix.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-insights/04-RESEARCH.md
@.planning/phases/04-results-insights/04-PATTERNS.md
@.planning/phases/04-results-insights/04-CONTEXT.md
@frontend/src/api/runs.ts
@frontend/src/api/constraints.ts
@frontend/src/api/client.ts
@frontend/src/api/runs.test.ts
@frontend/src/lib/formatTimestamp.ts
@backend/services/serialize.py
@backend/domain/result.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: results.ts — hand-written RunResult type + getRunResult + docs/API.md warnings fix</name>
  <files>frontend/src/api/results.ts, frontend/src/api/results.test.ts, docs/API.md</files>
  <read_first>
    - frontend/src/api/runs.ts (error-throw convention: `throw { status: response.status, ...error }`; single `client` import)
    - frontend/src/api/runs.test.ts (the vi.mock("./client") boundary-mock pattern to replicate in results.test.ts)
    - backend/services/serialize.py (THE wire contract — field names, nesting, `_num()` non-finite→null coercion; source the interface field-for-field from here, not docs/API.md, not schema.d.ts)
    - backend/domain/result.py (ScheduleRow fields: contact_id, member_name, task_id, function, shift_id, start_h, end_h; SolveResult.warnings default_factory=list)
    - .planning/phases/04-results-insights/04-RESEARCH.md (Pitfall 1 + Code Example 2 — the exact RunResult interface and the deviation comment)
    - docs/API.md (current RunResult model table + example JSON — the drift to fix)
  </read_first>
  <behavior>
    - getRunResult(runId) resolves to a RunResult object when client.GET succeeds; the returned object exposes metrics.total_cost, metrics.coverage_by_function, metrics.coverage_by_day, schedule[], and warnings[].
    - On a client error, getRunResult throws an object carrying `.status` equal to response.status.
    - RunResult's nullable fields (total_cost, total_unmet_hours, coverage_by_function[fn].required_h/served_h/pct, coverage_by_day[key]) are typed `number | null`; warnings is `string[]`.
  </behavior>
  <action>
    Create frontend/src/api/results.ts exporting a hand-written `RunResult` interface (and the nested CoverageStat shape) plus `getRunResult(runId: string): Promise<RunResult>`. Field names, nesting, and nullability come field-for-field from serialize_result() in backend/services/serialize.py: top-level status (string), metrics { total_cost, total_unmet_hours (both number|null), scheduled_shifts, scheduled_members (numbers), coverage_by_function (Record of { required_h, served_h, pct } each number|null), coverage_by_day (Record<string, number|null>) }, stats { status, wall_time_s, unmet_objective_hours, cost_objective }, schedule (array of { contact_id, member_name, task_id, function, shift_id, start_h, end_h }), and warnings (string[]). The wrapper calls the single `client.GET("/runs/{run_id}/result", { params: { path: { run_id: runId } } })`, throws `{ status: response.status, ...error }` on error (T-1-02, mirroring runs.ts), and returns `data as RunResult` — the ONE cast point. Include a top-of-file deviation comment explaining why the type is hand-written (no FastAPI response_model → schema.d.ts resolves to an unknown-indexed record), mirroring formatTimestamp.ts's single-explanatory-comment precedent. Then create results.test.ts mirroring runs.test.ts's vi.mock("./client") boundary-mock — assert getRunResult returns the mocked body and that an errored response throws with `.status` attached. Finally, update docs/API.md's RunResult model table and example JSON to document the top-level `warnings` string-array field (the field is already returned at runtime by serialize.py:44 — this closes the documented drift; it does not change any type).
    Keep the deviation comment free of any literal token that an acceptance grep negatives on — describe the raw-HTML-injection prohibition by concept, not by pasting the API name.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/api/results.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/results.ts` exports `interface RunResult` and `async function getRunResult`.
    - `src/api/results.ts` contains `warnings` in the RunResult interface and performs exactly one `as RunResult` cast (grep count of `as RunResult` in results.ts equals 1).
    - `npx vitest run src/api/results.test.ts` exits 0 and asserts both the success body and the error-throws-with-status path.
    - `docs/API.md` contains a `warnings` entry in the RunResult model table.
    - `tsc --noEmit` passes with no `unknown`-typed access to a RunResult field anywhere.
  </acceptance_criteria>
  <done>getRunResult returns a fully-typed RunResult (warnings included) from one cast point, results.test.ts is green, and docs/API.md documents the warnings field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insights.ts (getRunInsights) + runs.ts getRun</name>
  <files>frontend/src/api/insights.ts, frontend/src/api/insights.test.ts, frontend/src/api/runs.ts</files>
  <read_first>
    - frontend/src/api/constraints.ts (exact wrapper shape to copy — InsightOut, like ConstraintParseResponse, is already in schema.d.ts so no hand-written type is needed)
    - frontend/src/api/runs.ts (listRuns/triggerRun shape; add getRun following the identical structure)
    - frontend/src/api/schema.d.ts (confirm InsightOut { ready, run_id, report?, status?, reason? } and the /runs/{run_id} GET → RunOut operation)
    - .planning/phases/04-results-insights/04-PATTERNS.md (insights.ts and runs.ts getRun pattern blocks)
  </read_first>
  <behavior>
    - getRunInsights(runId) resolves to the InsightOut body ({ ready, report?, ... }) on success; it never converts a ready:false 200 into a throw.
    - getRunInsights throws `{ status, ...error }` with status 502 when the backend returns a 502.
    - getRun(runId) resolves to RunOut for any run status (PENDING/RUNNING/COMPLETED/FAILED) without throwing on non-terminal states.
  </behavior>
  <action>
    Create frontend/src/api/insights.ts exporting `getRunInsights(runId: string)` over `client.GET("/runs/{run_id}/insights", ...)`, returning `data` (the typed InsightOut) unmodified and throwing `{ status: response.status, ...error }` on error — same T-1-02 convention, so RES-05's 502 is distinguishable by status. Do not interpret the `ready` field in the wrapper; the caller (InsightPanel, plan 04-06) branches on it. Add `getRunInsights.test.ts` mirroring runs.test.ts's boundary-mock: assert the success body passes through and a 502 throws with `.status === 502`. Then add `getRun(runId: string)` to frontend/src/api/runs.ts as a third export over `client.GET("/runs/{run_id}", ...)` returning `data` (RunOut) with the identical error-throw convention; update runs.ts's header comment which currently says "No getRun/per-run wrapper" (that statement is now obsolete — this phase adds it).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/api/insights.test.ts src/api/runs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/insights.ts` exports `getRunInsights` and returns the response body without inspecting `ready`.
    - `src/api/runs.ts` exports `getRun` and its header comment no longer claims there is no per-run wrapper.
    - `npx vitest run src/api/insights.test.ts src/api/runs.test.ts` exits 0, including a 502-throws-with-status assertion.
  </acceptance_criteria>
  <done>getRunInsights and getRun exist, follow the established error-throw convention, and their boundary-mocked tests pass — the InsightOut and RunOut reads this phase needs are wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: formatShiftWindow hour-offset formatter</name>
  <files>frontend/src/lib/formatShiftWindow.ts, frontend/src/lib/formatShiftWindow.test.ts</files>
  <read_first>
    - frontend/src/lib/formatTimestamp.ts (the deterministic-pure-utility-with-header-comment precedent — same file shape: top comment justifying the non-obvious approach, defensive behavior, no host-locale dependence)
    - .planning/phases/04-results-insights/04-RESEARCH.md (Code Example 1 — the exact formatDayTime/formatShiftWindow implementation; Pitfall 2 (1-indexed day) and Pitfall 3 (cross-midnight))
    - backend/ingest/scenario_time.py (hour-offset convention and the cross-midnight window handling that makes a night shift a real, reachable case)
  </read_first>
  <behavior>
    - formatShiftWindow(startH, endH) with start and end on the same day returns `Day N, HH:MM–HH:MM` (1-indexed: hours 0–24 → Day 1).
    - When start and end fall on different days (end_h ≥ 24 past start's day, or end < start after %24), returns `Day N, HH:MM – Day M, HH:MM`.
    - Rounding is done once in total minutes so 23.999h does not render as `24:00`; HH and MM are zero-padded to two digits.
  </behavior>
  <action>
    Create frontend/src/lib/formatShiftWindow.ts with an internal `formatDayTime(h)` helper (round once via Math.round(h*60) total minutes; day = Math.floor(totalMinutes/1440)+1; minutesOfDay = totalMinutes%1440; zero-pad hh and mm) and an exported `formatShiftWindow(startH, endH)` that returns the same-day form `Day ${start.day}, ${start.hhmm}–${end.hhmm}` when start.day === end.day, else the cross-midnight form `Day ${start.day}, ${start.hhmm} – Day ${end.day}, ${end.hhmm}`. Add a top-of-file comment explaining the round-once-in-minutes choice (avoiding the 24:00 boundary bug) and the 1-indexed convention, mirroring formatTimestamp.ts. Add formatShiftWindow.test.ts covering: a same-day shift (e.g. 8.5→16.0 → "Day 1, 08:30–16:00"), a second-day shift (30.0→38.0 → "Day 2, 06:00–14:00"), a cross-midnight shift (22.0→30.0 → "Day 1, 22:00 – Day 2, 06:00"), and the rounding boundary (23.999h does not yield "24:00").
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/formatShiftWindow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/formatShiftWindow.ts` exports `formatShiftWindow`.
    - `npx vitest run src/lib/formatShiftWindow.test.ts` exits 0 with the same-day, second-day, cross-midnight, and rounding-boundary cases.
    - A shift starting at hour 0 renders "Day 1" (1-indexed), never "Day 0".
  </acceptance_criteria>
  <done>formatShiftWindow produces the D-10 shift-window strings, 1-indexed and cross-midnight-safe, with a passing test — ready for ScheduleTable (plan 04-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API → client | GET /runs/{run_id}/result returns a raw dict with no response_model; its shape is untyped by codegen and consumed as `RunResult` in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | untyped RunResult response consumed at multiple call sites | low | mitigate | A single hand-written `RunResult` interface + one `as RunResult` cast in results.ts (not N scattered casts that could each drift from the real backend shape). |
| T-04-05 | Information disclosure | getRunInsights/getRunResult error object | low | mitigate | Errors throw `{ status, ...error }` for status branching only; error text is never rendered by these wrappers (rendering is the consuming component's concern, mitigated in 04-04/04-06/04-07). |
</threat_model>

<verification>
- `npx vitest run src/api/results.test.ts src/api/insights.test.ts src/api/runs.test.ts src/lib/formatShiftWindow.test.ts` all green.
- results.ts has exactly one RunResult cast; docs/API.md documents warnings.
- formatShiftWindow is 1-indexed and handles cross-midnight.
</verification>

<success_criteria>
The typed read contract (RunResult + getRunResult, getRunInsights, getRun) and the shift-window formatter exist with green boundary-mocked tests, and docs/API.md documents the warnings field — the foundation the hooks (04-03) and components (04-04/04-05/04-06) build on.
</success_criteria>

## Artifacts this phase produces

- Files: `frontend/src/api/results.ts`, `frontend/src/api/results.test.ts`, `frontend/src/api/insights.ts`, `frontend/src/api/insights.test.ts`, `frontend/src/lib/formatShiftWindow.ts`, `frontend/src/lib/formatShiftWindow.test.ts`
- Symbols: `interface RunResult`, `interface CoverageStat`, `getRunResult()` (results.ts); `getRunInsights()` (insights.ts); `getRun()` (added to runs.ts); `formatShiftWindow()`, `formatDayTime()` (formatShiftWindow.ts)
- Doc change: `docs/API.md` RunResult model table + example JSON now include `warnings: string[]`
