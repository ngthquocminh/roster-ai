---
phase: 02-scenario-detail-plain-english-constraints
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - frontend/src/components/editor/ScenarioHeader.tsx
  - frontend/src/components/editor/ScenarioHeader.test.tsx
  - frontend/src/components/editor/OverridesList.tsx
  - frontend/src/components/editor/OverridesList.test.tsx
  - frontend/src/lib/toolLabels.ts
autonomous: true
requirements: [SCEN-03, CONS-02]
tags: [frontend, ui, editor, overrides]

must_haves:
  truths:
    - "The scenario detail header renders name (Heading 20px/600), fixture (monospace, ScenarioTable convention), and time_limit_s + created_at (Label 12px muted captions) once GET /scenarios/{id} resolves (UI-SPEC E1 populated)."
    - "While GET /scenarios/{id} is in flight the header shows a centered spinner + \"Loading scenario…\" — no partial header renders before the fetch resolves (E1 loading)."
    - "A 404 on GET /scenarios/{id} renders the terminal \"Scenario not found\" view with a \"Back to Scenarios\" action routing to \"/\" (E1 error-404 / E7), distinct from a network/5xx failure which reuses ErrorBanner (E1 error-network)."
    - "The overrides list renders one row per override showing parsed_constraint text only — never raw {tool, args} JSON (CONS-02, E2 populated), in the exact order the backend returns (no client re-sort)."
    - "A legacy override missing parsed_constraint renders the fallback \"{Tool Label}: {comma-separated key=value args}\" (italic, muted) with a \"(legacy entry)\" caption — not a blank or broken row (D-02 migration, E2 partial)."
    - "The overrides list shows \"Loading overrides…\" (spinner) while loading, \"No constraints applied yet\" when empty, ErrorBanner on fetch error, and scrolls inside a max-h-[420px] container past ~10 rows (E2 loading/empty/error/overflow)."
    - "One override renders identically to many; no count label (E2 zero-one-many)."
    - statement: "A very long scenario name in the header truncates or wraps without breaking layout (visual truncation not assertable at spec time)."
      verification: backstop
    - statement: "A long parsed_constraint sentence or a long legacy key=value string wraps or truncates within its row without breaking layout (visual wrapping not assertable at spec time)."
      verification: backstop
  artifacts:
    - "frontend/src/components/editor/ScenarioHeader.tsx (+ test)"
    - "frontend/src/components/editor/OverridesList.tsx (+ test)"
    - "frontend/src/lib/toolLabels.ts — the fixed Tool Label Map"
  key_links:
    - "OverridesList must render override.parsed_constraint verbatim when present and only fall back to the Tool Label Map when it is null/undefined — the discriminator is the field's presence, not tool value."
  prohibitions:
    - "The overrides list must never render raw {tool, args} JSON as the user-facing form of an override — parsed_constraint (verbatim) or the documented legacy fallback only (CONS-02 fidelity intent)."
    - "Neither component may client-side re-sort the overrides — render in the exact order the backend returns (same principle as ScenarioTable)."
    - "Neither component may use dangerouslySetInnerHTML for parsed_constraint / args text — plain JSX text-node interpolation only (server-influenced, name-derived strings)."
---

<objective>
Build the read half of the Editor: the scenario detail header (SCEN-03 header fields, with the 404 terminal view that finally resolves Phase 1's bad-deep-link backstop) and the durable Applied Overrides list (SCEN-03/D-01/D-02) rendering the persisted `parsed_constraint` sentence, with a graceful legacy fallback. Both are read-only surfaces over the plan-02-03 hooks; they do not compose into the route yet (that is plan 02-06).

Purpose: A user opening a scenario sees its details and every override currently applied, in readable form, surviving reload — never raw tool-call JSON.
Output: `ScenarioHeader.tsx`, `OverridesList.tsx`, the `toolLabels.ts` fixed map, and their tests.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-CONTEXT.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-RESEARCH.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-PATTERNS.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-UI-SPEC.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-03-SUMMARY.md
</context>

<artifacts_produced>
New symbols this plan creates:
- `ScenarioHeader` component (`frontend/src/components/editor/ScenarioHeader.tsx`)
- `OverridesList` component (`frontend/src/components/editor/OverridesList.tsx`)
- `TOOL_LABELS` map + `toolLabel(tool)` helper (`frontend/src/lib/toolLabels.ts`)
- Test files for both components
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScenarioHeader — single-record detail with loading / 404-terminal / error / populated states</name>
  <files>frontend/src/components/editor/ScenarioHeader.tsx, frontend/src/components/editor/ScenarioHeader.test.tsx</files>
  <behavior>
    - Loading: renders spinner + "Loading scenario…".
    - Error status 404: renders "Scenario not found" heading + body + a "Back to Scenarios" link/button to "/".
    - Error non-404: renders ErrorBanner.
    - Populated: renders name (Heading), fixture (monospace), time_limit_s + created_at (Label captions).
  </behavior>
  <read_first>
    - frontend/src/components/scenarios/ScenarioTable.tsx (whole file) — the loading/error/populated state triad (lines 57-73) and the fixture monospace/truncate class (line 123) to adapt for a single record.
    - frontend/src/components/layout/ErrorBanner.tsx — the reused non-404 error surface.
    - frontend/src/components/scenarios/CreateScenarioDialog.tsx lines 131, 145-146 — the Heading (20px/600) title class and the Label caption class to reuse.
    - frontend/src/hooks/useScenario.ts — the hook this component reads (isLoading/isError/error/data; error carries `status`).
    - frontend/src/components/scenarios/ScenarioTable.test.tsx — the RTL render/QueryClientProvider + vi.mock pattern for component tests.
    - .planning/phases/.../02-UI-SPEC.md Copywriting Contract ("Scenario-not-found", header fields) + UI Considerations E1/E7.
    - .planning/phases/.../02-PATTERNS.md section "ScenarioHeader.tsx".
  </read_first>
  <action>
    Create frontend/src/components/editor/ScenarioHeader.tsx taking a scenarioId prop (or reading the useScenario query result passed in — pick the prop shape that lets Editor share the same query instance in plan 02-06; prefer accepting the query object so the same useScenario call drives both header and the useOverrides `enabled` gate). Render the four states above. For the 404 view use the exact Copywriting Contract copy ("Scenario not found" / "It may have been removed, or the link is incorrect.") and a react-router Link/navigation to "/" labeled "Back to Scenarios". For populated, use the Heading class for name, `font-mono truncate` for fixture, and Label caption classes for time_limit_s and created_at. Use plain JSX interpolation for all text (no dangerouslySetInnerHTML).
    Create the test asserting each of the four states renders its distinct content (mock useScenario / the api module): loading text, 404 terminal view with the Back link, ErrorBanner on non-404, and the populated field set.
  </action>
  <verify>
    <automated>cd frontend && npm test -- ScenarioHeader && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- ScenarioHeader` passes for loading, 404-terminal (with "Back to Scenarios"), non-404 error (ErrorBanner), and populated states.
    - The 404 branch keys off `error.status === 404`, not message text.
    - Populated header renders name, fixture (monospace class present), time_limit_s, and created_at.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The header renders all E1/E7 states, resolving the Phase 1 bad-deep-link backstop as a real terminal view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OverridesList + Tool Label Map — durable list with legacy fallback</name>
  <files>frontend/src/components/editor/OverridesList.tsx, frontend/src/components/editor/OverridesList.test.tsx, frontend/src/lib/toolLabels.ts</files>
  <behavior>
    - Loading: spinner + "Loading overrides…".
    - Empty (data=[]): "No constraints applied yet" + body.
    - Error: ErrorBanner.
    - Populated: one row per override keyed by override.id; parsed_constraint rendered verbatim.
    - Legacy row (parsed_constraint null): fallback "{Tool Label}: key=value, …" italic muted + "(legacy entry)" caption.
    - Order preserved as received; scroll container max-h-[420px] overflow-y-auto.
  </behavior>
  <read_first>
    - frontend/src/components/scenarios/ScenarioTable.tsx (whole file) — the list state machine (lines 57-91) and the `max-h-[420px] overflow-y-auto rounded-md border border-border` container (line 94) + the no-client-resort comment (lines 15-18) + the React-escaping comment (lines 25-27).
    - frontend/src/hooks/useOverrides.ts — the hook (dependent query; the component reads its isLoading/isError/error/data).
    - .planning/phases/.../02-UI-SPEC.md Copywriting Contract rows: "Applied-overrides empty/loading/error", "Legacy override fallback", plus the Tool Label Map table (the five tool→label rows).
    - .planning/phases/.../02-PATTERNS.md section "OverridesList.tsx".
    - frontend/src/components/scenarios/ScenarioTable.test.tsx — component test harness.
  </read_first>
  <action>
    Create frontend/src/lib/toolLabels.ts exporting the fixed TOOL_LABELS map (lock_worker_shift→"Lock worker shift", set_min_workers_per_task→"Minimum workers per task", exclude_worker_from_task→"Exclude worker from task", scale_demand→"Scale demand", set_max_hours→"Max hours") and a toolLabel(tool: string) helper returning the mapped label or the raw tool string as last-resort fallback. This is a fixed lookup, NOT a runtime humanization attempt.
    Create frontend/src/components/editor/OverridesList.tsx: accept the useOverrides query result (or scenarioId + enabled) — choose the shape that lets Editor pass the dependent-query result in plan 02-06. Copy ScenarioTable's loading/empty/error state machine and overflow container verbatim in structure. For each override row: if override.parsed_constraint is a non-empty string, render it verbatim (Body text); else render the legacy fallback using toolLabel(override.tool) + a comma-joined key=value list built from override.args, italic + muted, with a "(legacy entry)" caption. Key rows by override.id (never index). Never re-sort. Plain JSX interpolation only.
    Create the test asserting: loading text, empty state copy, ErrorBanner on error, a populated row shows parsed_constraint verbatim (and NOT the raw args object), a legacy row (parsed_constraint null) shows the "{Tool Label}: key=value" fallback + "(legacy entry)", and that a single override and multiple overrides both render without a count label.
  </action>
  <verify>
    <automated>cd frontend && npm test -- OverridesList && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- OverridesList` passes for loading, empty, error, populated (parsed_constraint verbatim), and legacy-fallback rows.
    - A test asserts the raw `{tool, args}` JSON string does not appear as an override's user-facing text when parsed_constraint is present.
    - `frontend/src/lib/toolLabels.ts` contains all five tool→label entries.
    - Rows are keyed by override.id and rendered in received order (no sort call in the component).
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The durable overrides list renders the readable parsed_constraint (with a graceful legacy fallback) across all E2 states, never as raw JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → JSX render | parsed_constraint / args values (server-influenced, task/member-name-derived) are rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering / Information Disclosure | OverridesList / ScenarioHeader text render | medium | mitigate | React default JSX text-node escaping is the complete mitigation for parsed_constraint/args/name strings (same T-1-03 convention as ScenarioTable); no dangerouslySetInnerHTML anywhere. |
| T-02-11 | Information Disclosure | non-404 error surface | low | mitigate | Non-404 header/list errors reuse ErrorBanner, which never renders raw backend internals (T-1-02 / ASVS V7). |
| T-02-12 | Spoofing | 404 terminal-view discrimination | low | mitigate | Branch on `error.status === 404`, not message text, so a non-404 failure cannot be misrendered as "scenario not found". |
| T-02-SC | Tampering | dependency installs | low | accept | No package installs — composes existing shadcn primitives + lucide-react. |

Block-on = high; no high-severity threats.
</threat_model>

<verification>
- `cd frontend && npm test -- ScenarioHeader OverridesList` — all E1/E2/E7 states.
- `cd frontend && npm run typecheck`.
- Backstops (E1/E2 long-text): no automated evidence at spec time → routed to human verification in plan 02-07.
</verification>

<success_criteria>
- SCEN-03 read surface complete: header (with 404 terminal view) + durable overrides list rendering parsed_constraint (legacy-safe), never raw JSON.
</success_criteria>

<output>
Create `.planning/phases/02-scenario-detail-plain-english-constraints/02-04-SUMMARY.md` when done.
</output>
