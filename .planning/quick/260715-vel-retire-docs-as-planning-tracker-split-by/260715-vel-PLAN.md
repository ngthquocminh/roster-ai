---
phase: quick-260715-vel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLAN.md
  - docs/README.md
  - docs/API.md
  - docs/design.md
  - docs/vision.md
  - docs/archive/phase-1-engine.md
  - docs/archive/phase-2-backend.md
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/PLAN.md no longer exists; .planning/ (STATE.md + ROADMAP.md + MILESTONES.md) is the only planning-lifecycle tracker in the repo."
    - "docs/API.md documents the two shipped LLM-layer endpoints (POST /constraints, GET /runs/{run_id}/insights) with contracts that match backend code exactly, plus the LLM_PROVIDER env var."
    - "docs/design.md contains no phase-status tracking and no unbuilt-provider claims; it holds the durable shipped LLM-layer design and states the design.md-vs-.planning/codebase ownership rule."
    - "docs/vision.md stays at docs/vision.md and is byte-for-byte unchanged except for the removal of its self-deprecation sentence."
    - "docs/README.md states the one-owner-per-audience boundary: .planning/ owns lifecycle, docs/ owns reference + rationale + origin."
    - ".planning/PROJECT.md's Context section makes no false claims about docs/decisions/ ADRs or PLAN.md."
    - "No live document in the repo contains a markdown link to the deleted or moved files."
  artifacts:
    - docs/README.md
    - docs/archive/phase-1-engine.md
    - docs/archive/phase-2-backend.md
  key_links:
    - "README.md Layout block + Documentation list — both name docs/PLAN.md today and break on deletion."
    - "docs/design.md header blockquote (line 7-8) + Status paragraph (line 18) + repo-layout block (line 98) — three separate PLAN.md links."
    - "docs/archive/*.md headers — each phase doc links back to PLAN.md and to each other."
    - "Generated (.planning/codebase/*) and historical (.planning/milestones/**, RETROSPECTIVE.md, todos/) artifacts also mention PLAN.md but are OUT OF SCOPE — see Task 3."
---

<objective>
Retire `docs/` as a planning tracker and split what remains by lifecycle owner, now that
`.planning/` (GSD) owns the planning lifecycle. `docs/` rotted because `PLAN.md` silently
duplicated `.planning/`'s job with no rule saying which wins. This task deletes the
duplicate, fixes the drift the duplication hid, and writes down the ownership boundary so
the drift cannot silently recur.

Purpose: One owner per audience. `.planning/` = what's next/shipped/decided.
`docs/` = reference (API.md, the live contract v0.4's React frontend is written against),
rationale (design.md, why the system is this shape), origin (vision.md, frozen).
Output: `PLAN.md` deleted; `API.md` covering the shipped LLM layer; `design.md` trimmed to
"why" with the shipped LLM design folded in; phase docs archived; `docs/README.md` created;
`PROJECT.md` Context corrected; zero dangling links.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./.claude/CLAUDE.md

# Read before deleting — Task 1 must confirm nothing is lost
@docs/PLAN.md

# Accurate shipped detail for the design.md refold (Task 2)
@.planning/MILESTONES.md
@.planning/milestones/v0.3-ROADMAP.md
</context>

<constraints>
**Docs-only. NO changes under `backend/`.** Backend files are read-only sources of truth
for this task. If a doc disagrees with the code, the doc is wrong — fix the doc.

**Do not create an ADR system.** `docs/decisions/` does not exist and never did; Task 3
corrects the claim that it does. Do not make the claim true.

**Do not rewrite history.** `.planning/milestones/**`, `.planning/RETROSPECTIVE.md`, and
`.planning/todos/pending/*` reference `docs/PLAN.md` as a factual record of what existed
when they were written. Those references are correct history and stay. `.planning/codebase/*`
is generated by `/gsd-map-codebase` — do not hand-edit it; it self-heals on regeneration.
Task 3 scopes the link sweep to live documents only.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Retire the tracker, archive the phase docs, freeze vision.md, write the ownership boundary</name>
  <files>docs/PLAN.md, docs/archive/phase-1-engine.md, docs/archive/phase-2-backend.md, docs/vision.md, docs/README.md</files>
  <action>
Read `docs/PLAN.md` first and confirm before deleting: all five of its "⏸ deferred/optional"
follow-ups already live in `.planning/todos/pending/` (migrated in commit a84ddec), and its
future phases 4 (frontend) and 5 (what-if/polish/deploy) are already captured in
`.planning/PROJECT.md` under "Next Milestone Goals" and "Out of Scope". Nothing is lost.
If any item is NOT found in `.planning/`, stop and report it rather than deleting.

Then, in order:

1. `git rm docs/PLAN.md`. It is pure duplication of `.planning/STATE.md` +
   `.planning/ROADMAP.md` + `.planning/MILESTONES.md`.

2. Archive the two phase docs with `git mv` (preserves history — do not copy-then-delete):
   `git mv docs/phase-1-engine.md docs/archive/phase-1-engine.md` and
   `git mv docs/phase-2-backend.md docs/archive/phase-2-backend.md`.
   Give each a short header blockquote at the top, above the existing content, stating: a
   historical plan doc, not maintained, kept as a record of how the phase was built; its
   durable design now lives in `../design.md`; project status lives in `.planning/`.
   Each file's existing header links back to the retired tracker and must be reconciled —
   fix those links as part of writing the new header (Task 3 verifies none survive).
   Their value drops to zero once design.md holds the durable content; that is why they are
   archived rather than kept in place.

3. `docs/vision.md` — **KEEP AT `docs/vision.md`. Do NOT move it to `docs/archive/`.**
   It is the origin snapshot: deliberately permanent, and its value grows over time rather
   than shrinking. It already self-declares as an unmaintained snapshot, which is exactly why
   it never rotted — a doc that correctly declares itself a snapshot cannot drift.
   **The only change:** delete line 8, the single sentence saying the file may be removed once
   it is no longer a useful reference. It is now permanent. Leave every other byte of that
   file untouched — no reflowing, no link updates, no header edits.

4. Create `docs/README.md`. Short — a boundary statement, not a doc index that will itself rot.
   State the guiding principle: documentation has exactly one owner per audience; `docs/`
   rotted because PLAN.md duplicated `.planning/`'s job with no rule saying which wins.
   Name the three classes and their owners:
   - Planning lifecycle (what's next / shipped / decided) → `.planning/`.
   - Reference + rationale (API.md = the live API contract; design.md = why the system is
     shaped this way and what was deliberately not built) → `docs/`. No GSD equivalent exists.
   - Origin (vision.md = the initial idea, frozen) → `docs/`.
   Note that the hand-written phase tracker was retired at the v0.3/v0.4 boundary and point to
   `.planning/` for status. Mention it in prose only — do **not** write a markdown link to the
   deleted file (Task 3's gate rejects links, not mentions).
   Mention `docs/archive/` holds the superseded Phase 1–2 plan docs.
  </action>
  <verify>
    <automated>test ! -f docs/PLAN.md && test ! -f docs/phase-1-engine.md && test ! -f docs/phase-2-backend.md && test -f docs/archive/phase-1-engine.md && test -f docs/archive/phase-2-backend.md && test -f docs/vision.md && test -f docs/README.md && git log --oneline --follow -- docs/archive/phase-1-engine.md | head -2 | wc -l | grep -q 2 && git diff HEAD --stat -- docs/vision.md | grep -qE '1 insertion|1 deletion|1 \+|1 -'</automated>
  </verify>
  <done>PLAN.md deleted after confirming zero loss; both phase docs moved via git mv with history preserved and re-headered; vision.md still at docs/vision.md with exactly one sentence removed and nothing else changed; docs/README.md states the one-owner-per-audience boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Make docs/ tell the truth — API.md gains the LLM layer, design.md is trimmed to "why"</name>
  <files>docs/API.md, docs/design.md</files>
  <action>
Both halves of this task have the same rule: **read the actual backend code and get the
contract exactly right — do not guess, and do not trust the existing prose.** Where a doc and
the code disagree, the code wins. Sources to read:
`backend/api/routers/constraints.py`, `backend/api/routers/runs.py`, `backend/api/schemas.py`,
`backend/settings.py`, `backend/services/insight_service.py`, `backend/services/constraint_service.py`,
`backend/llm/` (base.py, translate.py, stub.py, gemini.py, openrouter.py).

**Part A — `docs/API.md` is missing the entire LLM layer.** Match the existing section format
exactly (see the current endpoint entries and the `## Data models` tables). Add:

- `POST /constraints` — top-level route, `scenario_id` in the request body (decision D-07),
  not nested under the scenario. Body is `ConstraintParseRequest`. Response is
  `ConstraintParseResponse` — document the real partial-apply shape: `applied[]`,
  `rejected[]`, `clarification_needed`, `no_constraint_found`, with the `AppliedConstraint`
  and `RejectedConstraint` sub-models. Note the key behaviours the shape encodes: per-call
  validation failures land in `rejected[]` rather than failing the request; only `applied[]`
  entries are persisted to the scenario's `overrides` column. Error responses: 404 scenario
  not found, 503 LLM provider unavailable. State explicitly that it does **not** trigger a
  solve (decision D-06) — the caller triggers a run separately.
- `GET /runs/{run_id}/insights` — synchronous, returns `InsightOut`, cached in the
  `runs.insight_json` column so a second call skips the provider. Document the real status
  codes from the route: the not-ready case is a deliberate `200` with `ready=false`, **not**
  a 409; and the failure code is **`502`** (provider failure or D-06 grounding-guard
  rejection) — check the route decorator and confirm rather than copying this sentence.
  Document the D-06 numeric grounding guard: any numeric token in the report that is not
  grounded in the run's metrics is rejected, so the endpoint fails rather than returning a
  fabricated number.
- Add `LLM_PROVIDER` to the `## Configuration` table: default `stub` (keeps CI keyless),
  options `stub|gemini|openrouter`. Check `default_settings()` for the other LLM env vars and
  their real defaults before deciding what else that table should carry.
- Update the `## Status code summary` table and the intro paragraph (which currently scopes the
  doc to "Phase 2") to match the endpoints the file now documents.

**Part B — `docs/design.md` currently does two jobs; only one is still its.** Trim it to the
"why", fix the rot, and fold in the shipped design:

- **Remove the phase map in §2 entirely**, and the phase-status claims in the header
  blockquote and the Status paragraph. Point to `.planning/ROADMAP.md` for status. Status
  tracking is not this document's job any more — that is the whole point of the split.
- **Fix the §2 architecture diagram's LLM-provider line. It is INVERTED from reality.**
  What actually shipped: `stub` (the default, keeps CI keyless) + Gemini (`google-genai`) +
  OpenRouter (`openai` SDK). The provider the diagram names as current was never built —
  there is no free API tier for it, and the decision to use a free provider first is logged
  in `.planning/PROJECT.md`'s Key Decisions table. Make the diagram match what exists.
  <!-- planner-discipline-allow: Claude -->
- **Fix §4's two wrong claims:** the LLM bullet names the wrong default provider (the default
  is `stub`), and the Deploy bullet names Docker Compose plus a free-tier PaaS host. Both
  `README.md` and `.planning/PROJECT.md` say AWS: frontend → S3/CloudFront; backend container → ECR +
  App Runner/ECS/EC2 — container compute, not Lambda, because CP-SAT solves are CPU-heavy and
  long-running. Note §3.1 already states the AWS target correctly; §4 contradicts it.
  **Keep** §4's frontend and what-if content as a forward-looking summary.
- **Fold the shipped LLM-layer design in as a real numbered section**, per the retired
  tracker's own documented rule ("when it ships, fold the durable design into design.md").
  This is the durable "why" that no tool regenerates. Cover: the `LLMProvider` Protocol seam;
  the provider-neutral `llm/translate.to_override_call` translation boundary (no vendor payload
  shape leaks past the seam — this is what let two real providers be added with zero seam
  changes); the `OverrideCall` domain seam with its content-hash id; overrides applied as soft
  round-2 penalties only, never able to make the model infeasible (verified across all five
  tools); the five tools (`lock_worker_shift`, `set_min_workers_per_task`,
  `exclude_worker_from_task`, `scale_demand`, `set_max_hours`); validation against real
  scenario IDs; the D-06 numeric grounding guard; and insights as a separate, cached post-run
  step so an LLM failure can never invalidate a valid schedule.
  Use `.planning/milestones/v0.3-ROADMAP.md` and `.planning/MILESTONES.md` for accurate detail
  and verify every claim against the real code in `backend/llm/` and `backend/services/`.
- **KEEP §1 (naming), §3 (engine design including §3.5 distilled-vs-production), and §5 (open
  decisions).** This is institutional knowledge no tool regenerates. Do not trim it.
- **State the ownership rule in the doc:** if `/gsd-map-codebase` could regenerate it, it does
  not belong in design.md. `.planning/codebase/*` is generated, descriptive, and agent-facing
  ("what is the code shaped like today"); design.md is hand-written and answers "why is it this
  shape, and what did we deliberately not build".
  </action>
  <verify>
    <automated>grep -q 'POST /constraints' docs/API.md && grep -q 'runs/{run_id}/insights' docs/API.md && grep -q 'LLM_PROVIDER' docs/API.md && grep -q 'insight_json' docs/API.md && grep -q '502' docs/API.md && grep -q 'no_constraint_found' docs/API.md && grep -q 'clarification_needed' docs/API.md && grep -q 'OpenRouter' docs/design.md && grep -q 'to_override_call' docs/design.md && grep -q 'ROADMAP.md' docs/design.md && grep -q 'CloudFront' docs/design.md && ! grep -qi 'Render' docs/design.md && ! grep -qi 'Gemini later' docs/design.md && ! grep -q '← \*current\*' docs/design.md && ! grep -q 'Phase map' docs/design.md && grep -q 'gsd-map-codebase' docs/design.md</automated>
  </verify>
  <done>API.md documents both LLM endpoints with contracts verified against the routers/schemas (including the 200+ready=false not-ready case and the 502 failure code) and carries LLM_PROVIDER; design.md has no phase map, no phase-status claims, a provider diagram matching stub+Gemini+OpenRouter, an AWS deploy claim consistent with README.md, a numbered shipped-LLM-layer section verified against backend/llm/ and backend/services/, §1/§3/§5 intact, and the design.md-vs-.planning/codebase ownership rule stated.</done>
</task>

<task type="auto">
  <name>Task 3: Fix PROJECT.md's false Context claims, then sweep every dangling link the split created</name>
  <files>.planning/PROJECT.md, README.md, docs/design.md, docs/README.md, docs/archive/phase-1-engine.md, docs/archive/phase-2-backend.md</files>
  <action>
**Part A — `.planning/PROJECT.md` Context section (around lines 104-107) makes three claims;
two are false and one is missing.**

- It claims `docs/decisions/` holds ADRs. **That directory does not exist and never did.**
  **Do NOT create an ADR system to make the claim true.** Correct the claim instead: decisions
  live in PROJECT.md's own Key Decisions table, `.planning/RETROSPECTIVE.md`, and design.md §5
  (open decisions).
- It calls PLAN.md the hand-written phase tracker. It was deleted in Task 1. Update the claim
  to reflect that `.planning/` owns the planning lifecycle.
- Add a pointer noting `docs/vision.md` is the origin snapshot PROJECT.md descends from.

Keep the rest of the Context section (LOC figures, Protocol seams, CP-SAT integer model,
known issues) as-is — it is accurate.

**Part B — the link sweep. A broken link is the predictable failure mode of this task.**
Deleting one file and moving two others invalidates links across the repo. Find them all:

```
grep -rn "PLAN\.md\|phase-1-engine\|phase-2-backend\|docs/decisions" \
  README.md docs/ .planning/PROJECT.md .planning/STATE.md .claude/CLAUDE.md
```

Fix every hit in those **live** documents. Known surface, at minimum:
- `README.md` — the `## Layout` block (names the tracker in the docs/ tree listing) and the
  `## Documentation` bullet list (links it). Update both: drop the tracker, add `docs/README.md`,
  keep `docs/vision.md` listed at its unchanged path, and mention `docs/archive/`. Point status
  at `.planning/`.
- `docs/design.md` — three separate references: the header blockquote, the Status paragraph,
  and the repo-layout code block in §3.1 which lists the docs/ tree. Task 2 covers the first
  two; §3.1's tree listing is easy to miss.
- `docs/archive/*.md` — each archived phase doc links back to the tracker and possibly to its
  sibling. Task 1's new headers should have handled this; verify.
- `docs/README.md` and `docs/vision.md` — confirm neither links to a deleted file.
  (vision.md links only to README/design/API — verify it needs no change, and do not edit it
  if it does not. Task 1 already froze it.)

**Two classes of hit you must NOT "fix":**
1. **False positives.** `.planning/config.json` and `.planning/milestones/v0.3-ROADMAP.md`
   match on GSD plan filenames (`01-01-PLAN.md`, `PLAN.md ## Risks`). Different file entirely.
   Leave them.
2. **History and generated output.** `.planning/milestones/**`, `.planning/RETROSPECTIVE.md`,
   and `.planning/todos/pending/*` reference the tracker as a factual record of what existed
   when written — that is correct history, leave it. `.planning/codebase/*` (STRUCTURE.md,
   INTEGRATIONS.md, CONCERNS.md) is generated by `/gsd-map-codebase` and is stale on other
   axes already (it predicts an Anthropic SDK integration that never shipped) — do not
   hand-edit; it self-heals on regeneration. This is the ownership rule Task 2 wrote into
   design.md, applied.

Report any hit you found outside the known surface rather than silently fixing it.
  </action>
  <verify>
    <automated>! grep -rEn '\]\((\.{1,2}/)*(docs/)?(PLAN\.md|phase-1-engine\.md|phase-2-backend\.md)\)' README.md docs/ .planning/PROJECT.md .planning/STATE.md .claude/CLAUDE.md && ! grep -rn 'docs/decisions' .planning/PROJECT.md README.md docs/ && grep -q 'vision.md' .planning/PROJECT.md && grep -q 'docs/README.md' README.md && grep -q 'docs/vision.md' README.md && ! test -d docs/decisions</automated>
  </verify>
  <done>PROJECT.md's Context section names no ADR directory, no longer calls the deleted tracker current, and points to docs/vision.md as the origin snapshot; no ADR directory was created; zero markdown links to the deleted/moved files survive in README.md, docs/, PROJECT.md, STATE.md, or CLAUDE.md; historical and generated artifacts were left untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Docs-only change. No code, no dependencies, no runtime surface, no new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-vel-01 | Information Disclosure | docs/API.md, docs/design.md | low | mitigate | New docs describe the LLM layer's config surface. Name env vars only (`LLM_PROVIDER`, key var names) — never a key value or a real key from `backend/.env`. `Settings` marks `llm_api_key`/`openrouter_api_key` `repr=False` (T-04-01) for the same reason; the docs must not undo that. |
| T-vel-02 | Tampering | none | low | accept | No package installs, no dependency changes, no executable content. Package legitimacy gate not applicable. |
</threat_model>

<verification>
Docs-only — no test suite is affected and none should change. Confirm the backend is untouched
and the split holds:

```bash
git diff --stat HEAD -- backend/            # must be empty: no backend changes
git status --porcelain docs/                # PLAN.md deleted; archive/ + README.md added
ls docs/                                    # API.md README.md archive design.md vision.md
```

Then re-run the Task 3 link sweep against the whole repo and confirm every remaining hit is a
GSD-plan false positive or a deliberate historical/generated reference.
</verification>

<success_criteria>
- `docs/PLAN.md` gone; nothing it tracked was lost (verified against `.planning/` before deletion).
- `docs/API.md` documents `POST /constraints` and `GET /runs/{run_id}/insights` with contracts
  matching `backend/` code exactly, plus `LLM_PROVIDER` (default `stub`).
- `docs/design.md` holds only "why": no phase map, no status, provider diagram matching reality
  (stub + Gemini + OpenRouter), AWS deploy target, the shipped LLM-layer design folded in as a
  numbered section, §1/§3/§5 intact, ownership rule stated.
- `docs/archive/` holds both phase docs with history preserved (`git log --follow` works).
- `docs/vision.md` unchanged at its original path except the removed sentence.
- `docs/README.md` states the one-owner-per-audience boundary.
- `.planning/PROJECT.md` Context makes no false ADR/tracker claims; no ADR directory created.
- Zero dangling markdown links in live documents; history and generated output untouched.
- `git diff --stat HEAD -- backend/` is empty.
</success_criteria>

<output>
Create `.planning/quick/260715-vel-retire-docs-as-planning-tracker-split-by/260715-vel-SUMMARY.md` when done
</output>
</content>
</invoke>
