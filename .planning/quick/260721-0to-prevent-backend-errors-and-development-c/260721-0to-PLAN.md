---
phase: quick-260721-0to
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/errors.ts
  - frontend/src/components/layout/ErrorBanner.tsx
  - frontend/src/components/layout/ErrorBanner.test.tsx
  - frontend/src/components/layout/RootErrorBoundary.tsx
  - frontend/src/routes/router.test.tsx
  - frontend/src/routes/ResultsView.tsx
  - frontend/src/routes/ResultsView.test.tsx
  - frontend/src/components/runs/RunHistoryTable.tsx
  - frontend/src/components/runs/RunHistoryTable.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users see short, actionable product copy when data cannot be loaded; no backend launch command, working directory, API jargon, or browser-console instruction appears in rendered UI."
    - "Users see one safe generic explanation for a failed run; the exception class/message persisted in RunOut.error never appears in run history or the results route."
    - "Diagnostic error objects remain available to console logging for developers, but are never used as JSX render input on app-level failure surfaces."
  artifacts:
    - path: "frontend/src/lib/errors.ts"
      provides: "Single shared source of user-safe app failure copy"
      exports: ["USER_ERROR_COPY", "getErrorStatus"]
    - path: "frontend/src/components/layout/ErrorBanner.tsx"
      provides: "Neutral connectivity/request-failure banner without development instructions"
    - path: "frontend/src/components/layout/RootErrorBoundary.tsx"
      provides: "Neutral crash fallback without developer-only remediation"
    - path: "frontend/src/routes/ResultsView.tsx"
      provides: "Failed-run route that does not render RunOut.error"
    - path: "frontend/src/components/runs/RunHistoryTable.tsx"
      provides: "Failed-run table rows that do not render RunOut.error"
  key_links:
    - from: "frontend/src/lib/errors.ts"
      to: "frontend/src/components/layout/ErrorBanner.tsx"
      via: "USER_ERROR_COPY connection copy imported as the only rendered error text"
      pattern: "USER_ERROR_COPY"
    - from: "frontend/src/lib/errors.ts"
      to: "frontend/src/routes/ResultsView.tsx"
      via: "USER_ERROR_COPY run-failure copy replaces direct RunOut.error rendering"
      pattern: "USER_ERROR_COPY.*run"
    - from: "frontend/src/lib/errors.ts"
      to: "frontend/src/components/runs/RunHistoryTable.tsx"
      via: "The same run-failure copy is reused in history and detail views"
      pattern: "USER_ERROR_COPY.*run"
---

<objective>
Prevent backend diagnostics and developer-only remediation from appearing in the frontend, including the reported server launch command and the related raw failed-run exception path.

Purpose: Keep user-facing failures actionable and product-oriented while preserving technical diagnostics outside rendered UI.
Output: Shared safe error copy, hardened connectivity/crash/run-failure surfaces, and regression tests proving diagnostic strings are absent from the DOM.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.codex/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Existing typed status accessor; extend this module as the single owner of fixed user-safe error copy.
@frontend/src/lib/errors.ts

# App-level request and render-failure surfaces. Both already avoid rendering their error object, but their fixed copy currently exposes developer guidance.
@frontend/src/components/layout/ErrorBanner.tsx
@frontend/src/components/layout/RootErrorBoundary.tsx

# RunOut.error is populated from the caught exception class and message, so it is diagnostic data rather than safe product copy.
@backend/services/run_service.py
@frontend/src/routes/ResultsView.tsx
@frontend/src/components/runs/RunHistoryTable.tsx
</context>

<source_audit>

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Prevent backend errors and development commands from being displayed in the frontend | 01 | COVERED | Task 1 removes developer remediation; Task 2 removes persisted exception rendering. |
| REQ | — | No roadmap requirement IDs are assigned to this ad-hoc quick task | — | N/A | Quick task is outside the completed milestone roadmap. |
| RESEARCH | — | No research artifact requested or needed for established local React/Vitest patterns | — | N/A | Level 0 discovery. |
| CONTEXT | — | No discussion/context artifact was created | — | N/A | User intent is explicit. |

</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace developer-oriented app error instructions with safe product copy</name>
  <files>frontend/src/lib/errors.ts, frontend/src/components/layout/ErrorBanner.tsx, frontend/src/components/layout/ErrorBanner.test.tsx, frontend/src/components/layout/RootErrorBoundary.tsx, frontend/src/routes/router.test.tsx</files>
  <behavior>
    - Test 1: Given an ErrorBanner error whose message contains a server launch command, backend path, stack trace, or exception text, the DOM contains only neutral retry/reload copy and none of the supplied diagnostic content.
    - Test 2: Given a route render exception, RootErrorBoundary keeps the fixed heading and Reload button, renders neutral reload guidance, and renders neither the exception nor an instruction to inspect developer tooling.
    - Test 3: Empty, undefined, and non-Error inputs continue to render the same fixed connection-failure copy.
  </behavior>
  <action>
    Start by updating the component tests to express the safe-copy contract, then implement it. Add a typed `USER_ERROR_COPY` constant to `frontend/src/lib/errors.ts` as the single owner of fixed connection, unexpected-crash, and run-failure titles/descriptions; preserve `getErrorStatus` unchanged. Use concise product wording: the connection branch says the content could not be loaded and suggests retrying/reloading; the crash branch says to reload and try again; neither names infrastructure, commands, directories, APIs, consoles, stack traces, or exception classes.

    Update `ErrorBanner` to render the shared connection copy and remove the inline code/remediation markup. Keep `console.error(error)` so developers retain diagnostics outside the rendered page, and update the module comment to make the UI/console boundary explicit. Update `RootErrorBoundary` to render the shared unexpected-crash copy while keeping `useRouteError`, console logging, and the Reload button behavior intact.

    Rewrite the affected tests: feed `ErrorBanner` a diagnostic-rich message (including the reported launch command and a backend file path), positively assert the new fixed copy, and assert the diagnostic fragments are absent from the DOM. Update the router boundary test to expect the neutral guidance and prove both the thrown message and the old developer-tool instruction are absent. Do not remove console logging and do not branch copy on error-message text.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/components/layout/ErrorBanner.test.tsx src/routes/router.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>ErrorBanner and RootErrorBoundary render only shared, user-safe retry/reload copy for every error shape; developer launch instructions and console guidance are absent from the DOM; focused tests and typecheck pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop failed-run views from rendering persisted backend exceptions</name>
  <files>frontend/src/routes/ResultsView.tsx, frontend/src/routes/ResultsView.test.tsx, frontend/src/components/runs/RunHistoryTable.tsx, frontend/src/components/runs/RunHistoryTable.test.tsx</files>
  <behavior>
    - Test 1: A FAILED run with an exception-shaped RunOut.error renders the shared generic run-failure copy in ResultsView and does not render any part of the exception string.
    - Test 2: The same FAILED run renders the same generic copy in RunHistoryTable and does not render any part of the exception string.
    - Test 3: A FAILED run with a null or empty error renders exactly the same generic copy, so the UI behavior cannot reveal whether diagnostic details were recorded.
    - Test 4: Failed branches still suppress completed-run result sections and preserve existing navigation/status behavior.
  </behavior>
  <action>
    Update the failed-run tests first with diagnostic-rich inputs that resemble `run_service._execute` output (exception class, message, file/path or launch-command fragments). Assert the shared run-failure description is present and every diagnostic fragment is absent.

    In `ResultsView`, replace the `run.error`/local-fallback expression with `USER_ERROR_COPY`'s run-failure description and remove the now-obsolete fallback constant/comment. Preserve branch precedence, the destructive Alert, the "Run Failed" title, and the rule that no results body renders for FAILED status. In `RunHistoryTable`, replace the corresponding inline `run.error` expression with the same shared description and remove the duplicate fallback constant. Preserve status labels, row navigation, wrapping/layout, and all non-failed states. Update comments that currently describe verbatim error rendering so they document the information-disclosure boundary instead.

    Do not alter `backend/services/run_service.py`, the RunOut API schema, or persistence: operators still need the stored diagnostic for debugging. This task changes only what reaches JSX. Do not sanitize by pattern-matching known exception formats; make the rendered copy independent of `run.error` so unknown future diagnostics are safe by default.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/routes/ResultsView.test.tsx src/components/runs/RunHistoryTable.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Neither failed-run UI reads RunOut.error as render input; both views show the same fixed user-safe explanation for populated, empty, or null diagnostic fields; focused tests and typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend/API → React UI | Error objects and RunOut.error cross from server/runtime diagnostics into browser state and must be treated as untrusted, non-display data. |
| React Router/runtime → crash fallback | Render and route exceptions may contain URLs, source paths, stack data, or library internals and must not become visible page copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0to-01 | Information Disclosure | ErrorBanner, RootErrorBoundary | medium | mitigate | Render only fixed `USER_ERROR_COPY` values; retain the caught object solely for console logging; tests inject diagnostic-rich errors and assert their absence from the DOM. |
| T-0to-02 | Information Disclosure | ResultsView, RunHistoryTable | medium | mitigate | Never render `RunOut.error`; both FAILED branches use one fixed shared message, with regression coverage for populated/null/empty diagnostics. |
| T-0to-03 | Tampering | Server-influenced text rendered elsewhere as JSX text children | low | accept | Existing domain response strings remain escaped by React and are intentionally user-facing validation/insight content; the audit found no additional raw exception/detail render path in those components. |
</threat_model>

<verification>
From `frontend/`, run:

1. `npm test -- src/components/layout/ErrorBanner.test.tsx src/routes/router.test.tsx src/routes/ResultsView.test.tsx src/components/runs/RunHistoryTable.test.tsx`
2. `npm run typecheck`
3. `npm run lint`

The focused tests must prove injected development commands, backend paths, exception text, and browser-console guidance never appear in rendered UI while retry/reload affordances and failed-state branching remain intact.
</verification>

<success_criteria>
- No rendered frontend copy contains a backend launch command, backend working-directory instruction, API jargon, or browser-console instruction.
- ErrorBanner and RootErrorBoundary continue to log their caught errors to the console but render only fixed safe copy.
- ResultsView and RunHistoryTable never render `RunOut.error`, regardless of whether it is populated, empty, or null.
- Domain validation feedback in TranscriptEntry remains unchanged because it is intentionally user-actionable rather than an uncaught backend diagnostic.
- Focused Vitest tests, TypeScript typecheck, and frontend lint pass.
</success_criteria>

<output>
Create `.planning/quick/260721-0to-prevent-backend-errors-and-development-c/260721-0to-SUMMARY.md` when done.
</output>
