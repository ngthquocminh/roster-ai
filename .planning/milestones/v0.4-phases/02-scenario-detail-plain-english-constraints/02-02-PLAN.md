---
phase: 02-scenario-detail-plain-english-constraints
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/openapi.json
  - frontend/src/api/schema.d.ts
  - frontend/src/api/scenarios.ts
  - frontend/src/api/constraints.ts
  - frontend/src/api/constraints.test.ts
  - frontend/src/components/ui/textarea.tsx
autonomous: true
requirements: [SCEN-03, CONS-01, CONS-05]
tags: [frontend, api-client, openapi-typescript, codegen, shadcn]

must_haves:
  truths:
    - "schema.d.ts is regenerated from the live backend OpenAPI schema after the D-01 change and contains the path \"/scenarios/{scenario_id}/overrides\"."
    - "getScenario and getScenarioOverrides call the typed client with the scenario_id path param and attach response.status on error (the mechanism the Editor's 404 view and CONS-05's 503-vs-422 branch depend on)."
    - "applyConstraint POSTs /constraints and, on error, throws {status: response.status, ...error} so callers can distinguish 503 (provider down) from 422 (validation) — never by parsing message text (CONS-05)."
    - "applyConstraint resolves to the full ConstraintParseResponse (applied[], rejected[], clarification_needed, no_constraint_found) so downstream rendering has every outcome field."
    - "Request/response types for all three wrappers are derived via indexed access into the generated paths type — no hand-authored payload interface exists in these files."
    - "The Textarea component is sourced from the official shadcn registry into frontend/src/components/ui/textarea.tsx (a source-file copy, not an npm dependency)."
  artifacts:
    - "frontend/src/api/schema.d.ts — regenerated, includes the overrides path"
    - "frontend/src/api/scenarios.ts — getScenario, getScenarioOverrides added"
    - "frontend/src/api/constraints.ts — applyConstraint wrapper (new file)"
    - "frontend/src/api/constraints.test.ts — wrapper unit test (new file)"
    - "frontend/src/components/ui/textarea.tsx — shadcn Textarea (new file)"
  key_links:
    - "The overrides path must exist in schema.d.ts (regenerated) before getScenarioOverrides can type paths[\"/scenarios/{scenario_id}/overrides\"] — codegen runs first."
    - "applyConstraint's throw-shape must match createScenario's exactly ({status, ...error}) so the shared status-branch convention (T-1-02) holds."
  prohibitions:
    - "No wrapper in these files may hand-author a request/response interface mirroring docs/API.md prose — types are derived from generated paths only (drift history, commit 93ca4e0)."
    - "Error handling must never branch on error message text to distinguish HTTP outcomes — only response.status is a reliable discriminator."
---

<objective>
Regenerate the typed client against the D-01 backend change and add the three typed endpoint wrappers the Editor needs, plus the one new shadcn primitive (Textarea) for the constraint box. This is the frontend data-access layer: no UI, no hooks yet — just the typed HTTP surface and its unit test.

Purpose: Every request/response type stays generated (never hand-written); the constraint wrapper attaches HTTP status so CONS-05's 503-vs-422 distinction is possible downstream.
Output: regenerated `schema.d.ts`; `getScenario` + `getScenarioOverrides` in `scenarios.ts`; new `constraints.ts` with `applyConstraint`; `constraints.test.ts`; `textarea.tsx`.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-CONTEXT.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-RESEARCH.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-PATTERNS.md
@.planning/phases/02-scenario-detail-plain-english-constraints/02-01-SUMMARY.md
</context>

<artifacts_produced>
New symbols this plan creates:
- `getScenario(scenarioId)` and `getScenarioOverrides(scenarioId)` — added to `frontend/src/api/scenarios.ts`
- `applyConstraint(body)` — new `frontend/src/api/constraints.ts`
- Regenerated `frontend/src/api/schema.d.ts` gaining `paths["/scenarios/{scenario_id}/overrides"]`
- `frontend/openapi.json` regenerated (codegen intermediate)
- `Textarea` component — `frontend/src/components/ui/textarea.tsx`
- `frontend/src/api/constraints.test.ts`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regenerate schema.d.ts and add the three typed endpoint wrappers</name>
  <files>frontend/openapi.json, frontend/src/api/schema.d.ts, frontend/src/api/scenarios.ts, frontend/src/api/constraints.ts</files>
  <behavior>
    - After `npm run codegen`, schema.d.ts contains `paths["/scenarios/{scenario_id}/overrides"]["get"]`.
    - getScenario(id) returns data on 200 and throws {status, ...error} on failure.
    - getScenarioOverrides(id) returns the overrides array on 200 and throws {status, ...error} on failure.
    - applyConstraint({scenario_id, text}) returns ConstraintParseResponse on 200 and throws {status: 503|422|404, ...error} on failure.
  </behavior>
  <read_first>
    - frontend/src/api/scenarios.ts (whole file) — the exact wrapper patterns: `listScenarios` (bare GET, plain throw) and `createScenario` (POST + `throw { status: response.status, ...error }`). Indexed-access typing lives here (CreateScenarioBody).
    - frontend/src/api/client.ts — the single `client` instance the wrappers call.
    - frontend/package.json scripts — `codegen` = `codegen:export` (runs `../backend` export_openapi.py) + `codegen:types` (openapi-typescript). Run `npm run codegen` from `frontend/`.
    - docs/API.md POST /constraints (lines 141-198) — the ConstraintParseRequest/Response contract the wrapper's derived types mirror.
    - .planning/phases/.../02-PATTERNS.md sections "frontend/src/api/scenarios.ts" and "frontend/src/api/constraints.ts" — verbatim shapes.
  </read_first>
  <action>
    From frontend/, run `npm run codegen` (regenerates openapi.json from the running backend export script, then schema.d.ts) so `paths["/scenarios/{scenario_id}/overrides"]` exists. Confirm the D-01 backend change (plan 02-01) is present before running — the path won't appear otherwise.
    In frontend/src/api/scenarios.ts add getScenario(scenarioId: string): call client.GET("/scenarios/{scenario_id}", { params: { path: { scenario_id: scenarioId } } }); on error throw { status: response.status, ...error } (the Editor needs status to render the 404 terminal view vs ErrorBanner — same T-1-02 convention). Add getScenarioOverrides(scenarioId: string) identically against "/scenarios/{scenario_id}/overrides".
    Create frontend/src/api/constraints.ts (new): import the shared client and paths; derive ConstraintParseRequest via indexed access `paths["/constraints"]["post"]["requestBody"]["content"]["application/json"]`; export applyConstraint(body) that POSTs "/constraints" and on error throws { status: response.status, ...error }, else returns data (the full ConstraintParseResponse). Do not hand-author any payload interface.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/scenarios/{scenario_id}/overrides' frontend/src/api/schema.d.ts` returns at least 1 (codegen picked up the new backend path).
    - `frontend/src/api/scenarios.ts` exports `getScenario` and `getScenarioOverrides`, both attaching `response.status` on the error throw.
    - `frontend/src/api/constraints.ts` exists, derives ConstraintParseRequest via indexed access into `paths`, and its error throw includes `status: response.status`.
    - `npm run typecheck` (`tsc --noEmit`) passes — the derived types resolve against the regenerated schema.
  </acceptance_criteria>
  <done>The typed client knows the overrides endpoint, and all three wrappers exist with the status-attach error convention, fully generated-typed.</done>
</task>

<task type="auto">
  <name>Task 2: Wrapper unit test + add the shadcn Textarea primitive</name>
  <files>frontend/src/api/constraints.test.ts, frontend/src/components/ui/textarea.tsx</files>
  <read_first>
    - frontend/src/api/scenarios.test.ts (whole file) — the exact `vi.mock("./client")` boundary-mock pattern and the `describe("createScenario")` success/error assertions to mirror. NOTE the header comment: this repo mocks at the client boundary, NOT with msw (msw was previously rejected — do not introduce it).
    - frontend/src/components/ui/input.tsx and frontend/src/components/ui/button.tsx — confirm the shadcn source-copy shape already present (Textarea joins them via `npx shadcn add textarea`).
    - .planning/phases/.../02-PATTERNS.md section "frontend/src/api/constraints.test.ts".
    - .planning/phases/.../02-UI-SPEC.md Registry Safety — official registry only, no vetting gate.
  </read_first>
  <action>
    Create frontend/src/api/constraints.test.ts mirroring scenarios.test.ts: vi.mock("./client") exposing a mocked POST; a `describe("applyConstraint")` with a success case (mocked POST resolves { data: <ConstraintParseResponse>, error: undefined } → applyConstraint returns that data) and error cases asserting the wrapper rejects with an object carrying `status: 503` and, separately, `status: 422` (CONS-05's exact discriminator). Do not use msw.
    From frontend/, run `npx shadcn add textarea` to copy the official Textarea into frontend/src/components/ui/textarea.tsx. Do not edit it beyond what the CLI produces; confirm it composes the native textarea with existing cn/Tailwind utilities (mirrors input.tsx) and adds no new package.json dependency.
  </action>
  <verify>
    <automated>cd frontend && npm test -- constraints && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- constraints` passes: applyConstraint returns data on success and rejects with {status:503} and {status:422} on the two error cases.
    - `frontend/src/api/constraints.test.ts` does not import or reference msw.
    - `frontend/src/components/ui/textarea.tsx` exists and exports a `Textarea` component; `git diff frontend/package.json` shows no new dependency added by the shadcn CLI.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The constraint wrapper is unit-tested at the client boundary, and the Textarea primitive is available for the constraint input in plan 02-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → FastAPI | The three wrappers carry untrusted `scenario_id`/`text` to the API; responses (server-influenced strings) return |
| shadcn official registry → local source tree | Textarea component file copied into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | wrapper error handling | low | mitigate | Wrappers attach `response.status` and re-throw the error object but the UI layer (later plans) never renders raw backend error text (T-1-02 / ASVS V7); this plan introduces no new render surface. |
| T-02-06 | Tampering | Textarea from shadcn registry | low | accept | Official shadcn registry only (UI-SPEC Registry Safety — no vetting gate); it is a source-file copy composing existing deps, not a new npm package. Confirmed by the no-new-dependency acceptance check. |
| T-02-07 | Spoofing/Tampering | 503-vs-422 discrimination | medium | mitigate | Branch strictly on `response.status`, never on error message text — a fabricated 503-looking body cannot masquerade as a validation error and vice-versa; this is the CONS-05 load-bearing mechanism, unit-tested here. |
| T-02-SC | Tampering | dependency installs | low | accept | No `npm install` of a new package this plan; Textarea is a registry source copy (RESEARCH "Package Legitimacy Audit — Not applicable this phase"). |

Block-on = high; no high-severity threats. T-02-07 (medium) is mitigated + tested.
</threat_model>

<verification>
- `cd frontend && npm run codegen && npm run typecheck` — schema regenerated and types resolve.
- `cd frontend && npm test -- constraints` — wrapper contract (success + 503/422 rejects).
- `grep -c '/scenarios/{scenario_id}/overrides' frontend/src/api/schema.d.ts` ≥ 1.
</verification>

<success_criteria>
- The overrides endpoint is known to the typed client; three status-attaching wrappers exist; Textarea is available; constraint wrapper is tested at the client boundary.
</success_criteria>

<output>
Create `.planning/phases/02-scenario-detail-plain-english-constraints/02-02-SUMMARY.md` when done.
</output>
