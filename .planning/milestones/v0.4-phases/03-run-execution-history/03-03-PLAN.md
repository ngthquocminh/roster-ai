---
phase: 03-run-execution-history
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/runs/RunStatusLabel.tsx
  - frontend/src/components/runs/RunStatusLabel.test.tsx
  - frontend/src/components/runs/RunHistoryTable.tsx
  - frontend/src/components/runs/RunHistoryTable.test.tsx
autonomous: true
requirements: [RUN-04, RUN-05]
must_haves:
  truths:
    - "The run history table renders one row per RunOut, newest-first in the server's returned order with no client re-sort, keyed by run.id (UI-SPEC E2 populated)."
    - "A single run renders with identical chrome to many runs; no row-count label appears anywhere (UI-SPEC E2 zero-one-many)."
    - "While runsQuery is loading the table shows a centered spinner + 'Loading runs…' and no rows (UI-SPEC E2 loading)."
    - "On a list-fetch error the table renders ErrorBanner and no partial or stale rows (UI-SPEC E2 error)."
    - "With zero runs the table renders the 'No runs yet' empty state with 'Trigger a run to solve this scenario.' and an inline 'Run Scenario' button, and no empty table chrome (UI-SPEC E2 empty)."
    - "Nullable started_at/finished_at render as a '—' em-dash placeholder rather than a blank or broken cell (UI-SPEC E2 partial)."
    - "The table body scrolls inside a fixed-height max-h-[420px] overflow-y-auto container past ~10 rows (UI-SPEC E2 overflow)."
    - "Each row's status renders as a lucide icon + text label from runStatusMeta — never a Badge; RUNNING uses LoaderCircle with animate-spin (UI-SPEC E2 populated / status mapping)."
    - "A FAILED row renders its run.error verbatim, wrapped, directly beneath the 'Failed' label — not hidden behind a click (UI-SPEC E5 populated / RUN-05)."
    - "A FAILED row whose error is null renders 'Failed — no error details were recorded.' — never a blank space where the reason belongs (UI-SPEC E5 empty defensive; edge RUN-05/empty)."
    - "run.error and all cell text render only as JSX text children (React auto-escaping); HTML- or unicode-looking error text renders as literal visible text with no element created from it (edge RUN-05/encoding; T-1-03 convention)."
    - "A COMPLETED run renders the neutral Check + 'Completed' treatment regardless of solver_status, and solver_status is never rendered in this table."
    - statement: "A long or multi-line FAILED error string wraps inside its cell without breaking the table's column widths"
      verification: backstop
  prohibitions:
    - "A COMPLETED run must not be rendered as a failure or warning on account of solver_status being UNKNOWN."
  artifacts:
    - frontend/src/components/runs/RunStatusLabel.tsx
    - frontend/src/components/runs/RunStatusLabel.test.tsx
    - frontend/src/components/runs/RunHistoryTable.tsx
    - frontend/src/components/runs/RunHistoryTable.test.tsx
  key_links:
    - "RunHistoryTable consumes a runsQuery prop (UseQueryResult<RunOut[]>) — the single useRuns instance owned by RunHistory (03-05) — mirroring OverridesList's overridesQuery prop; it never calls useRuns itself."
    - "Row click navigates to /scenarios/:scenarioId/runs/:runId (the pre-existing Phase-4 route), same tabIndex/onClick/onKeyDown Enter+Space pattern as ScenarioTable."
    - "RunStatusLabel and the FAILED-error color both consume runStatusMeta / the destructive token from lib/runStatus (03-01) — one status vocabulary."
---

<objective>
Build the read surface for RUN-04 and RUN-05: RunStatusLabel (the reusable icon+text status cell) and RunHistoryTable (every run for a scenario, newest-first, with status, created/started/finished timing, and a FAILED run's error shown inline). This is where prior runs become visible and a failure stops being "merely absent".

Purpose: Deliver the history table as a faithful clone of ScenarioTable/OverridesList's proven state machine (loading/error/empty/populated/overflow), extended with the run-status icon convention and the RUN-05 inline-error requirement, without inventing a second status idiom (no Badge) and without ever rendering solver_status.
Output: frontend/src/components/runs/RunStatusLabel.tsx (+ test) and frontend/src/components/runs/RunHistoryTable.tsx (+ test).
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-run-execution-history/03-UI-SPEC.md
@frontend/src/components/scenarios/ScenarioTable.tsx
@frontend/src/components/scenarios/ScenarioTable.test.tsx
@frontend/src/components/editor/OverridesList.tsx
@frontend/src/components/editor/TranscriptEntry.tsx
@frontend/src/components/layout/ErrorBanner.tsx
@frontend/src/lib/runStatus.ts
@frontend/src/api/schema.d.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reusable status cell (RunStatusLabel)</name>
  <files>frontend/src/components/runs/RunStatusLabel.tsx, frontend/src/components/runs/RunStatusLabel.test.tsx</files>
  <read_first>
    - frontend/src/components/editor/TranscriptEntry.tsx (the icon + text convention: lucide icon with size-4 + aria-hidden, neutral vs destructive text — the pattern RunStatusLabel generalizes; note there is NO Badge)
    - frontend/src/lib/runStatus.ts (runStatusMeta — the source of icon/label/className/spin)
    - .planning/phases/03-run-execution-history/03-UI-SPEC.md (Color section status mapping table)
  </read_first>
  <behavior>
    - RunStatusLabel status="PENDING" shows a Clock icon and the text "Queued", muted.
    - RunStatusLabel status="RUNNING" shows a spinning LoaderCircle (animate-spin) and the text "Running", muted.
    - RunStatusLabel status="COMPLETED" shows a Check icon and the text "Completed", neutral foreground (not destructive, not accent, not green).
    - RunStatusLabel status="FAILED" shows an X icon and the text "Failed", destructive.
    - RunStatusLabel with an unknown status shows the neutral fallback icon and the raw status string as its label.
  </behavior>
  <action>
    Create frontend/src/components/runs/RunStatusLabel.tsx exporting `RunStatusLabel({ status }: { status: string })`. Read `runStatusMeta(status)` from @/lib/runStatus and render the returned `Icon` (className `size-4 shrink-0` plus the meta className, adding `animate-spin` when meta.spin is set, `aria-hidden="true"`) next to a text span holding meta.label styled Body (14px) with the meta className — an inline flex row with a small gap, exactly mirroring TranscriptEntry's icon+text rows. Do NOT introduce a Badge or any pill component. Create frontend/src/components/runs/RunStatusLabel.test.tsx asserting each of the four statuses renders the correct label text and that RUNNING's icon carries animate-spin; assert an unknown status renders its raw value as the label.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/runs/RunStatusLabel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RunStatusLabel renders the exact labels "Queued"/"Running"/"Completed"/"Failed" for the four statuses (test asserts each).
    - The RUNNING variant's icon element has the animate-spin class (test asserts).
    - An unknown status renders its raw string as the visible label (test asserts).
    - No import of a Badge component appears in the file.
    - `cd frontend && npx vitest run src/components/runs/RunStatusLabel.test.tsx` exits 0.
  </acceptance_criteria>
  <done>RunStatusLabel renders the four run statuses as the UI-SPEC icon+text mapping (no Badge), with an honest fallback for unknown statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run history table with inline failure text (RunHistoryTable)</name>
  <files>frontend/src/components/runs/RunHistoryTable.tsx, frontend/src/components/runs/RunHistoryTable.test.tsx</files>
  <read_first>
    - frontend/src/components/scenarios/ScenarioTable.tsx (the loading/empty/populated/overflow state machine, table-fixed + column widths, max-h-[420px] overflow-y-auto container, and the row tabIndex/onClick/onKeyDown Enter+Space navigation to replicate)
    - frontend/src/components/scenarios/ScenarioTable.test.tsx (the createMemoryRouter navigation-assertion convention and the HTML-looking-text escaping test to mirror for the error field)
    - frontend/src/components/editor/OverridesList.tsx (the "takes a UseQueryResult prop and renders loading/error/empty/populated from it" pattern — the exact shape RunHistoryTable uses; note the isSuccess guard is NOT needed here because runsQuery is not a dependent/gated query)
    - frontend/src/components/editor/TranscriptEntry.tsx (whitespace-pre-wrap break-words for wrapping server-provided error text)
    - frontend/src/components/layout/ErrorBanner.tsx (reused verbatim for the error state)
    - frontend/src/lib/runStatus.ts (the RunOut type via generated components; status vocabulary)
    - .planning/phases/03-run-execution-history/03-UI-SPEC.md (Application Structure run history table; Copywriting Contract rows for empty/loading/error/timestamp placeholder/FAILED error/null-error fallback)
  </read_first>
  <behavior>
    - Loading: runsQuery.isLoading true → centered spinner + "Loading runs…", zero rows.
    - Error: runsQuery.isError true → ErrorBanner only, zero rows.
    - Empty: data [] → "No runs yet" heading + "Trigger a run to solve this scenario." + an inline "Run Scenario" button wired to onTriggerRun; no table element.
    - Populated: one row per run in supplied order, keyed by run.id; columns Status | Created | Started | Finished; Status cell uses RunStatusLabel.
    - Nullable started_at/finished_at → "—" placeholder; created_at rendered verbatim.
    - FAILED row: run.error rendered verbatim and wrapped directly beneath the "Failed" label; when error is null → "Failed — no error details were recorded."
    - A COMPLETED run with solver_status "UNKNOWN" renders as Completed/neutral; solver_status never appears in the DOM.
    - An error string containing HTML renders as literal text with no element created from it.
    - Row click / Enter / Space navigates to /scenarios/:scenarioId/runs/:runId.
    - One row renders identical chrome to many; no count label.
  </behavior>
  <action>
    Create frontend/src/components/runs/RunHistoryTable.tsx exporting `RunHistoryTable({ runsQuery, scenarioId, onTriggerRun }: { runsQuery: UseQueryResult<RunOut[], unknown>; scenarioId: string; onTriggerRun?: () => void })` where RunOut is `components["schemas"]["RunOut"]` from @/api/schema. Replicate OverridesList/ScenarioTable state handling in order: isLoading → centered LoaderCircle spinner + "Loading runs…"; isError → `<ErrorBanner error={runsQuery.error} />`; `data ?? []` length 0 → the "No runs yet" empty block with the "Trigger a run to solve this scenario." body and an inline `<Button onClick={onTriggerRun}>Run Scenario</Button>` (a plain Button + callback, exactly like ScenarioTable's empty-state onCreateScenario — NOT the stateful TriggerRunButton from 03-04, to avoid a cross-plan coupling); otherwise the populated table. Use a `Table className="table-fixed"` with a `max-h-[420px] overflow-y-auto rounded-md border border-border` wrapper (same as ScenarioTable) and four `TableHead`s Status / Created / Started / Finished with explicit widths so a long error cannot widen columns. For each run map a `TableRow` keyed by run.id with tabIndex 0, cursor-pointer, and onClick/onKeyDown(Enter, Space) calling `navigate(\`/scenarios/${scenarioId}/runs/${run.id}\`)` via useNavigate (mirror ScenarioTable exactly). In the Status cell render `<RunStatusLabel status={run.status} />`; when run.status is "FAILED" render, directly beneath it, a Body-size destructive paragraph with `whitespace-pre-wrap break-words` holding `run.error` verbatim, or the fixed fallback copy "Failed — no error details were recorded." when run.error is null/empty. Render created_at verbatim and render started_at/finished_at as their value or a muted "—" em dash when null. Never read or render run.solver_status anywhere. Do NOT use dangerous raw-HTML rendering — every value is a plain JSX text child. Create frontend/src/components/runs/RunHistoryTable.test.tsx mirroring ScenarioTable.test.tsx (mock the query result object directly, render inside a createMemoryRouter with a :runId probe route): cover loading, error (ErrorBanner + zero rows), empty (heading + inline button + no table), populated newest-first order, nullable-timestamp "—", FAILED inline error verbatim, FAILED null-error fallback copy, a COMPLETED+solver_status:"UNKNOWN" run showing "Completed" and never the word "UNKNOWN", an HTML-looking error rendered as literal text with no created element, single-vs-many identical chrome with no count label, and row-click navigation to /scenarios/:scenarioId/runs/:runId.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/runs/RunHistoryTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The component takes a runsQuery prop and renders loading spinner / ErrorBanner / empty state / populated table from it (test covers all four).
    - Populated rows appear in the supplied order, keyed by run.id, with columns Status/Created/Started/Finished; a single run has identical chrome to many and no count label is present (test asserts).
    - Null started_at/finished_at render "—" (test asserts a PENDING run shows "—" for both).
    - A FAILED row shows run.error verbatim beneath "Failed"; a FAILED row with null error shows "Failed — no error details were recorded." (tests assert both).
    - A COMPLETED run whose solver_status is "UNKNOWN" renders "Completed" and the DOM does not contain "UNKNOWN" (test asserts the negative on rendered output).
    - An HTML-looking error string renders as literal text with no matching element created (test mirrors ScenarioTable's T-1-03 assertion; querySelector for the injected tag is null).
    - Row click navigates to /scenarios/:scenarioId/runs/:runId (test asserts via memory-router probe).
    - `cd frontend && npx vitest run src/components/runs/RunHistoryTable.test.tsx` exits 0.
  </acceptance_criteria>
  <done>RunHistoryTable renders every run newest-first with status/timing, shows a FAILED run's error inline (with a defensive null fallback), never renders solver_status, and navigates on row activation.</done>
</task>

</tasks>

<flagged_assumptions>
- Spec-less edge probe RUN-04 (category: unclassified — "review manually") is surfaced here rather than auto-resolved. Planner assumption: RUN-04's only classifiable edges beyond the UI-SPEC-covered states (empty / single-vs-many / newest-first / nullable timing) are the timestamp-null placeholder and the zero-one-many identity, both already authored as covered truths above. No additional testable predicate is inferred; recorded so the row is not silently dropped. No-silent-drop accounting for this plan: this flagged assumption + the RUN-05/empty and RUN-05/encoding covered truths + the E2/E5 long-text backstop together account for the RUN-04 and RUN-05 probe rows owned by this plan.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend RunOut.error string → DOM | a server-recorded error message (potentially long, multi-line, or containing HTML/unicode) is rendered into the run history table (RUN-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-05 | Tampering (XSS) | RunHistoryTable error/timestamp/status cells | high | mitigate | run.error and every cell value are rendered as plain JSX text children only — React auto-escapes them; no dangerouslySetInnerHTML is used. Proven by a test that renders an HTML-looking error and asserts no element is created from it (ScenarioTable T-1-03 convention). Blocks-on-high satisfied by the passing negative test. |
| T-3-06 | Information disclosure | inline FAILED error text | low | accept | RUN-05 deliberately surfaces the recorded error to the user; this is required behavior, not a leak. Internal unauthenticated demo app (auth out of scope per REQUIREMENTS.md); residual risk accepted. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/runs/RunStatusLabel.test.tsx src/components/runs/RunHistoryTable.test.tsx` exits 0.
- `cd frontend && npm run typecheck` passes.
- Backstop (long/multi-line FAILED error wraps without breaking column widths): no wired automated evidence at plan time — routes to human verification at end-of-phase.
</verification>

<success_criteria>
- Prior runs are visible with status and created/started/finished timing (RUN-04).
- A FAILED run shows its recorded error inline, with a defensive fallback when null (RUN-05).
- solver_status is never rendered; a COMPLETED run always reads as neutral success.
- No Badge; no raw-HTML rendering.
</success_criteria>

<artifacts_produced>
New files: frontend/src/components/runs/RunStatusLabel.tsx (+ test), frontend/src/components/runs/RunHistoryTable.tsx (+ test).
New exports: RunStatusLabel({ status }), RunHistoryTable({ runsQuery, scenarioId, onTriggerRun }).
</artifacts_produced>

<output>
Create `.planning/phases/03-run-execution-history/03-03-SUMMARY.md` when done.
</output>
