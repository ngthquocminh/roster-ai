---
phase: 03-run-execution-history
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/runs/RunInFlightPanel.tsx
  - frontend/src/components/runs/RunInFlightPanel.test.tsx
  - frontend/src/components/runs/TriggerRunButton.tsx
  - frontend/src/components/runs/TriggerRunButton.test.tsx
autonomous: true
requirements: [RUN-01, RUN-03]
must_haves:
  truths:
    - "The 'Run Scenario' button is enabled once the initial list fetch has resolved and no non-terminal run exists (UI-SPEC E1 idle)."
    - "The button stays disabled until GET /scenarios/{id}/runs first resolves, so there is no flash of an enabled button before the true in-progress state is known (UI-SPEC E1 loading-initial)."
    - "While the POST is in flight the button shows a spinner + 'Starting…' and is disabled (UI-SPEC E1 loading-submit)."
    - "A 404 trigger error renders 'This scenario no longer exists.' inline beneath the button; any other trigger error renders 'Couldn't start the run. Try again.' — branching on error.status via getErrorStatus, not message text (UI-SPEC E1 error-404 / error-other, T-1-02)."
    - "While the fetched list already contains a PENDING or RUNNING run the button is disabled with the caption 'A run is already in progress for this scenario.' (UI-SPEC E1 disabled-in-progress)."
    - "The in-progress disable makes it impossible to queue a second run the single-worker backend would only serialize — two near-simultaneous triggers cannot both start (edge RUN-01/adjacency)."
    - "The in-flight panel renders the 'Queued' heading + 'Waiting for the solver to pick this up — it's next in line.' body for a PENDING newest-active run (UI-SPEC E3 populated PENDING)."
    - "The in-flight panel renders the 'Solving…' heading + the honest 'This can take a few minutes…it can't be cancelled once started — let it finish.' body for a RUNNING newest-active run (UI-SPEC E3 populated RUNNING — RUN-03's primary satisfaction)."
    - "The in-flight panel renders nothing — not even empty chrome — when there is no non-terminal run (UI-SPEC E3 empty/hidden)."
    - "Only the newest non-terminal run drives the single panel; additional queued runs appear in the table only, never as stacked panels (UI-SPEC E3 zero-one-many)."
  prohibitions:
    - "A cancel affordance must not exist — no control that purports to abort an in-flight run."
    - "No progress bar or determinate progress affordance may imply imminent completion; the only motion is the indeterminate RUNNING spinner."
  artifacts:
    - frontend/src/components/runs/RunInFlightPanel.tsx
    - frontend/src/components/runs/RunInFlightPanel.test.tsx
    - frontend/src/components/runs/TriggerRunButton.tsx
    - frontend/src/components/runs/TriggerRunButton.test.tsx
  key_links:
    - "TriggerRunButton is presentational: RunHistory (03-05) passes onTrigger, isLoadingList, runInProgress, isPending, and error; the button owns no hook."
    - "Error branching uses lib/errors.ts getErrorStatus (existing) for the 404-vs-other split — the same convention as CreateScenarioDialog/ScenarioHeader."
    - "The panel receives the single newestActiveRun the parent derived from the one useRuns response; it never fetches."
---

<objective>
Build the write and status-communication surface for RUN-01 and RUN-03: TriggerRunButton (the "Run Scenario" CTA with all its idle/loading/disabled/error states) and RunInFlightPanel (the honest, non-lying wait panel). This is where a run gets started and where the "it takes minutes and cannot be cancelled" truth is told.

Purpose: Deliver both as purely presentational components driven entirely by props (the parent owns the hooks), so every copy state is trivially and exhaustively testable, and so RUN-03's honesty (no cancel, no imminent-completion affordance) is provable by assertion rather than hope.
Output: frontend/src/components/runs/TriggerRunButton.tsx (+ test) and frontend/src/components/runs/RunInFlightPanel.tsx (+ test).
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-run-execution-history/03-UI-SPEC.md
@frontend/src/components/scenarios/CreateScenarioDialog.tsx
@frontend/src/components/editor/ProviderDownBanner.tsx
@frontend/src/lib/errors.ts
@frontend/src/lib/runStatus.ts
@frontend/src/components/ui/button.tsx
@frontend/src/api/schema.d.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Presentational trigger CTA with all states (TriggerRunButton)</name>
  <files>frontend/src/components/runs/TriggerRunButton.tsx, frontend/src/components/runs/TriggerRunButton.test.tsx</files>
  <read_first>
    - frontend/src/components/scenarios/CreateScenarioDialog.tsx (the submit-button pattern: LoaderCircle spinner while pending, disabled logic, and the branch-on-error.status inline messaging — the exact idle/loading/error shape to mirror, but here as a standalone presentational button)
    - frontend/src/lib/errors.ts (getErrorStatus — the single accessor for the 404-vs-other branch)
    - frontend/src/components/ui/button.tsx (the stock Button default variant — no new variant, matches "New Scenario"/"Apply Constraint")
    - .planning/phases/03-run-execution-history/03-UI-SPEC.md (Copywriting Contract: "Run Scenario", "Starting…", the in-progress caption, and the two error strings; "Run Scenario button behavior" for the disabled logic)
  </read_first>
  <behavior>
    - Idle (isLoadingList false, runInProgress false, isPending false, error null): enabled Button labeled "Run Scenario".
    - Initial list loading (isLoadingList true): disabled, no caption.
    - Submitting (isPending true): disabled, spinner + "Starting…".
    - In progress (runInProgress true): disabled, caption "A run is already in progress for this scenario." below the button.
    - Error 404: destructive inline "This scenario no longer exists." below the button.
    - Error other (e.g. 500 or a network throw): destructive inline "Couldn't start the run. Try again." below the button.
    - Clicking an enabled button calls onTrigger exactly once.
  </behavior>
  <action>
    Create frontend/src/components/runs/TriggerRunButton.tsx exporting `TriggerRunButton({ onTrigger, isLoadingList, runInProgress, isPending, error }: { onTrigger: () => void; isLoadingList: boolean; runInProgress: boolean; isPending: boolean; error: unknown })`. Render a stock `Button` (default variant, type="button") whose disabled is `isLoadingList || runInProgress || isPending`, onClick calls onTrigger. When isPending, render a LoaderCircle size-4 animate-spin before the label and use the label "Starting…"; otherwise the label "Run Scenario". Below the button, when `runInProgress && !isPending`, render a muted-foreground Body caption "A run is already in progress for this scenario." When `error` is present, compute `getErrorStatus(error)` (from @/lib/errors) and render a destructive Body line: "This scenario no longer exists." when the status is 404, else "Couldn't start the run. Try again." Render the button + its caption/error stacked in a small flex-col so the caption sits directly beneath. Do NOT render any cancel/abort control and do NOT render any determinate progress element (no progress bar, no percentage, no ETA). Create frontend/src/components/runs/TriggerRunButton.test.tsx covering: idle enabled + label; isLoadingList disabled; isPending disabled + "Starting…" + spinner; runInProgress disabled + caption; error 404 message; error other message; onTrigger called on click; and a negative assertion that no element with an accessible name matching /cancel/i and no progressbar role exist in any state.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/runs/TriggerRunButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The button is enabled only when isLoadingList, runInProgress, and isPending are all false (tests cover each disabling input).
    - isPending renders the "Starting…" label with a spinning icon; the idle label is "Run Scenario" (tests assert).
    - runInProgress (and not pending) renders the caption "A run is already in progress for this scenario." (test asserts).
    - error.status 404 → "This scenario no longer exists."; any other error → "Couldn't start the run. Try again." (tests assert both, branching via status not message text).
    - clicking the enabled button calls onTrigger once (test asserts).
    - In every rendered state there is no control named /cancel/i and no element with role progressbar (test asserts the negatives).
    - `cd frontend && npx vitest run src/components/runs/TriggerRunButton.test.tsx` exits 0.
  </acceptance_criteria>
  <done>TriggerRunButton is a props-only CTA covering idle/loading/submitting/in-progress/404/other, with no cancel control and no imminent-completion affordance, all asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest in-flight wait panel (RunInFlightPanel)</name>
  <files>frontend/src/components/runs/RunInFlightPanel.tsx, frontend/src/components/runs/RunInFlightPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/editor/ProviderDownBanner.tsx (the persistent inline Alert default-variant pattern with AlertTitle/AlertDescription and a data-testid — the exact structural analog for the in-flight panel)
    - frontend/src/lib/runStatus.ts (RunOut type; isTerminalStatus for the defensive guard)
    - .planning/phases/03-run-execution-history/03-UI-SPEC.md (Copywriting Contract: In-flight panel PENDING and RUNNING copy — verbatim; Application Structure item 2: renders only while a non-terminal run exists, only the newest one)
  </read_first>
  <behavior>
    - run null → renders nothing (no DOM output).
    - run with status "PENDING" → an Alert with AlertTitle "Queued" and AlertDescription "Waiting for the solver to pick this up — it's next in line."
    - run with status "RUNNING" → an Alert with AlertTitle "Solving…" and AlertDescription "This can take a few minutes: round 1 finds a feasible schedule quickly, then round 2 spends the rest of the time tuning cost. It can't be cancelled once started — let it finish."
    - run with a terminal status (defensive; parent should never pass one) → renders nothing.
    - No cancel control and no determinate progress element in any state.
  </behavior>
  <action>
    Create frontend/src/components/runs/RunInFlightPanel.tsx exporting `RunInFlightPanel({ run }: { run: RunOut | null })` where RunOut is `components["schemas"]["RunOut"]` from @/api/schema. If run is null OR isTerminalStatus(run.status) is true, return null (render nothing — not even empty chrome). Otherwise render an `Alert variant="default"` (with a stable data-testid such as "run-in-flight-panel", mirroring ProviderDownBanner) containing an AlertTitle and AlertDescription whose copy is selected by run.status: for "PENDING" the "Queued" title and the "next in line" body; for "RUNNING" the "Solving…" title and the full honest multi-minute / cannot-be-cancelled body — both strings taken verbatim from the UI-SPEC Copywriting Contract. Use AlertDescription's own typography (do not add a new role). Do NOT render any cancel/abort control and do NOT render any progress bar, percentage, ETA, or determinate progress affordance — the panel is text only (RUN-03 honesty). Create frontend/src/components/runs/RunInFlightPanel.test.tsx covering: null run renders nothing; PENDING renders the "Queued" title + next-in-line body; RUNNING renders the "Solving…" title + the exact honest body including the "can't be cancelled" clause; a terminal run renders nothing; and negative assertions that no /cancel/i control and no progressbar role appear.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/runs/RunInFlightPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - run null (and run with a terminal status) renders no DOM (test asserts container is empty).
    - PENDING renders title "Queued" and body "Waiting for the solver to pick this up — it's next in line." (test asserts verbatim).
    - RUNNING renders title "Solving…" and a body containing "This can take a few minutes" and "can't be cancelled" (test asserts the honest wait/no-cancel copy).
    - No control named /cancel/i and no element with role progressbar appears in PENDING or RUNNING state (test asserts the negatives — the panel implies no imminent completion and offers no abort).
    - `cd frontend && npx vitest run src/components/runs/RunInFlightPanel.test.tsx` exits 0.
  </acceptance_criteria>
  <done>RunInFlightPanel shows the correct honest copy for PENDING/RUNNING, renders nothing when there is no active run, and offers neither a cancel control nor a completion-implying progress affordance.</done>
</task>

</tasks>

<flagged_assumptions>
- Spec-less edge probe RUN-03 (category: unclassified — "review manually") is surfaced here rather than auto-resolved. Planner assumption: RUN-03's substance is fully carried by (a) the two in-flight-panel copy truths, (b) the disabled-while-in-progress button truth + the RUN-01/adjacency truth, and (c) the two prohibitions (no cancel affordance, no imminent-completion progress). The probe row yields no additional independent testable predicate beyond these; recorded so the row is not silently dropped. No-silent-drop accounting for this plan: this flagged assumption together with the E1/E3 covered truths and the two prohibitions above accounts for the RUN-03 probe row and the RUN-01/adjacency probe row owned by this plan.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend trigger error → button caption | a thrown trigger error (carrying a backend-provided status/detail) influences which inline message renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-07 | Information disclosure | TriggerRunButton error line | low | mitigate | The button renders one of two fixed copy strings selected by getErrorStatus(error); the backend error body is never rendered (same discipline as ErrorBanner/ProviderDownBanner), so backend internals cannot leak through the CTA. |
| T-3-08 | Repudiation / honesty | RunInFlightPanel + button affordances | medium | mitigate | RUN-03 requires an honest wait: negative tests assert no cancel control and no progressbar/determinate-progress element exist in any state, so the UI cannot imply an abort path that does not exist or a completion that is not imminent. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/runs/TriggerRunButton.test.tsx src/components/runs/RunInFlightPanel.test.tsx` exits 0.
- `cd frontend && npm run typecheck` passes.
</verification>

<success_criteria>
- A run can be triggered from an honest CTA that disables while a run is in progress and reports 404 vs other failures (RUN-01).
- The in-flight panel tells the multi-minute / cannot-be-cancelled truth and offers no cancel or progress-to-completion affordance (RUN-03).
</success_criteria>

<artifacts_produced>
New files: frontend/src/components/runs/TriggerRunButton.tsx (+ test), frontend/src/components/runs/RunInFlightPanel.tsx (+ test).
New exports: TriggerRunButton({ onTrigger, isLoadingList, runInProgress, isPending, error }), RunInFlightPanel({ run }).
</artifacts_produced>

<output>
Create `.planning/phases/03-run-execution-history/03-04-SUMMARY.md` when done.
</output>
