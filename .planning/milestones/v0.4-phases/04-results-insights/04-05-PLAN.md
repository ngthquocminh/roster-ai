---
phase: 04-results-insights
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/components/results/DemandVsServedChart.tsx
  - frontend/src/components/results/DemandVsServedChart.test.tsx
  - frontend/src/components/results/ScheduleTable.tsx
  - frontend/src/components/results/ScheduleTable.test.tsx
autonomous: true
requirements: [RES-02, RES-03]
must_haves:
  truths:
    - "DemandVsServedChart renders one grouped-bar pair per function from coverage_by_function — required as an outline-only bar (fill='none', stroke var(--muted-foreground)) and served as a solid indigo (#4F46E5) fill — via the shadcn ChartContainer/ChartTooltip primitives, with an explicit min-h-[280px] so Recharts' ResponsiveContainer can measure a non-zero height on first paint (RES-02, D-02, D-03; RESEARCH.md Pattern 2 pitfall guard)."
    - "The chart is by-function only and never shows a number the by-day breakdown table shows — each coverage view has exactly one home (D-02/D-05 no-duplication)."
    - "A null required_h/served_h for a function renders a zero-height bar AND a tooltip line reading 'Not computed' so a zero-height bar is not misread as zero hours (RESEARCH.md Pitfall 5; UI-SPEC 'Chart tooltip (a value is null)')."
    - "ScheduleTable renders the schedule as a scrollable table (max-h-[420px] overflow-y-auto, reusing RunHistoryTable's exact container) in server order with no client sort or filter (RES-03, D-08, D-09); columns are Member | Task | Function | Shift Window with the window via formatShiftWindow (D-10)."
    - "A COMPLETED run with zero scheduled shifts renders 'No shifts were scheduled for this run.' — a legitimate solver outcome, not an error (E6 empty)."
    - statement: "The chart's behavior when coverage_by_function is empty on a COMPLETED run is an unverified reachability case (not confirmed against the engine whether SolveResult.metrics always populates coverage_by_function when scheduling occurs)."
      verification: backstop
  prohibitions:
    - "ScheduleTable must not sort, re-order, or filter the schedule rows client-side — it renders server order only, matching RunHistoryTable's explicit precedent (D-09)."
    - "Schedule cell values (member_name / function / task_id) and chart labels must render as plain JSX text children only, never via a raw-HTML injection sink — React's default escaping is the mitigation."
  artifacts:
    - frontend/src/components/results/DemandVsServedChart.tsx
    - frontend/src/components/results/ScheduleTable.tsx
  key_links:
    - "DemandVsServedChart imports ChartContainer/ChartTooltip/ChartTooltipContent/ChartConfig from the chart.tsx installed in plan 04-01 (which pulls recharts); the served color #4F46E5 is a literal hex matching ScenarioLayout.tsx's existing active-tab usage (no CSS variable exists for it)."
    - "ScheduleTable uses formatShiftWindow from plan 04-02 for the Shift Window column; its 1-indexed 'Day N' must agree with CoverageByDayTable (plan 04-04) for the same calendar day (Pitfall 2)."
    - "Both components are presentational — fed RunResult.metrics.coverage_by_function and RunResult.schedule as props by ResultsView (plan 04-07); neither fetches."
---

<objective>
Build the two data-visual components: the demand-vs-served grouped bar chart (the first chart in the project — RES-02, D-02/D-03) and the scrollable schedule table (RES-03, D-08/D-09/D-10). Both are presentational, fed already-fetched RunResult fields.

Purpose: The chart makes coverage-by-function legible at a glance (required vs served per function), while the schedule table makes the solved assignments readable with human day/time labels. The chart owns by-function; the by-day table (plan 04-04) owns by-day — no number appears in both.
Output: DemandVsServedChart.tsx, ScheduleTable.tsx (+ tests).
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-insights/04-RESEARCH.md
@.planning/phases/04-results-insights/04-PATTERNS.md
@.planning/phases/04-results-insights/04-UI-SPEC.md
@frontend/src/components/runs/RunHistoryTable.tsx
@frontend/src/routes/ScenarioLayout.tsx
@frontend/src/lib/formatShiftWindow.ts
@frontend/src/api/results.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DemandVsServedChart (D-02, D-03, RES-02)</name>
  <files>frontend/src/components/results/DemandVsServedChart.tsx, frontend/src/components/results/DemandVsServedChart.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/chart.tsx (the shadcn chart primitives installed in 04-01 — ChartContainer, ChartTooltip, ChartTooltipContent, ChartConfig; how ChartConfig's color keys map to var(--color-<key>))
    - .planning/phases/04-results-insights/04-RESEARCH.md (Pattern 2 — the exact BarChart composition; Pitfall 4 — recharts v3 ResponsiveContainer breaking change and the min-h requirement; Open Question 2 — use var(--muted-foreground) for the required outline)
    - .planning/phases/04-results-insights/04-PATTERNS.md (DemandVsServedChart pattern block)
    - .planning/phases/04-results-insights/04-UI-SPEC.md (Color: served #4F46E5 fill, required var(--muted-foreground) outline; Copywriting Contract: X axis function name, Y axis "Hours", tooltip lines; Spacing: min-h-[280px])
    - frontend/src/routes/ScenarioLayout.tsx (confirm #4F46E5 is a literal hex used for the active-tab indicator, not a CSS variable)
  </read_first>
  <behavior>
    - A pure `toChartData(coverage_by_function)` helper maps Record<string,{required_h,served_h,pct}> to an array of { function, required_h, served_h } — one entry per function, preserving function keys.
    - The chart renders required as an outline-only bar and served as a solid indigo fill, one grouped pair per function.
    - The tooltip for a function whose required_h or served_h is null shows "Not computed" for that line rather than a numeric line.
  </behavior>
  <action>
    Create frontend/src/components/results/DemandVsServedChart.tsx. Export a pure `toChartData` helper (Object.entries → array of { function, required_h, served_h }) so the data transform is unit-testable independent of Recharts' DOM. Export `DemandVsServedChart` taking coverage_by_function as a prop: build a ChartConfig with required_h { label "Required", color "var(--muted-foreground)" } and served_h { label "Served", color "#4F46E5" }, and render a Recharts BarChart inside ChartContainer with className including an explicit `min-h-[280px] w-full` (Pitfall 4 guard), a CartesianGrid, an XAxis dataKey "function", a ChartTooltip with ChartTooltipContent, a required bar (fill "none", stroke var(--color-required_h)) and a served bar (fill var(--color-served_h)). Label the Y axis "Hours". Guard per-value nulls so the tooltip reads "Not computed" for a null (do not coerce null to 0 silently in a way that reads as real zero hours). Add DemandVsServedChart.test.tsx asserting toChartData maps 2 functions to 2 entries with correct required/served values, preserves keys, and handles a null value; plus a render smoke test that the component mounts without throwing (do not assert SVG pixel dimensions — jsdom cannot measure ResponsiveContainer).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/DemandVsServedChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/results/DemandVsServedChart.tsx` exports `DemandVsServedChart` and a pure `toChartData` helper.
    - `src/components/results/DemandVsServedChart.tsx` contains `min-h-[280px]` and the literal `#4F46E5`.
    - `npx vitest run src/components/results/DemandVsServedChart.test.tsx` exits 0, covering the data mapping (incl. a null value) and a mount smoke test.
  </acceptance_criteria>
  <done>The demand-vs-served grouped bar chart renders per-function required/served with brand indigo + muted outline and a non-zero min height, null-safe in the tooltip — RES-02 is real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScheduleTable (D-08, D-09, D-10, RES-03)</name>
  <files>frontend/src/components/results/ScheduleTable.tsx, frontend/src/components/results/ScheduleTable.test.tsx</files>
  <read_first>
    - frontend/src/components/runs/RunHistoryTable.tsx (copy the scrollable container verbatim — `max-h-[420px] overflow-y-auto rounded-md border border-border` + `Table className="table-fixed"`; drop the row-click navigation and the loading/error/empty state machine tied to a query; keep the plain-JSX-text rendering discipline from its header comment)
    - frontend/src/lib/formatShiftWindow.ts (the Shift Window column formatter from 04-02)
    - .planning/phases/04-results-insights/04-PATTERNS.md (ScheduleTable pattern block — server-order comment convention)
    - .planning/phases/04-results-insights/04-UI-SPEC.md (Copywriting Contract: columns Member/Task/Function/Shift Window; empty-state copy "No shifts were scheduled for this run.")
    - frontend/src/api/results.ts (RunResult.schedule row shape: member_name, task_id, function, start_h, end_h, ...)
  </read_first>
  <behavior>
    - ScheduleTable with N schedule rows renders N table rows in the given (server) order — no re-sort.
    - The Shift Window cell renders formatShiftWindow(row.start_h, row.end_h).
    - An empty schedule renders "No shifts were scheduled for this run."
  </behavior>
  <action>
    Create frontend/src/components/results/ScheduleTable.tsx taking the RunResult.schedule array as a prop. Reuse RunHistoryTable's scrollable container (`max-h-[420px] overflow-y-auto rounded-md border border-border`) wrapping a shadcn `Table className="table-fixed"` with header cells Member | Task | Function | Shift Window. Map each schedule row (in given order — no sort/filter) to a TableRow: Member = member_name, Task = task_id, Function = function, Shift Window = formatShiftWindow(start_h, end_h) — all as plain JSX text children. When the array is empty, render the centered empty-state copy "No shifts were scheduled for this run." instead of the table. Add a header comment stating server order only (no client re-sort), matching RunHistoryTable's precedent. Add ScheduleTable.test.tsx asserting: rows render in input order (assert the first-row identity matches the first input element, not a sorted one); the Shift Window cell shows a formatShiftWindow string; an empty array renders the empty-state copy.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/ScheduleTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/results/ScheduleTable.tsx` exports `ScheduleTable` and reuses the `max-h-[420px] overflow-y-auto` container.
    - `src/components/results/ScheduleTable.tsx` calls `formatShiftWindow` and contains no `.sort(` / `.filter(` over the schedule rows.
    - `grep -c dangerouslySetInnerHTML src/components/results/ScheduleTable.tsx` returns 0.
    - `npx vitest run src/components/results/ScheduleTable.test.tsx` exits 0, covering server-order render, a formatted shift window, and the empty state.
  </acceptance_criteria>
  <done>The schedule renders as a scrollable, server-ordered table with human day/time windows and an honest empty state — RES-03 is real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture/solver data → browser DOM | schedule cell strings (member_name/function/task_id) and function labels render into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering / Elevation of Privilege | ScheduleTable / chart rendering data-sourced strings | medium | mitigate | All cell/label strings render as plain JSX text children only (no `dangerouslySetInnerHTML`); ScheduleTable acceptance greps the sink to 0. |
| T-04-07 | Tampering (misrepresentation) | null required_h/served_h shown as zero hours | low | mitigate | A null renders a zero-height bar plus a "Not computed" tooltip line so it is not misread as zero hours. |
</threat_model>

<verification>
- `npx vitest run src/components/results/DemandVsServedChart.test.tsx src/components/results/ScheduleTable.test.tsx` all green.
- Chart carries min-h-[280px] and #4F46E5; ScheduleTable is server-order-only and has no raw-HTML sink.
</verification>

<success_criteria>
The chart and schedule table render a completed run's coverage-by-function and assignments legibly — brand indigo grouped bars with a measurable height, and a scrollable server-ordered schedule with human shift windows — RES-02 and RES-03 are real.
</success_criteria>

## Artifacts this phase produces

- Files: `frontend/src/components/results/DemandVsServedChart.tsx` (+ test), `frontend/src/components/results/ScheduleTable.tsx` (+ test)
- Symbols: `DemandVsServedChart`, `toChartData()` (chart); `ScheduleTable` (React components)

<output>
Create `.planning/phases/04-results-insights/04-05-SUMMARY.md` when done.
</output>
