---
phase: 04-results-insights
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/results/DemandVsServedChart.tsx
  - frontend/src/components/results/DemandVsServedChart.test.tsx
autonomous: true
requirements: [RES-02]
gap_closure: true
gap_ids: [G-04-4]
must_haves:
  truths:
    - "When coverage_by_function is {} on a COMPLETED run, DemandVsServedChart renders a visible empty-state message instead of a blank BarChart box (G-04-4) — a user can tell 'no demand for this run' from broken/loading."
    - "A populated coverage_by_function still renders the grouped required-vs-served bar chart unchanged — no regression to RES-02."
    - "A non-empty coverage_by_function whose values are null still renders the chart (the empty-state fires only on a zero-length map, never on null hours)."
  prohibitions:
    - "The empty-state guard must key off the mapped-data length being zero (the same condition ScheduleTable.tsx uses), NOT a null-value check — a function with null required_h/served_h is still a real function and must render its bar (preserving the existing 'values are null' smoke test)."
  artifacts:
    - frontend/src/components/results/DemandVsServedChart.tsx
    - frontend/src/components/results/DemandVsServedChart.test.tsx
  key_links:
    - "The empty-state early-return sits before the ChartContainer/BarChart render on the same zero-length-data condition ScheduleTable.tsx already uses, so an empty run never reaches Recharts (whose ResponsiveContainer draws nothing measurable in jsdom anyway)."
---

<objective>
Close UAT gap G-04-4: DemandVsServedChart currently renders a completely blank box (empty axes, no bars, no labels, no text) when `coverage_by_function` is `{}`, leaving a user unable to distinguish "no demand for this run" from a broken or still-loading chart. Add an explicit empty-state branch — mirroring the sibling ScheduleTable.tsx's established zero-state pattern — plus a test asserting it renders.

Purpose: RES-02's chart must render an honest empty/absent state for a zero-demand COMPLETED run (verification test 4), matching the empty-state honesty ScheduleTable and CoverageByDayTable already provide in this same phase.
Output: an empty-state branch in DemandVsServedChart.tsx and a regression test in DemandVsServedChart.test.tsx.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-insights/04-UAT.md
@frontend/src/components/results/DemandVsServedChart.tsx
@frontend/src/components/results/DemandVsServedChart.test.tsx
@frontend/src/components/results/ScheduleTable.tsx
@frontend/src/components/results/ScheduleTable.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add honest empty-state to DemandVsServedChart when coverage_by_function is empty</name>
  <files>frontend/src/components/results/DemandVsServedChart.tsx, frontend/src/components/results/DemandVsServedChart.test.tsx</files>
  <read_first>
    - frontend/src/components/results/ScheduleTable.tsx (the empty-state pattern to mirror verbatim: a module-level copy constant, then an early return of a centered muted paragraph when the rendered collection is empty — reuse the same wrapper/typography classes)
    - frontend/src/components/results/ScheduleTable.test.tsx (the "empty" describe block asserting the copy renders and the table is absent — mirror its shape for the chart)
    - frontend/src/components/results/DemandVsServedChart.tsx (the component under change — the guard goes right after `toChartData(...)`, before the `chartData` map and the ChartContainer render)
    - frontend/src/components/results/DemandVsServedChart.test.tsx (existing suite: toChartData unit tests + populated/null render smoke tests the new empty test sits alongside without disturbing)
  </read_first>
  <behavior>
    - Given coverage_by_function is {} , the component renders the empty-state copy text and does NOT render a chart container / BarChart.
    - Given a populated coverage_by_function (e.g. Pick/Receiving), the chart renders as before (existing smoke test stays green).
    - Given a non-empty coverage_by_function whose required_h/served_h are null, the chart still renders (the empty-state does not fire — existing "values are null" smoke test stays green).
  </behavior>
  <action>
    In DemandVsServedChart.tsx, add a module-level string constant named EMPTY_COVERAGE_COPY set to the copy "No coverage data for this run." (tone-matched to ScheduleTable.tsx's zero-state copy). Immediately after `const data = toChartData(coverage_by_function);` in the DemandVsServedChart component, add an early return that fires when `data.length === 0`: return the same centered zero-state block ScheduleTable.tsx uses — a div carrying the classes `flex flex-col items-center gap-2 py-16 text-center` wrapping a `p` with `text-sm leading-[1.5] text-muted-foreground` whose text child is EMPTY_COVERAGE_COPY. Gate strictly on the mapped `data.length` (zero-length map), never on null field values, so a function with null hours still reaches the existing chart render. Leave the existing `chartData` map, ChartContainer, and BarChart render path below untouched. In DemandVsServedChart.test.tsx, add a new `describe("DemandVsServedChart: empty", ...)` block mirroring ScheduleTable.test.tsx's empty describe: render the component with `coverage_by_function={{}}`, assert the empty-state copy is in the document via `screen.getByText`, and assert no chart is drawn (query for the absence of any `role="img"`/svg, or simply that the copy is present and the populated smoke tests remain the only chart-rendering cases). Import `screen` from @testing-library/react alongside the existing `render`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/DemandVsServedChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DemandVsServedChart returns the empty-state block (not a ChartContainer/BarChart) when `coverage_by_function` is `{}`.
    - `npx vitest run src/components/results/DemandVsServedChart.test.tsx` exits 0, including the new empty-state assertion AND the pre-existing populated + "values are null" smoke tests (the null-values case must still render the chart, proving the guard keys off length, not null).
    - The empty-state paragraph reuses ScheduleTable.tsx's zero-state wrapper/typography classes so the two components' empty states read as one system.
  </acceptance_criteria>
  <done>G-04-4 is closed: an empty coverage_by_function renders an honest, readable message instead of a blank chart box, with a regression test locking the behavior and no regression to the populated or null-valued chart paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run result → chart render | Run-derived `coverage_by_function` reaches the DOM; the new empty-state path renders no run-derived data (a compile-time constant only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering (injection) | DemandVsServedChart empty-state render | low | accept | The empty-state renders a hardcoded string constant as a plain JSX text child — no interpolated run data, no `dangerouslySetInnerHTML` sink; React's default text-node escaping already covers every other path in the file, unchanged by this plan. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/results/DemandVsServedChart.test.tsx` is green, covering: empty `{}` → empty-state copy renders and no chart; populated → chart renders; null field values → chart still renders.
- Manual/UAT re-check (test 4): a real zero-demand run (`coverage_by_function: {}`) now shows the readable empty-state message rather than a blank box.
</verification>

<success_criteria>
DemandVsServedChart renders an honest empty/absent state when `coverage_by_function` is empty — distinguishable from broken/loading — while the populated and null-valued chart paths remain unchanged and tested.
</success_criteria>

<output>
Create `.planning/phases/04-results-insights/04-08-SUMMARY.md` when done.
</output>
