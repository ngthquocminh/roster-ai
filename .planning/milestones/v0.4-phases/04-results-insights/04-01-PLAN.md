---
phase: 04-results-insights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/chart.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/components.json
autonomous: false
requirements: [RES-01, RES-02]
must_haves:
  truths:
    - "recharts@3.9.x is present in frontend/package.json as the ONLY net-new runtime dependency added by this phase (D-01) — verified by a package.json diff after install."
    - "frontend/src/components/ui/card.tsx, chart.tsx, and tooltip.tsx exist as shadcn source-copied components (not a bare npm install), matching the Phase 2 Textarea source-copy precedent."
    - "The recharts [SUS] legitimacy gate was resolved by an explicit human approval BEFORE the install command ran — the approval is recorded, not auto-advanced (RES-02 depends on recharts, which is [SUS]-flagged)."
  prohibitions:
    - "recharts must not be added via a bare `npm install recharts` — it must arrive through `npx shadcn@latest add card chart tooltip` so it lands as a source-copied registry component with a package.json dependency entry, per RESEARCH.md Package Legitimacy Audit."
    - "The install must not be run before the blocking-human legitimacy checkpoint is approved — a [SUS] package install is never auto-approvable (workflow.auto_advance is ignored for this gate)."
  artifacts:
    - frontend/src/components/ui/card.tsx
    - frontend/src/components/ui/chart.tsx
    - frontend/src/components/ui/tooltip.tsx
  key_links:
    - "recharts is declared in frontend/package.json and imported by the generated chart.tsx — DemandVsServedChart (plan 04-05) consumes ChartContainer/ChartTooltip from chart.tsx, which itself imports from recharts."
    - "card.tsx (Card/CardHeader/CardContent) is consumed by CoverageSummary (plan 04-04); tooltip.tsx (Tooltip/TooltipContent/TooltipTrigger) is consumed by CoverageSummary's D-07 null-metric tooltip."
---

<objective>
Resolve the recharts [SUS] package-legitimacy gate with an explicit human approval, then install the three new shadcn registry components this phase needs — `card`, `chart` (which pulls `recharts@3.9.x`), and `tooltip` — via the project's established source-copy path.

Purpose: recharts is flagged `[SUS]` by the legitimacy audit (a "too-new" false positive against the patch-publish date of a 10-year-old, 49M-weekly-download library — see RESEARCH.md Package Legitimacy Audit and UI-SPEC Registry Safety). Per protocol a [SUS] install must clear a blocking-human checkpoint before it runs; this plan makes that gate the wave-1 foundation everything downstream (chart, cards, tooltips) builds on.
Output: card.tsx / chart.tsx / tooltip.tsx under frontend/src/components/ui/, recharts declared in package.json (only new dep), recorded human approval of the legitimacy gate.
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-insights/04-RESEARCH.md
@.planning/phases/04-results-insights/04-UI-SPEC.md
@frontend/components.json
@frontend/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Blocking-human legitimacy gate for recharts [SUS]</name>
  <files>.planning/phases/04-results-insights/04-01-SUMMARY.md</files>
  <action>
    Present the `<what-built>` findings and the `<how-to-verify>` steps below to the human, then STOP and
    wait for the `<resume-signal>`. Do not run any install command and do not touch package.json until the
    human responds. Record their verbatim decision in 04-01-SUMMARY.md. If the npmjs.com page's package
    name or source repo does not match the expected values, stop the phase and report it — a mismatch is
    not a thing to work around. This checkpoint is NOT auto-approvable: a [SUS] supply-chain gate is
    human-only and `workflow.auto_advance` is ignored here regardless.
  </action>
  <what-built>Nothing installed yet. This gate precedes any package install. recharts@3.9.2 is flagged [SUS] by `gsd-tools query package-legitimacy check` on a "too-new" signal that fired against the package's latest patch publish date (2026-07-04), NOT its real age. Independent facts already gathered (RESEARCH.md Package Legitimacy Audit + UI-SPEC Registry Safety): `npm view recharts time.created` = 2015-08-07 (10+ years old), ~49.1M weekly downloads, repository github.com/recharts/recharts, `postinstall` script is null. The UI-spec pass additionally ran `npx shadcn view chart` and found no `fetch`/`XMLHttpRequest`/`sendBeacon`, no `process.env`, no `eval`/`Function`, no external dynamic import, no obfuscated identifiers — the one `dangerouslySetInnerHTML` in chart.tsx injects only a `<style>` block of `--color-<key>` CSS custom properties derived from a developer-authored ChartConfig object (shadcn's own published boilerplate).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/recharts and confirm: publisher/maintainer history, ~49M weekly downloads, first-published 2015, source repo github.com/recharts/recharts.
    2. Confirm the version to be installed resolves to 3.9.x: run `cd frontend && npm view recharts version` (expect `3.9.x`).
    3. Confirm this matches the established Phase 1/Phase 2 shadcn source-copy precedent (Phase 2 added Textarea the same way) — this is not a novel supply-chain surface, it is the first charting registry component.
    4. Decide: approve the recharts install, or reject and stop the phase.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the `npx shadcn@latest add card chart tooltip` install, or describe the concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install card + chart + tooltip via shadcn, verify single new dependency</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/components/ui/card.tsx, frontend/src/components/ui/chart.tsx, frontend/src/components/ui/tooltip.tsx</files>
  <read_first>
    - frontend/components.json (shadcn config: style radix-nova, cssVariables true, registry — confirms `npx shadcn add` targets src/components/ui/)
    - frontend/package.json (baseline dependency list — the before-image for the diff check)
    - .planning/phases/04-results-insights/04-RESEARCH.md ("Installation path" + Package Legitimacy Audit — the exact command and the single-new-dep expectation)
    - .planning/phases/04-results-insights/04-UI-SPEC.md ("New shadcn components this phase" table — card/chart/tooltip rationale; tooltip is the addition beyond RESEARCH's `card chart` list, expected to add zero new package.json deps because the radix-ui umbrella already covers the Tooltip primitive)
  </read_first>
  <action>
    Only after Task 1 is approved. From frontend/, capture the current package.json dependency set, then run `npx shadcn@latest add card chart tooltip` (the official registry, source-copy — do NOT run a bare `npm install recharts`). This writes card.tsx, chart.tsx, and tooltip.tsx into src/components/ui/ and adds `recharts` to package.json dependencies. After it completes, diff package.json against the captured baseline and confirm `recharts` (version 3.9.x) is the ONLY net-new dependency — tooltip is expected to add zero new deps because the already-installed radix-ui umbrella (^1.6.2) covers the Tooltip primitive, exactly as it silently covered Alert/Dialog/Select/Tabs in prior phases. If any dependency OTHER than recharts appears (a surprise transitive UI dep), stop and report it as its own legitimacy item before proceeding. Confirm `npm view recharts version` returns 3.9.x. Do not modify any component or route yet — this plan only lands the primitives.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/ui/card.tsx && test -f src/components/ui/chart.tsx && test -f src/components/ui/tooltip.tsx && node -e "const p=require('./package.json');if(!/^[\^~]?3\.9\./.test(p.dependencies.recharts||''))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/card.tsx`, `chart.tsx`, and `tooltip.tsx` all exist after the command.
    - `frontend/package.json` `dependencies.recharts` matches `3.9.x`.
    - A package.json dependency diff shows `recharts` as the only added dependency (no other new dep landed); if any other appeared it is reported, not silently accepted.
    - `npm run build` (or `tsc --noEmit`) still succeeds — the copied ui components compile against the existing toolchain.
  </acceptance_criteria>
  <done>recharts@3.9.x is installed as the sole new dependency and card/chart/tooltip source components are present under src/components/ui/, ready for plans 04-04 and 04-05 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | A new package (recharts) and its transitive deps enter the build via the shadcn CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | `npx shadcn add chart` → recharts npm install | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) precedes the install; source-copy via shadcn (not bare npm), package.json diff confirms recharts is the only new dep, `postinstall` confirmed null in RESEARCH audit. |
| T-04-SC2 | Tampering | surprise transitive UI dependency | medium | mitigate | Task 2's package.json diff halts and reports any non-recharts new dependency as its own legitimacy item before merge. |
</threat_model>

<verification>
- card.tsx, chart.tsx, tooltip.tsx exist under frontend/src/components/ui/.
- recharts 3.9.x is in package.json; no other new dependency was added.
- The legitimacy checkpoint was approved by a human before the install ran (recorded in the SUMMARY).
</verification>

<success_criteria>
The three shadcn primitives are installed via the source-copy path with recharts as the only new dependency, gated by a recorded human approval — the wave-1 foundation for the coverage cards (04-04) and the demand-vs-served chart (04-05).
</success_criteria>

## Artifacts this phase produces

- Files: `frontend/src/components/ui/card.tsx`, `frontend/src/components/ui/chart.tsx`, `frontend/src/components/ui/tooltip.tsx`
- Symbols (from shadcn registry): `Card`, `CardHeader`, `CardContent`, `CardTitle`, `CardDescription`, `CardFooter` (card.tsx); `ChartContainer`, `ChartTooltip`, `ChartTooltipContent`, `ChartLegend`, `ChartLegendContent`, `ChartConfig`, `ChartStyle` (chart.tsx); `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider` (tooltip.tsx)
- Dependency: `recharts@3.9.x` added to `frontend/package.json`

<output>
Create `.planning/phases/04-results-insights/04-01-SUMMARY.md` when done — record the legitimacy approval, the installed recharts version, and the package.json diff result.
</output>
