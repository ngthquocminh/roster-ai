---
phase: 04-results-insights
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/components/results/WarningsBanner.tsx
  - frontend/src/components/results/WarningsBanner.test.tsx
  - frontend/src/components/results/CoverageSummary.tsx
  - frontend/src/components/results/CoverageSummary.test.tsx
  - frontend/src/components/results/CoverageByDayTable.tsx
  - frontend/src/components/results/CoverageByDayTable.test.tsx
autonomous: true
requirements: [RES-01, RES-06]
must_haves:
  truths:
    - "The coverage stat row renders two Cards — 'Total Cost' (Intl.NumberFormat USD) and 'Total Unmet Hours' ('{value} h', one decimal) — as the page's Display-type focal point; a null metric renders literal 'Not computed' with a Tooltip explaining the solver hit its time limit, never a currency zero, a plain 0, or a bare em dash (RES-01, D-04, D-07)."
    - "Each stat card guards its own value independently: one card can read 'Not computed' while its sibling shows a real number (RESEARCH.md Pitfall 5 partial case; E3 partial)."
    - "The WarningsBanner renders directly above the stat row ONLY when warnings.length > 0 — one paragraph per string, each string verbatim as plain JSX text — and renders nothing at all (not even empty chrome) when the array is empty (RES-06, D-06); the one call site guards with `warnings ?? []` against pre-warnings-era cached rows (RESEARCH.md Open Question 1)."
    - "The WarningsBanner uses Alert variant='default' (neutral surface, not destructive) with a TriangleAlert icon — a caveat about a COMPLETED run, not an error; no green/amber severity color is introduced (UI-SPEC Color)."
    - "CoverageByDayTable renders one row per coverage_by_day key as 1-indexed 'Day N' (Number(dayKey)+1) with the value as '{value}%' or literal 'Not computed' when null — by-day only, never duplicating the chart's by-function numbers (RES-01, D-05)."
    - statement: "A single long warning string wraps inside the WarningsBanner without breaking the page layout (visual, unverifiable at spec time — same class as Phase 3's FAILED-error long-text backstop)."
      verification: backstop
    - statement: "The CoverageByDayTable's rendering when coverage_by_day is empty on a COMPLETED run is an unverified reachability case (not confirmed against the engine whether SolveResult.metrics always populates coverage_by_day when scheduling occurs)."
      verification: backstop
  prohibitions:
    - "A null total_cost / total_unmet_hours / per-day coverage value must not render as a real zero, a currency zero, or a bare em dash — a null solver metric is the honest fact 'the solver did not compute this' and must read 'Not computed' (D-07; this project's never-hide-solver-limitations principle)."
    - "No string in warnings[] may be silently dropped, truncated, collapsed, or deduplicated — every warning renders verbatim; warnings and coverage strings must render as plain JSX text children only, never via a raw-HTML injection sink."
  artifacts:
    - frontend/src/components/results/WarningsBanner.tsx
    - frontend/src/components/results/CoverageSummary.tsx
    - frontend/src/components/results/CoverageByDayTable.tsx
  key_links:
    - "In ResultsView (plan 04-07) the render order is WarningsBanner → CoverageSummary → CoverageByDayTable, so the coverage-honesty caveat is read before the numbers it qualifies (D-06)."
    - "CoverageByDayTable's 1-indexed 'Day N' (Number(dayKey)+1) must equal formatShiftWindow's day math in ScheduleTable (plan 04-05) so the same calendar day reads identically in both tables (RESEARCH.md Pitfall 2)."
    - "CoverageSummary imports Card/CardHeader/CardContent and Tooltip/TooltipContent/TooltipTrigger from the ui components installed in plan 04-01; all three components take already-fetched RunResult fields as props (presentational, never fetch)."
---

<assumption_delta_decision>
The assumption-delta detector fired (`pluralization`, term "alongside") on the ROADMAP phrase "degenerate-solve warnings recorded on the run surfaced alongside them rather than silently dropped."

- Primary noun: unchanged — the run's coverage summary. RES-06 adds a *display* of an already-existing `SolveResult.warnings` field next to the coverage it qualifies.
- Decision: **no-change**. The "alongside" cue is a UI-layout adjacency phrase (warnings shown alongside coverage cards), not an identity-model pluralization — no core assumption lost its monopoly. No second platform, tenant, auth method, or source of truth is introduced; there is one run, one result shape, one warnings array. Advisory (non-blocking) checkpoint recorded per the assumption-delta contract.
</assumption_delta_decision>

<objective>
Build the coverage cluster: the WarningsBanner (D-06/RES-06 — the coverage-honesty caveat), the two-Card stat row with null-safe "Not computed" handling (D-04/D-07/RES-01), and the by-day breakdown table (D-05/RES-01). All three are pure presentational components fed already-fetched RunResult fields.

Purpose: RES-01 folds RES-06 in — the warnings are a coverage-honesty signal that belongs above the numbers it qualifies. The through-line across all three components is this project's never-hide-solver-limitations principle: a null cost says "Not computed" (not $0.00), and a warning is surfaced verbatim (never dropped).
Output: WarningsBanner.tsx, CoverageSummary.tsx, CoverageByDayTable.tsx (+ tests).
</objective>

<execution_context>
@D:/MyData/Projects/rosterai/.claude/gsd-core/workflows/execute-plan.md
@D:/MyData/Projects/rosterai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results-insights/04-RESEARCH.md
@.planning/phases/04-results-insights/04-PATTERNS.md
@.planning/phases/04-results-insights/04-UI-SPEC.md
@frontend/src/components/layout/ErrorBanner.tsx
@frontend/src/components/runs/RunInFlightPanel.tsx
@frontend/src/components/runs/RunHistoryTable.tsx
@frontend/src/api/results.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WarningsBanner (D-06, RES-06)</name>
  <files>frontend/src/components/results/WarningsBanner.tsx, frontend/src/components/results/WarningsBanner.test.tsx</files>
  <read_first>
    - frontend/src/components/layout/ErrorBanner.tsx (exact Alert/AlertTitle/AlertDescription structure to adapt — but note the key deviation: ErrorBanner renders FIXED copy to keep backend internals out of JSX; WarningsBanner instead renders the solver's already-display-ready warning strings verbatim, which is correct here per RESEARCH.md Security Domain)
    - frontend/src/components/runs/RunInFlightPanel.tsx (Alert variant='default' neutral-surface precedent)
    - .planning/phases/04-results-insights/04-PATTERNS.md (WarningsBanner pattern block — including the deviation rationale)
    - .planning/phases/04-results-insights/04-UI-SPEC.md (Copywriting Contract: title "Heads up"; Color: variant='default' + TriangleAlert, no severity color)
  </read_first>
  <behavior>
    - WarningsBanner({ warnings: [] }) renders nothing (returns null) — no empty chrome.
    - WarningsBanner({ warnings: ["a"] }) renders one Alert with title "Heads up" and one paragraph "a".
    - WarningsBanner({ warnings: ["a","b"] }) renders both strings, each as its own paragraph, inside a SINGLE banner (not two stacked banners).
  </behavior>
  <action>
    Create frontend/src/components/results/WarningsBanner.tsx exporting `WarningsBanner({ warnings }: { warnings: string[] })`. Return null when warnings.length === 0. Otherwise render one Alert variant='default' (neutral surface, not destructive) with a lucide-react TriangleAlert icon and AlertTitle "Heads up", and an AlertDescription mapping each warning to its own paragraph element (key on the string), rendered as plain JSX text children. Add a header comment noting the deliberate deviation from ErrorBanner (solver warnings are display-ready and safe to render verbatim, unlike backend error internals). Add WarningsBanner.test.tsx asserting: empty array → renders nothing; one warning → the string is present; multiple warnings → all strings present within a single banner element.
    Keep the raw-HTML-injection sink name out of this action body; describe the plain-text rule by concept only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/WarningsBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/results/WarningsBanner.tsx` exports `WarningsBanner` and returns null for an empty array.
    - `grep -c dangerouslySetInnerHTML src/components/results/WarningsBanner.tsx` returns 0.
    - `npx vitest run src/components/results/WarningsBanner.test.tsx` exits 0 covering empty / one / many warnings, with the multi-warning case rendering a single banner.
  </acceptance_criteria>
  <done>WarningsBanner renders the coverage-honesty caveat verbatim above the stat row, or nothing when there are no warnings — RES-06's surfacing is real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoverageSummary stat row (D-04, D-07, RES-01)</name>
  <files>frontend/src/components/results/CoverageSummary.tsx, frontend/src/components/results/CoverageSummary.test.tsx</files>
  <read_first>
    - frontend/src/components/runs/RunInFlightPanel.tsx (the "purely presentational, receives already-fetched data as props, never fetches" precedent)
    - frontend/src/components/ui/card.tsx and frontend/src/components/ui/tooltip.tsx (the shadcn primitives installed in 04-01 — Card/CardHeader/CardContent; Tooltip/TooltipTrigger/TooltipContent/TooltipProvider)
    - .planning/phases/04-results-insights/04-PATTERNS.md (CoverageSummary null-handling pattern — the CostStat guard; do NOT use `value ?? 0`)
    - .planning/phases/04-results-insights/04-UI-SPEC.md (Typography: Display 28px/600 for the numeric value; Copywriting Contract rows for Total Cost / Total Unmet Hours populated + null; the null tooltip copy)
    - .planning/phases/04-results-insights/04-RESEARCH.md (Pitfall 5: null not NaN not absent; Don't Hand-Roll: Intl.NumberFormat, not manual `$`/thousands building)
  </read_first>
  <behavior>
    - CoverageSummary with total_cost=1234.5 renders the value via Intl.NumberFormat USD; total_unmet_hours=12.3 renders "12.3 h".
    - CoverageSummary with total_cost=null renders "Not computed" (a tooltip trigger), NOT "$0.00" and NOT a plain 0.
    - With total_cost populated and total_unmet_hours=null, the cost card shows the real number and the hours card shows "Not computed" independently.
  </behavior>
  <action>
    Create frontend/src/components/results/CoverageSummary.tsx exporting `CoverageSummary` taking the RunResult metrics fields as props (total_cost, total_unmet_hours — both number|null). Render two shadcn Cards side by side: "Total Cost" and "Total Unmet Hours", with the numeric value in Display type (28px/600) as the page focal point (D-04). Add a small internal stat helper that, when its value is null, renders the literal text "Not computed" wrapped in a Tooltip whose content reads "The solver reached its time limit before finishing this calculation." (D-07); when non-null, Total Cost formats via `new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" })` and Total Unmet Hours renders "{value} h" to one decimal. Each card guards its own value independently (do not gate both on one top-level check). Card labels ("Total Cost", "Total Unmet Hours") use Label type. Add CoverageSummary.test.tsx asserting: populated both → formatted currency + hours present; total_cost null → "Not computed" present and no misleading zero rendered; partial (cost populated, hours null) → the cost number and "Not computed" coexist. Wrap tooltip usage in TooltipProvider as the shadcn primitive requires.
    Do not write the misleading-zero literal in this action; the test asserts its absence.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/CoverageSummary.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/results/CoverageSummary.tsx` exports `CoverageSummary` and renders two Cards.
    - `src/components/results/CoverageSummary.tsx` contains `Intl.NumberFormat` and the literal `Not computed`.
    - `npx vitest run src/components/results/CoverageSummary.test.tsx` exits 0, including the null → "Not computed" case and the partial (one null, one populated) case, asserting no `$0.00`/`0` is rendered for a null value.
  </acceptance_criteria>
  <done>The stat row renders cost/unmet-hours honestly — real numbers formatted, nulls as "Not computed" + tooltip, each card independent — RES-01's D-04/D-07 behavior is real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CoverageByDayTable (D-05, RES-01)</name>
  <files>frontend/src/components/results/CoverageByDayTable.tsx, frontend/src/components/results/CoverageByDayTable.test.tsx</files>
  <read_first>
    - frontend/src/components/runs/RunHistoryTable.tsx (shadcn Table/TableHeader/TableBody/TableRow/TableCell composition to reuse — but drop the scroll container, row-click navigation, and loading/error/empty state machine; this table renders synchronously from already-loaded data)
    - .planning/phases/04-results-insights/04-PATTERNS.md (CoverageByDayTable day-indexing pattern — Number(dayKey)+1; per-day null → "Not computed")
    - .planning/phases/04-results-insights/04-UI-SPEC.md (Copywriting Contract: column headers "Day"/"Coverage %"; per-day null → literal "Not computed", no tooltip at row granularity)
    - frontend/src/api/results.ts (RunResult.metrics.coverage_by_day is Record<string, number|null>)
  </read_first>
  <behavior>
    - CoverageByDayTable with coverage_by_day {"0": 61.2, "1": 88.0} renders rows "Day 1"/"61.2%" and "Day 2"/"88%".
    - A null per-day value renders "Not computed" (plain text, no tooltip).
    - Day labels are 1-indexed (key "0" → "Day 1").
  </behavior>
  <action>
    Create frontend/src/components/results/CoverageByDayTable.tsx exporting `CoverageByDayTable` taking coverage_by_day (Record<string, number|null>) as a prop. Reuse RunHistoryTable's shadcn Table structure (header row "Day" | "Coverage %"), mapping Object.entries(coverage_by_day) to one TableRow each: the Day cell renders `Day ${Number(dayKey) + 1}` (1-indexed — MUST match ScheduleTable's formatShiftWindow convention per Pitfall 2), and the value cell renders `${value}%` when non-null else the literal "Not computed". No scroll container, no row click, no loading/error state. Add a header comment stating the 1-indexed rule and its cross-table consistency requirement. Add CoverageByDayTable.test.tsx asserting: keys "0"/"1" render "Day 1"/"Day 2"; a numeric value renders with a "%"; a null value renders "Not computed".
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/CoverageByDayTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/results/CoverageByDayTable.tsx` exports `CoverageByDayTable` and renders 1-indexed "Day N" labels.
    - `npx vitest run src/components/results/CoverageByDayTable.test.tsx` exits 0, asserting key "0" → "Day 1" and a null value → "Not computed".
  </acceptance_criteria>
  <done>The by-day breakdown table renders 1-indexed day rows with null-safe coverage values, sharing the schedule table's day convention — RES-01's D-05 half is real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver output → browser DOM | SolveResult.warnings are solver-generated free-text strings rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering / Elevation of Privilege | WarningsBanner rendering warnings[] strings | medium | mitigate | Warnings render as plain JSX text children only (no `dangerouslySetInnerHTML`, no `innerHTML`) — React's default text-node escaping is the mitigation; acceptance criterion greps the sink to 0. |
| T-04-07 | Tampering (misrepresentation) | null solver metric shown as a real value | medium | mitigate | Null cost/hours/coverage render literal "Not computed" (never $0.00/0/em-dash) — an honesty control, tested in CoverageSummary.test.tsx and CoverageByDayTable.test.tsx. |
</threat_model>

<verification>
- `npx vitest run src/components/results/WarningsBanner.test.tsx src/components/results/CoverageSummary.test.tsx src/components/results/CoverageByDayTable.test.tsx` all green.
- No component renders warnings via a raw-HTML sink (grep 0).
- Null metrics render "Not computed"; day labels are 1-indexed and match the schedule table.
</verification>

<success_criteria>
The coverage cluster renders honestly: warnings surfaced verbatim above the numbers they qualify, nulls as "Not computed" (never a fabricated zero), and by-day coverage 1-indexed consistently with the schedule table — RES-01 and RES-06 are real for a completed run.
</success_criteria>

## Artifacts this phase produces

- Files: `frontend/src/components/results/WarningsBanner.tsx` (+ test), `frontend/src/components/results/CoverageSummary.tsx` (+ test), `frontend/src/components/results/CoverageByDayTable.tsx` (+ test)
- Symbols: `WarningsBanner`, `CoverageSummary`, `CoverageByDayTable` (React components)

<output>
Create `.planning/phases/04-results-insights/04-04-SUMMARY.md` when done.
</output>
