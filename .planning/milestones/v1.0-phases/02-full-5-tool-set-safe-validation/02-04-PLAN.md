---
phase: 02-full-5-tool-set-safe-validation
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - backend/tests/test_constraints_api.py
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "Validation tests cover unknown task references and unknown member references (each rejected, 200, naming the offending token and listing valid options)."
    - "Validation tests cover out-of-bounds args (scale_demand factor <= 0, set_max_hours = 0, negatives) — each rejected naming the arg."
    - "A mixed multi-tool call where one reference is valid and one is unknown yields applied[] for the valid one AND rejected[] for the invalid one in the same 200 response (criterion 5)."
    - "A mixed multi-tool call where one tool is valid and one has an out-of-bounds arg yields applied[] + rejected[] in the same 200 response."
    - "All TEST-03 cases run against StubLLMProvider + StubEngine via app.dependency_overrides with zero network calls."
  artifacts:
    - "backend/tests/test_constraints_api.py: TEST-03 validation suite (unknown IDs, OOB args, mixed valid/invalid, mixed valid/OOB)"
  key_links:
    - "Tests reuse the existing client fixture (get_engine->StubEngine, get_llm_provider->StubLLMProvider) and scenario_id helper"
  prohibitions:
    - "No test may call a live LLM API or the real Claude provider."
    - "No invalid reference or out-of-bounds arg in any test may end up persisted to scenario.overrides."
---

<objective>
Harden the validation surface with the dedicated TEST-03 suite: unknown IDs,
out-of-bounds args, and the cross-cutting mixed valid/invalid multi-tool calls that
can only be exercised now that all five tools coexist behind the partial-apply
contract.

## Phase Goal (user story)

**As a** scheduler, **I want to** trust that unsafe or unknown constraint fragments
are rejected with clear guidance while the valid fragments in the same sentence
still apply, **so that** one bad reference never silently discards my good edits.

Purpose: Delivers TEST-03 and proves criterion 5 (mixed valid/invalid multi-tool).
Output: A passing validation suite covering unknown task/member refs, out-of-bounds
args, and mixed calls — all stub-driven, no network.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-5-tool-set-safe-validation/02-CONTEXT.md
@.planning/phases/02-full-5-tool-set-safe-validation/02-RESEARCH.md
@.planning/phases/02-full-5-tool-set-safe-validation/02-PATTERNS.md
@.planning/phases/02-full-5-tool-set-safe-validation/02-01-SUMMARY.md
@.planning/phases/02-full-5-tool-set-safe-validation/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates/changes these symbols:
- TEST-03 test functions in test_constraints_api.py: test_unknown_task_rejected, test_unknown_member_rejected, test_scale_demand_bad_factor, test_max_hours_zero_rejected, test_rejection_error_names_ref, test_mixed_valid_invalid_multi_tool, test_mixed_valid_oob_multi_tool
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unknown-reference + out-of-bounds rejection tests</name>
  <files>backend/tests/test_constraints_api.py</files>
  <read_first>
    - backend/tests/test_constraints_api.py (client fixture L67-82; scenario_id helper; applied/rejected shape from Plan 02-01/02-02)
    - .planning/phases/02-full-5-tool-set-safe-validation/02-RESEARCH.md (Validation Architecture test map lines 984-1000; template lines 1002-1024)
    - .planning/phases/02-full-5-tool-set-safe-validation/02-PATTERNS.md (test sections lines 384-431; dependency_overrides isolation lines 499-501)
  </read_first>
  <behavior>
    - An unknown task token returns rejected[] whose error names the token and lists valid tasks; nothing persisted.
    - An unknown member token returns rejected[] whose error names the token and lists valid members; nothing persisted.
    - scale_demand factor <= 0 and set_max_hours = 0 (and a negative variant) each return rejected[] naming the offending arg; nothing persisted.
    - Every case returns HTTP 200 (per-call failures never 400).
  </behavior>
  <action>
    Append to test_constraints_api.py, reusing the existing client and scenario_id fixtures: test_unknown_task_rejected (text referencing a nonexistent task -> 200, len(rejected)==1, token in rejected[0].error); test_unknown_member_rejected (text referencing a nonexistent member -> 200, token in rejected[0].error); test_scale_demand_bad_factor (a scale_demand phrase coerced to factor<=0 -> rejected naming factor); test_max_hours_zero_rejected (a cap phrase with 0 hours -> rejected naming max_hours); test_rejection_error_names_ref asserting a rejection error string contains both the offending reference and at least one valid option. In each rejection test, re-GET or re-read the scenario overrides and assert the rejected item was not persisted.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_constraints_api.py -x -q -k "rejected or bad_factor or zero_rejected or names_ref"</automated>
  </verify>
  <acceptance_criteria>
    - Unknown task and unknown member each produce a rejected entry naming the token at HTTP 200.
    - factor<=0 and max_hours==0 each produce a rejected entry naming the arg.
    - Rejection error strings list valid options; rejected items are never persisted.
  </acceptance_criteria>
  <done>Unknown-ID and out-of-bounds rejection paths are covered and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mixed valid/invalid multi-tool tests (criterion 5)</name>
  <files>backend/tests/test_constraints_api.py</files>
  <read_first>
    - backend/tests/test_constraints_api.py (the rejection tests added in Task 1; client + scenario_id fixtures)
    - .planning/phases/02-full-5-tool-set-safe-validation/02-CONTEXT.md (specifics lines 224-235 — mixed-sentence examples; D-01/D-02)
    - .planning/phases/02-full-5-tool-set-safe-validation/02-RESEARCH.md (test map lines 999-1000)
    - .planning/phases/02-full-5-tool-set-safe-validation/02-PATTERNS.md (mixed test example lines 420-430)
  </read_first>
  <behavior>
    - "at least 2 on <valid task> and exclude <unknown member> from <valid task>" -> 200 with one applied entry and one rejected entry naming the unknown member.
    - A sentence pairing a valid tool with an out-of-bounds arg tool -> 200 with one applied and one rejected naming the bad arg.
    - In both mixed cases only the valid constraint is persisted to scenario.overrides.
  </behavior>
  <action>
    Append test_mixed_valid_invalid_multi_tool: post a two-fragment sentence whose first fragment is a valid set_min_workers (or other valid tool) and whose second references an unknown member; assert 200, len(applied)==1, len(rejected)==1, and the unknown token appears in rejected[0].error. Append test_mixed_valid_oob_multi_tool: post a sentence pairing a valid tool with a scale_demand/set_max_hours fragment carrying an out-of-bounds arg; assert 200, len(applied)==1, len(rejected)==1 naming the arg. In both, re-read scenario overrides and assert only the applied constraint id is present (the rejected fragment was not persisted).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_constraints_api.py -x -q -k "mixed"</automated>
  </verify>
  <acceptance_criteria>
    - A mixed valid/unknown-ref sentence yields applied[]+rejected[] in one 200 response; only the valid one persists.
    - A mixed valid/out-of-bounds sentence yields applied[]+rejected[] in one 200 response; only the valid one persists.
  </acceptance_criteria>
  <done>Criterion 5 mixed multi-tool behavior is proven; full validation suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> app | Stub provider + stub engine injected via dependency_overrides; no live API crosses the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | mixed multi-tool persistence | high | mitigate | Tests assert that in a mixed call only the valid constraint is persisted — proving no hallucinated/out-of-bounds fragment reaches the override store (criterion 5 / VAL-02). |
| T-02-12 | Spoofing | live LLM in CI | medium | mitigate | All tests force StubLLMProvider via app.dependency_overrides[get_llm_provider]; no API key, no network call (TEST-01 invariant). |
| T-02-SC | Tampering | dependency supply chain | low | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd backend && pytest tests/test_constraints_api.py -x -q` green.
- `cd backend && pytest -x -q` full suite green.
</verification>

<success_criteria>
- TEST-03 covers unknown IDs, out-of-bounds args, and mixed valid/invalid multi-tool calls.
- All cases pass stub-driven with zero network calls; no invalid fragment is ever persisted.
</success_criteria>

<output>
Create `.planning/phases/02-full-5-tool-set-safe-validation/02-04-SUMMARY.md` when done.
</output>
