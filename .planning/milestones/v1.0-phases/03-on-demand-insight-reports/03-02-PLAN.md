---
phase: 03-on-demand-insight-reports
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/tests/test_insights_api.py
  - backend/tests/test_llm_provider.py
autonomous: true
requirements: [INS-02, INS-03]
must_haves:
  truths:
    - "Forcing the provider to fail leaves the run status COMPLETED and result_json untouched; only the insight call returns 502 and nothing is cached (D-08, INS-02)"
    - "A report containing a number absent from the run's metrics is rejected by the D-06 guard → 502, nothing cached (D-06, INS-03)"
    - "A run carrying a degenerate-family warning has that warning narrated honestly in the report rather than reported as success (D-05 item 3, INS-03)"
    - "StubLLMProvider.generate_insights is deterministic and performs no I/O (LLM-01, TEST-01)"
  artifacts:
    - "Negative-path + edge test matrix appended to backend/tests/test_insights_api.py"
    - "FailingInsightProvider, FabricatingInsightProvider, CountingInsightProvider test doubles"
    - "generate_insights determinism unit test in backend/tests/test_llm_provider.py"
  key_links:
    - "Failure tests inject a stub via app.dependency_overrides[get_llm_provider]; after a 502 they re-query GET /runs/{id} and /runs/{id}/result to prove run state is intact (INS-02)"
    - "The fabrication test proves the guard lives at the service seam (not in the stub), so it will also protect the Phase-4 real Claude provider (D-06)"
---

<objective>
Harden and prove the safety guarantees of the insight endpoint built in 03-01: the
provider/guard failure path never mutates run state and caches nothing (D-08/INS-02),
the D-06 grounding guard rejects any fabricated number (INS-03), degenerate-family
warnings are narrated honestly (D-05 item 3), and the stub generator is deterministic
(LLM-01/TEST-01). This is the validation + error-state slice of the same feature.

Purpose: Criteria 2 and 3 of the phase are about what must NOT happen (no fabricated
figures, no corrupted run state). The production guarantees already exist in 03-01's
service; this plan locks them in with the negative-path test matrix and the test doubles
that inject failure, fabrication, and call-counting — all stub-driven, zero network.

Output: the failing/fabricating/counting test doubles + the negative + edge test matrix
in test_insights_api.py, and the generate_insights determinism unit test in
test_llm_provider.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-on-demand-insight-reports/03-CONTEXT.md
@.planning/phases/03-on-demand-insight-reports/03-RESEARCH.md
@.planning/phases/03-on-demand-insight-reports/03-PATTERNS.md
@.planning/phases/03-on-demand-insight-reports/03-01-SUMMARY.md
</context>

## Phase Goal

**As a** scheduling operations user, **I want to** trust that fetching an insight report can never fabricate figures or damage my computed schedule, **so that** I can rely on the explanation and on the schedule it describes.

<tasks>

<task type="auto">
  <name>Task 1: Failure-isolation, fabrication-rejection, degenerate-warning, and edge test matrix</name>
  <files>backend/tests/test_insights_api.py</files>
  <read_first>
    - backend/tests/test_insights_api.py (the file created in 03-01 — its `client` fixture, StubEngine, and `_wait_terminal` helper; this task appends to it)
    - backend/services/insight_service.py (03-01 — get_or_generate, InsightGenerationError, _grounding_guard contract being verified)
    - backend/api/routers/runs.py (03-01 — get_run_insights 404/502 mapping)
    - backend/tests/test_constraints_api.py (line 93 — the get_llm_provider override pattern)
    - .planning/phases/03-on-demand-insight-reports/03-PATTERNS.md ("Stub variants for failure / fabrication injection" — FailingInsightProvider, FabricatingInsightProvider, CountingInsightProvider)
    - .planning/phases/03-on-demand-insight-reports/03-RESEARCH.md (Test Map + "Stub-driven test approach" + Pitfall 1/2/4/5)
  </read_first>
  <behavior>
    - test_provider_failure_leaves_run_completed: override get_llm_provider with FailingInsightProvider; GET /runs/{id}/insights → 502; then GET /runs/{id} still shows status COMPLETED and GET /runs/{id}/result is unchanged; then GET /runs/{id}/insights with a WORKING provider succeeds — proving nothing was cached on failure (D-08, INS-02).
    - test_grounding_guard_rejects_fabricated_number: override with FabricatingInsightProvider returning a number absent from metrics; GET → 502 and no insight is cached (a later GET with a working provider still generates) (D-06, INS-03).
    - test_report_narrates_degenerate_warnings: drive a StubEngine variant whose SolveResult carries a warnings entry (a zero-served family); GET → 200 and the report text contains the warning narration, not a generic "coverage was adequate" phrase (D-05 item 3, INS-03).
    - test_unknown_and_failed_run: GET on an unknown run id → 404; GET on a FAILED run → 200 ready=false (no metrics to ground).
    - test_second_fetch_uses_cache (counting variant): with CountingInsightProvider, two SEQUENTIAL fetches yield identical bodies and call_count stays 1 — do NOT assert counts under parallel load (Open Question 1).
  </behavior>
  <action>
    Append to backend/tests/test_insights_api.py the three inline test doubles from PATTERNS.md —
    FailingInsightProvider (generate_insights raises), FabricatingInsightProvider (returns a report
    with a number not in metrics, e.g. a large cost figure the StubEngine never produces), and
    CountingInsightProvider (increments a call counter and emits only grounded numbers). Each double
    also defines a trivial parse_constraints returning [] and a name attribute so it satisfies the
    Protocol. Add the five test functions named exactly as in the behavior block, each installing its
    chosen provider via app.dependency_overrides[get_llm_provider] within the test (or a parametrized
    fixture) and clearing overrides afterward. For the failure and fabrication tests, assert the 502
    AND re-query GET /runs/{id} (status COMPLETED) and GET /runs/{id}/result (body unchanged) to prove
    the insight path never wrote status/result_json (Pitfall 1), then prove nothing was cached by
    swapping back to a working StubLLMProvider and getting a 200 report. For the degenerate-warning
    test, define a local StubEngine variant whose SolveResult.warnings lists a zero-coverage family and
    assert the returned report contains that warning text. For the cache test, assert call_count == 1
    across two sequential GETs only. Keep all assertions stub-driven with zero network calls (TEST-01).
  </action>
  <verify>
    <automated>uv run --directory backend pytest tests/test_insights_api.py -x</automated>
  </verify>
  <done>All insight API tests pass: provider failure returns 502 with run COMPLETED and result_json intact and nothing cached; a fabricated number is rejected with 502 and not cached; a degenerate-family warning is narrated in the report; unknown→404 and FAILED→200 not-ready; sequential cache fetch keeps call_count at 1.</done>
  <acceptance_criteria>
    - `uv run --directory backend pytest tests/test_insights_api.py -x` exits 0 with all of: test_provider_failure_leaves_run_completed, test_grounding_guard_rejects_fabricated_number, test_report_narrates_degenerate_warnings, test_unknown_and_failed_run, test_second_fetch_uses_cache passing.
    - After a forced provider failure, a follow-up GET /runs/{id} reports status COMPLETED (asserted in test_provider_failure_leaves_run_completed).
    - After a forced provider/guard failure, a subsequent GET /runs/{id}/insights with a working provider returns 200 ready=true (proves nothing was cached).
    - `grep -n "FailingInsightProvider\|FabricatingInsightProvider\|CountingInsightProvider" backend/tests/test_insights_api.py` shows all three doubles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: generate_insights determinism unit test (LLM-01 / TEST-01)</name>
  <files>backend/tests/test_llm_provider.py</files>
  <read_first>
    - backend/tests/test_llm_provider.py (full file — existing StubLLMProvider tests + import style; this task appends a test)
    - backend/llm/stub.py (03-01 — StubLLMProvider.generate_insights, the behavior under test)
    - backend/services/serialize.py (the metrics dict shape to build a representative summary input)
  </read_first>
  <behavior>
    - test_generate_insights_deterministic: build a JSON-safe summary dict ({metrics with total_cost, total_unmet_hours, coverage_by_function, coverage_by_day; warnings; overrides}); call StubLLMProvider().generate_insights(summary) twice and assert the two results are equal, non-empty str; assert every numeric token in the output is drawn from the metrics (spot-check that the cost and unmet values appear and no foreign number appears).
  </behavior>
  <action>
    Append test_generate_insights_deterministic to backend/tests/test_llm_provider.py. Construct a
    representative summary dict matching the serialize_result metrics shape (a coverage_by_function
    entry with required_h/served_h/pct as a fraction, a coverage_by_day entry, total_cost,
    total_unmet_hours, a warnings list, and an overrides list of {tool, args}). Instantiate
    StubLLMProvider, call generate_insights twice with the same summary, and assert: the result is a
    non-empty str, the two calls return identical text (determinism, no I/O — TEST-01/LLM-01), and the
    rendered cost and unmet-hours values appear in the text. This proves the stub is reproducible so the
    grounding guard's criterion-2 verification is stable in CI.
  </action>
  <verify>
    <automated>uv run --directory backend pytest tests/test_llm_provider.py -x</automated>
  </verify>
  <done>test_generate_insights_deterministic passes; two calls with the same summary return identical non-empty text; existing provider tests still pass.</done>
  <acceptance_criteria>
    - `uv run --directory backend pytest tests/test_llm_provider.py::test_generate_insights_deterministic -x` exits 0.
    - `uv run --directory backend pytest -q` exits 0 (full suite green).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider seam → service | provider output (possibly failing or fabricated) crosses here; the guard + isolation must contain it |
| insight failure path → runs row | a failure must write nothing — status/result_json/insight_json all stay as-is |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-02 | Tampering / Info disclosure | fabricated numbers in report (D-06) | high | mitigate | test_grounding_guard_rejects_fabricated_number proves the seam-level guard rejects an ungrounded number with 502 and caches nothing (Task 1). |
| T-3-03 | Tampering | run status / result_json integrity (D-08) | high | mitigate | test_provider_failure_leaves_run_completed proves a provider failure leaves status COMPLETED + result_json unchanged and caches nothing (Task 1). |
| T-3-05 | Repudiation / Info disclosure | dishonest "all good" narration over a degenerate solve (INS-03) | medium | mitigate | test_report_narrates_degenerate_warnings proves warnings[] are narrated honestly rather than masked (Task 1). |
| T-3-SC | Tampering | dependency installs | n/a | accept | No new packages this phase — pytest + httpx already vendored; tests use stdlib only (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run --directory backend pytest tests/test_insights_api.py -x` — full negative + edge matrix green.
- `uv run --directory backend pytest tests/test_llm_provider.py -x` — determinism unit test green.
- `uv run --directory backend pytest -q` — full suite green (no network calls; stub-driven).
</verification>

<success_criteria>
- Criterion 2 (INS-03): every number in the report appears in the run's metrics; a fabricated figure is rejected → 502, nothing cached.
- Criterion 3 (INS-02): forcing the provider to fail leaves the run COMPLETED and the schedule result untouched; only the insight call errors.
- Degenerate-family warnings are narrated honestly (no generic "coverage was adequate" language).
- StubLLMProvider.generate_insights is deterministic and I/O-free (LLM-01/TEST-01).
</success_criteria>

<output>
Create `.planning/phases/03-on-demand-insight-reports/03-02-SUMMARY.md` when done.
</output>
